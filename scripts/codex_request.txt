````text
You are Codex working in the GitHub repository `WG-14/bithumb-bot`.

Task: perform a focused follow-up hardening patch for the execution-quality system that already exists on `main`.

Do not reimplement the whole execution-quality feature. The current patch already added:
- `execution_quality.py`
- `ExecutionQualityRecord`
- side-aware slippage calculations
- order-level execution-quality upsert
- `execution-quality-report`
- manifest slippage comparison
- telemetry-first execution quality config
- tests in `tests/test_execution_quality.py`

The remaining concern is narrower and more operationally important:

The execution-quality model is structurally good, but live latency evidence may still be based on fallback event timestamps instead of actual broker request/response timestamps. That weakens the ability to analyze real submit latency and submit-to-fill latency. The next patch should harden the evidence source, expose the operator workflow clearly, and make execution-quality status visible in ops/readiness without unexpectedly blocking live trading.

Keep the core problem explicit:

Accounting correctness is not the same as profitability validity. The repo must be able to prove whether live fills occurred within the research/backtest/paper cost and latency assumptions. Execution quality must remain a first-class, queryable, reproducible operating signal, not just an aggregate report or generic logging.

Hypotheses to verify or refute

H1: Actual broker submit request/response timestamps are not reliably captured in live submit evidence.
The current execution-quality report can work by falling back to order event timestamps, but those timestamps may not represent the true moment the broker request was sent and the true moment the broker response was received.

Verification:
- Inspect `src/bithumb_bot/broker/live_submit_orchestrator.py`.
- Inspect `src/bithumb_bot/broker/live_submission_execution.py`.
- Inspect the actual call path around `run_standard_submit_pipeline()` and `broker.submit_order(...)`.
- Find every `submit_evidence` payload that includes `request_ts` and `response_ts`.
- Determine whether `request_ts` and `response_ts` are currently actual broker-call timestamps or placeholders/fallbacks.
- Determine whether success, timeout, transport error, ambiguous response, and planning failure paths behave correctly.

Expected result:
- For real broker dispatch paths, `request_ts` should be set immediately before the broker call.
- `response_ts` should be set immediately after the broker call returns or after an exception is caught following a sent request.
- Planning failures before dispatch should not pretend a request was sent; they may keep `request_ts=None` and `response_ts=None`, with clear evidence/reason.
- If a request was sent but response is unknown/timeout, preserve `request_ts`; set `response_ts` only when there is a real response/catch timestamp that represents the endpoint returning control.

H2: The execution-quality report currently works, but its latency metrics may be polluted by fallback timestamps.
Verification:
- Inspect `build_execution_quality_record()` in `execution_quality.py`.
- Confirm where `submit_sent_ts_ms`, `submit_response_ts_ms`, `response_latency_ms`, `first_fill_latency_ms`, and `full_fill_latency_ms` come from.
- Keep fallback behavior for historical rows, but make fresh live rows use true request/response timestamps.

H3: The operator workflow exists in code but may not be documented clearly enough for live operations.
Verification:
- Inspect README, RUNBOOK, operations docs, or equivalent docs.
- Determine where live recovery/reporting commands are documented.
- Add a concise section documenting `execution-quality-report`, JSON output, manifest comparison, and how to interpret `INSUFFICIENT_EVIDENCE`, `WARN`, and `FAIL`.

H4: Execution quality gate config exists but may not be visible in readiness/ops surfaces.
Verification:
- Inspect health, ops report, runtime readiness, and live preflight/readiness outputs.
- Determine whether execution quality status appears anywhere operational other than the standalone report.
- Add telemetry-only visibility if missing. Do not introduce default live blocking.

Scope

This is a follow-up hardening patch, not a rewrite.

In scope:
1. Capture actual broker request/response timestamps in live submit evidence.
2. Preserve safe behavior for failure modes.
3. Keep historical fallback logic in execution-quality reconstruction.
4. Add focused tests proving real request/response timestamps are recorded and used.
5. Add or update operator documentation for `execution-quality-report`.
6. Optionally surface execution-quality gate status in ops/readiness/health if this can be done cleanly and without default blocking.

Out of scope:
- Rewriting `execution_quality.py` from scratch.
- Replacing the existing execution-quality table/model.
- Changing strategy rules.
- Tightening slippage thresholds as a substitute for evidence.
- Changing market/limit order policy.
- Enabling hard live blocking by default.
- Removing existing accounting/recovery safeguards.
- Adding heavyweight dependencies.

Implementation requirements

A. Capture actual live broker dispatch timestamps

Find the exact live order dispatch point, likely inside `run_standard_submit_pipeline()` or a helper around `broker.submit_order(...)`.

Implement timestamp capture with clear semantics:
- `request_ts`: wall-clock epoch ms immediately before broker submit call.
- `response_ts`: wall-clock epoch ms when the broker submit call returns successfully or control returns due to an exception after dispatch.
- Optional but preferred: `submit_elapsed_ms` or equivalent monotonic duration if the repo already uses monotonic timing patterns.

Use existing repo time conventions. If there is an existing clock abstraction, use it. If not, use:
- `int(time.time() * 1000)` for persisted wall-clock timestamps.
- `time.monotonic()` only for elapsed durations, not persisted ordering.

B. Preserve failure semantics

Classify paths carefully:

1. Planning failure before broker dispatch
- `request_ts=None`
- `response_ts=None`
- quality/reason should make clear that no request was sent.
- Do not fabricate latency.

2. Failure before send
- `request_ts=None`
- `response_ts=None`
- existing failed-before-send semantics preserved.

3. Sent but timeout / transport error / ambiguous response
- `request_ts` must be present if the broker call was attempted.
- `response_ts` should be set only if the exception point reliably represents control returning after the attempted request.
- `timeout_flag` and existing `SUBMIT_UNKNOWN` / failure reason semantics must remain unchanged.
- Do not weaken unresolved order gates or recovery requirements.

4. Confirmed success
- `request_ts` and `response_ts` must both be present.
- `response_ts >= request_ts`.
- execution-quality reconstruction should prefer these evidence timestamps over order event fallback timestamps.

C. Update submit evidence consistently

Every submit evidence payload for a real dispatch phase should use the captured timestamps instead of hard-coded `None`.

Search and update all relevant payload construction sites:
- planning/preflight/signed/confirmation/application evidence,
- success,
- temporary error,
- broker rejection,
- submission unknown,
- ambiguous response,
- any application phase record that currently stores `request_ts=None` and `response_ts=None`.

If application phase cannot know the original timestamps directly, pass them through from the confirmed submission/request context rather than setting them to `None`.

If adding fields to dataclasses is necessary, do it in a narrow, explicit way. Possible examples:
- add `request_ts_ms` / `response_ts_ms` to the submit context or confirmed submission object,
- or add a small dispatch evidence object used by submit attempt recording.

Do not rely on parsing logs.

D. Keep historical fallback behavior

`execution_quality.py` currently falls back to confirmation event timestamps when evidence timestamps are missing. Keep this behavior for old DB rows.

But for new live rows:
- evidence timestamps should be present,
- `build_execution_quality_record()` should prefer evidence `request_ts`/`response_ts`,
- tests should prove this.

E. Schema and persistence verification

Do not assume the schema is fine just because tests pass.

Verify:
- `execution_quality_events` is created by `ensure_schema()`.
- `client_order_id` is unique or primary-keyed.
- `created_ts` and `updated_ts` exist.
- nulls are allowed for historical/missing evidence fields.
- `upsert_execution_quality_record()` is idempotent.
- re-running `execution-quality-report` does not duplicate rows.

If any of these are not true, patch them with a minimal migration-safe schema change.

F. Operator documentation

Add a concise execution-quality operating section to the most appropriate docs file. Prefer an existing RUNBOOK/operations doc if one exists; otherwise use README or a dedicated operations doc.

Document exact commands:

```bash
uv run bithumb-bot execution-quality-report --limit 100
uv run bithumb-bot execution-quality-report --format json
uv run bithumb-bot execution-quality-report --compare-manifest examples/research/sma_filter_manifest.example.json
sqlite3 "$DB_PATH" ".schema execution_quality_events"
````

Document interpretation:

* `PASS`: enough samples and within configured thresholds/model.
* `WARN`: evidence exists but there are missing links, latency concerns, partial-fill concerns, or other telemetry issues.
* `FAIL`: observed slippage/model breach exceeds configured thresholds.
* `INSUFFICIENT_EVIDENCE`: not enough rows or missing evidence; do not treat this as proof that live execution is healthy.

Document the key operational distinction:

* Accounting reconciliation proves local/exchange trade records agree.
* Execution-quality reporting proves whether live fills matched the assumptions used by research/backtest/paper.

G. Optional ops/readiness visibility

If the repo has an obvious `ops-report`, `health`, or runtime readiness surface, add execution-quality status there in telemetry-only form.

Output shape example:

```text
[EXECUTION-QUALITY]
quality_gate_status=INSUFFICIENT_EVIDENCE
sample_count=12
primary_issue=insufficient_sample
next_action=collect_more_live_execution_quality_samples
```

Do not hard-block live trading by default.
Respect:

* `LIVE_EXECUTION_QUALITY_GATE_ENABLED=false`
* `LIVE_EXECUTION_QUALITY_GATE_MODE=telemetry`

If enabling hard blocking would require broader policy work, leave it disabled and documented.

Testing requirements

Add focused tests. Do not only update snapshots.

Required tests:

1. Successful live submit evidence timestamps

* Simulate or fake the broker submit path.
* Assert submit evidence contains non-null `request_ts` and `response_ts`.
* Assert `response_ts >= request_ts`.
* Assert `build_execution_quality_record()` uses those evidence timestamps for `submit_sent_ts_ms`, `submit_response_ts_ms`, and `response_latency_ms`.

2. Historical fallback remains safe

* Seed a submit attempt with missing `request_ts` and `response_ts`.
* Assert the report still works.
* Assert missing/fallback behavior is explicit and does not crash.

3. Sent-but-error path

* Simulate a broker temporary error, timeout, or submission-unknown path if existing tests support it.
* Assert `request_ts` is recorded when dispatch was attempted.
* Assert existing status/recovery semantics remain unchanged.
* Assert no fake fill latency is produced without fills.

4. Planning failure before dispatch

* Assert no request timestamp is fabricated.
* Assert evidence/reason indicates failure before send or planning failure.

5. CLI/report still passes

* Existing `execution-quality-report` tests must pass.
* Add a test, if needed, that JSON output still includes `quality_gate_status`, `primary_issue`, and `next_action`.

6. Schema/idempotency

* Assert `execution_quality_events` exists after `ensure_db()`.
* Assert repeated `refresh_execution_quality_records()` or repeated report runs do not duplicate rows for the same `client_order_id`.

7. Regression safety

* Existing order submission tests pass.
* Existing recovery/accounting tests pass.
* Existing execution quality tests pass.
* Existing paper/backtest/research tests pass if relevant.

Suggested commands to run

Run the smallest relevant set first, then the broader suite:

```bash
uv run pytest tests/test_execution_quality.py
uv run pytest tests -q
```

If the repo has formatting/typecheck conventions, run them too.

Acceptance criteria

The patch is acceptable only if:

* New live submit evidence records actual broker request/response timestamps for real dispatches.
* Planning failures and failed-before-send paths do not fabricate request timestamps.
* Sent-but-error paths preserve request timing when a broker call was attempted.
* Execution-quality reconstruction prefers real evidence timestamps but still handles historical missing evidence.
* Latency metrics are no longer primarily dependent on order event fallback for new live submissions.
* Operator docs explain how to run and interpret `execution-quality-report`.
* Any ops/readiness integration is telemetry-only unless explicitly configured.
* Existing safety, accounting, recovery, and unresolved-order behavior are not weakened.
* Tests cover success, fallback, failure-before-send, sent-but-error where feasible, CLI/report, and idempotency.

Final report format

When done, report in this exact structure:

1. Summary

* One paragraph explaining what changed and why this improves execution-quality evidence rather than just reporting.

2. Files changed

* List each changed file and one-line purpose.

3. Hypothesis results

* H1: supported/refuted/partially supported, with repo evidence.
* H2: supported/refuted/partially supported, with repo evidence.
* H3: supported/refuted/partially supported, with repo evidence.
* H4: supported/refuted/partially supported, with repo evidence.

4. Timestamp semantics

* Explain exactly what `request_ts`, `response_ts`, and any elapsed duration mean.
* Explain how success, planning failure, failed-before-send, timeout/transport error, and ambiguous response are handled.

5. Operator workflow

* Exact commands to run.
* Example output shape.
* Explanation of PASS/WARN/FAIL/INSUFFICIENT_EVIDENCE.

6. Tests

* Exact tests added or updated.
* Exact commands run.
* Pass/fail result.

7. Recovery and safety notes

* Explain idempotency/upsert behavior.
* Explain historical missing-evidence fallback.
* Explain why live behavior is not unexpectedly blocked by default.

8. Remaining risks / follow-up

* Only concrete remaining work, such as hard gate rollout, paper-live-gap-report, or deeper readiness integration.

```
```

