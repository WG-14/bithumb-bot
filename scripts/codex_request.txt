Use the attached incident-analysis context as background, but do not treat it as proven fact. Verify it directly from `WG-14/bithumb-bot`. 

You are working in the GitHub repository `WG-14/bithumb-bot`.

The repository has already been patched multiple times after an EC2 live-trading accounting incident. Your task is **not** to repeat the previous fee-authority patch mechanically. The fee-authority path appears substantially improved. Your next task is to verify that improvement, then harden the remaining recovery system so the repo stops confusing accounting-root-cause incidents with market-risk incidents.

The next priority is **recovery policy**, not fee validation.

Keep the priority sharp:

```text
The repo should move from:
  one wrong fee estimate can destabilize accounting and trigger flatten/manual repair loops

to:
  exchange paid_fee is settlement authority when coherent,
  fee-estimate drift is visible diagnostics,
  accounting-root-cause incidents do not recommend flatten by default,
  repair paths are auditable and reproducible,
  open_position_lots is treated as a rebuildable projection rather than canonical truth.
```

## Incident context

On EC2, live trading repeatedly entered this failure cycle after real Bithumb BUY/SELL fills:

```text
Bithumb order fills successfully
→ local fee validation classifies the fill as fee_pending
→ local fills/trades/portfolio/lot accounting becomes incomplete or inconsistent
→ broker/local portfolio mismatch appears
→ startup/resume gates block live
→ recovery suggests flatten or manual repair
→ flatten itself creates another real fill
→ that fill goes through the same fee/accounting failure path
→ operator manually repairs fee/principal/lot state and sometimes bot_health
```

Runtime evidence from the incident:

```text
Actual Bithumb mobile-app fee rate: 0.04% = 0.0004 = ~4 bps

DB-observed examples:
flatten SELL fee_bps = 3.999784
latest BUY fee_bps   = 3.999902
previous SELL        = 3.998178
previous BUY         = 3.999148

Resolved setting during incident:
LIVE_FEE_RATE_ESTIMATE = 0.0025
LIVE_FILL_FEE_ALERT_MIN_NOTIONAL_KRW = 10000.0
```

The likely direct trigger was:

```text
configured expected live fee rate = 0.0025
actual Bithumb paid fee rate      = 0.0004
→ expected_fee_rate_mismatch
→ old behavior: order_level_candidate / fee_pending
→ accounting/recovery cascade
```

But do **not** reduce the fix to `LIVE_FEE_RATE_ESTIMATE=0.0004`. That is operational hygiene, not the repository-level fix. The repository-level fix is that coherent exchange `paid_fee` must outrank configured estimates for settlement accounting.

## Current patch state to verify

Verify this from the current repo before making further changes.

### Fee authority appears substantially improved

`FeeEvaluation` appears to include explicit semantic fields:

```python
fee_authority
accounting_decision
accounting_eligibility
diagnostic_flags
```

Expected invariant:

```text
exchange order-level paid_fee
+ single fill
+ executed_volume/executed_funds/identifiers match
→ fee_authority="exchange_order_paid_fee_single_fill"
→ accounting_decision="finalize"
→ accounting_status="accounting_complete"

LIVE_FEE_RATE_ESTIMATE mismatch
→ diagnostic_flags contains "expected_fee_rate_mismatch"
→ not fee_pending by itself
```

Verify in:

```text
src/bithumb_bot/fee_observation.py
src/bithumb_bot/broker/bithumb_order_queries.py
src/bithumb_bot/broker/live_submission_execution.py
src/bithumb_bot/broker/myorder_runtime.py
src/bithumb_bot/execution.py
tests/test_fee_observation.py
tests/test_myorder_runtime_ingestion.py
tests/test_bithumb_private_api.py
tests/test_live_broker.py
```

### Operational diagnostics appear improved but need surface checks

`fee-diagnostics` / `reporting.py` appears to expose fee-rate drift diagnostics. Verify whether equivalent or summarized drift appears on operator restart surfaces:

```text
bithumb-bot health
bithumb-bot recovery-report
bithumb-bot restart-checklist
```

At minimum, if drift is present, at least one restart/recovery surface should tell the operator to review fee diagnostics.

### Remaining suspected gap

Searches did not clearly show explicit policy surfaces such as:

```text
forensic_fee_authority_diagnosis
accounting_root_cause
flatten_primary_recommendation=0
flatten_not_primary
```

So the suspected remaining gap is:

```text
Recovery may still lack a first-class accounting-root-cause mode.
Flatten may still be too easy to recommend when accounting evidence is unreliable.
Repair commands may still be individually safe but not integrated into one auditable repair-plan surface.
open_position_lots may still behave too much like source-of-truth rather than a deterministic projection.
```

Verify or falsify these suspicions from the repo.

## Working hypotheses

Keep these as hypotheses until code/tests prove them.

### Hypothesis A — fee authority problem is mostly fixed

Coherent exchange `paid_fee` is now settlement authority and configured fee-rate mismatch is only diagnostic.

Verification:

```bash
uv run pytest tests/test_fee_observation.py
uv run pytest tests/test_myorder_runtime_ingestion.py -k "paid_fee or expected_fee_rate"
uv run pytest tests/test_bithumb_private_api.py -k "fee or paid_fee or expected_fee_rate or single_fill"
uv run pytest tests/test_live_broker.py -k "fee or paid_fee or expected_fee_rate or accounting"
```

Falsification condition:

```text
Any REST/private/live path still converts:
  coherent exchange paid_fee + estimate mismatch
into:
  order_level_candidate / fee_pending / principal_applied_fee_pending
```

### Hypothesis B — recovery still does not clearly separate accounting-root-cause from market-risk

Accounting-root-cause incidents should not make flatten the primary recommendation. Flatten is a real order that creates another fill and accounting event. If accounting is unreliable, flatten can amplify the incident.

Verification target:

```text
accounting_root_cause_unresolved=1
accounting_evidence_reliable=0
maybe actual_executable_exposure=1
→ recovery_mode=forensic_accounting
→ additional_orders_allowed=0
→ flatten_primary_recommendation=0
→ recommended_action=collect_broker_fill_evidence_and_build_repair_plan
```

Falsification condition:

```text
recovery-report / restart-checklist already has this policy and tests enforce it.
```

If not present, add it.

### Hypothesis C — repair commands are individually safe but not integrated into a repair-plan layer

The repo has many repair commands and many safety tests, such as:

```text
manual-flat-accounting-repair
fee-gap-accounting-repair
fee-pending-accounting-repair
rebuild-position-authority
external-cash-adjustment
cash-drift-report
```

These appear to have preview, confirmation, non-mutating, unsafe-refusal, and convergence tests. That is good. The gap is operator orchestration: the operator still has to infer which command to run, in what order, from multiple surfaces.

Verification target:

```text
Does the repo already have:
  bithumb-bot doctor --live --explain
  bithumb-bot repair-plan --live
  bithumb-bot repair-apply --plan-id ...

or an equivalent integrated preview that lists:
  incident_class
  recommended_mode
  additional_orders_allowed
  flatten_primary_recommendation
  candidate_repairs
  preconditions
  touched_tables
  before/after portfolio
  before/after lot projection
  idempotency key
  rollback/backup path
  expected audit-ledger result
  why safe
```

Falsification condition:

```text
recovery-report already fully orders all repair previews with these fields and tests enforce it.
```

If not present, add a first scoped `repair-plan` preview. Do **not** over-scope into full repair-apply unless the repo already has enough infrastructure.

### Hypothesis D — open_position_lots still needs explicit projection semantics

`open_position_lots` should not be canonical settlement truth. It should be a deterministic, rebuildable materialized projection.

Target invariant:

```text
canonical truth:
  fills
  trades
  fee finalization / adjustment events
  external adjustments
  repair events

projection:
  open_position_lots
```

Verification target:

```text
When broker and canonical portfolio converge, but open_position_lots projection drifts:
  recovery should identify projection drift,
  not treat it automatically as market-risk exposure,
  not recommend flatten by default,
  and route to projection repair / repair-plan under safe preconditions.
```

Falsification condition:

```text
repo already has explicit tests proving projection drift does not produce flatten-first guidance.
```

If not present, add tests or policy.

## Required design changes

### 1. Add explicit accounting-root-cause recovery mode if missing

Add a first-class mode in recovery/reporting/startup guidance, with naming close to:

```text
recovery_mode=forensic_accounting
incident_class=ACCOUNTING_ROOT_CAUSE
accounting_evidence_reliable=0
additional_orders_allowed=0
flatten_primary_recommendation=0
recommended_action=collect_broker_fill_evidence_and_build_repair_plan
```

Trigger it when any of the following are active or unresolved:

```text
active fee pending
fee authority / fee-rate drift with active accounting issue
broker/local mismatch caused by accounting projection
position authority projection drift without reliable accounting evidence
unresolved fee gap recovery
manual accounting repair required
projection convergence required without safe accounting evidence
```

Do not block all flatten forever. Flatten remains valid only under the market-risk mode:

```text
actual executable exposure exists
accounting evidence is reliable
open orders / unresolved fills are clean
broker/local portfolio is converged
fee/accounting root cause is not active
```

### 2. Suppress flatten as primary recommendation during accounting-root-cause incidents

In `recovery-report`, `restart-checklist`, and resume guidance, ensure this invariant:

```text
accounting_root_cause_unresolved
→ flatten_primary_recommendation=0
→ additional_orders_allowed=0
→ recommended_action=forensic_accounting_diagnosis_or_repair_plan
```

Do not remove flatten command. Change guidance so it is not the first-line recovery for accounting-root-cause incidents.

Search and patch where needed:

```text
src/bithumb_bot/engine.py
src/bithumb_bot/app.py
src/bithumb_bot/recovery.py
src/bithumb_bot/runtime_readiness.py
src/bithumb_bot/reporting.py
```

Search terms:

```text
flatten
flatten-position
manual-flat
recommended_command
recommended_action
next_required_action
resume_guidance
restart_checklist
recovery_report
```

### 3. Add a scoped repair-plan preview layer

Do not attempt a full repair engine if too large. Start with read-only preview.

Add CLI if feasible:

```bash
bithumb-bot repair-plan
```

or:

```bash
bithumb-bot doctor --explain
```

The command must be non-mutating by default.

It should aggregate existing repair preview functions where available, such as:

```text
fee-pending accounting repair preview
fee-gap accounting repair preview
manual-flat accounting repair preview
position-authority rebuild preview
external-cash-adjustment / cash-drift preview if applicable
```

Output must include at least:

```text
[REPAIR-PLAN]
  plan_id=...
  mode=...
  primary_incident_class=...
  recommended_mode=forensic_accounting | recovery | market_risk
  additional_orders_allowed=0|1
  flatten_primary_recommendation=0|1

  candidate_repairs:
    - name=...
      needed=0|1
      active_issue=0|1
      safe_to_apply=0|1
      preconditions=...
      touched_tables=...
      expected_after=...
      idempotency_key=...
      rollback_or_backup=...
      why_safe=...
```

This command should not require the operator to guess between five separate repair commands.

### 4. Make open_position_lots projection semantics explicit

Add code comments, tests, and/or operator output that state and enforce:

```text
open_position_lots is a rebuildable projection.
It is not canonical settlement truth.
Canonical truth comes from fills/trades/fee finalization/adjustment events/external adjustments/repair events.
```

Add or improve projection drift output so the operator sees:

```text
canonical_portfolio_qty
broker_qty
open_position_lots_projected_qty
projection_converged
source_of_truth=fills+trades+fee_adjustments
projection_kind=open_position_lots
rebuildable=1
safe_to_rebuild=...
reason=...
```

Prefer including this in `repair-plan` or `rebuild-position-authority` preview.

### 5. Keep fee-rate drift visible on restart/recovery surfaces

`fee-diagnostics` is not enough. Operators commonly run:

```bash
bithumb-bot health
bithumb-bot recovery-report
bithumb-bot restart-checklist
```

If fee-rate drift is present, expose at least a compact summary or pointer:

```text
fee_rate_drift=diagnostic_only
configured_fee_bps=25.000
observed_fee_bps_median=4.000
startup_impact=diagnostic_only_without_active_fee_pending
action=review_fee_diagnostics
```

This should not become startup-blocking unless there is active fee pending/accounting issue. The distinction must be visible.

## Required tests

Add or strengthen tests in the repo. Prefer focused tests over broad fragile ones.

### Fee authority regression tests

Verify existing tests still pass:

```bash
uv run pytest tests/test_fee_observation.py
uv run pytest tests/test_myorder_runtime_ingestion.py -k "paid_fee or expected_fee_rate"
uv run pytest tests/test_bithumb_private_api.py -k "fee or paid_fee or expected_fee_rate or single_fill"
uv run pytest tests/test_live_broker.py -k "fee or paid_fee or expected_fee_rate or accounting"
```

They must prove:

```text
coherent exchange paid_fee + configured_fee_rate mismatch
→ fee_authority=exchange_order_paid_fee_single_fill
→ accounting_decision=finalize
→ accounting_status=accounting_complete
→ diagnostic_flags contains expected_fee_rate_mismatch
→ not fee_pending
```

### Recovery policy tests

Add tests such as:

```text
test_recovery_report_suppresses_flatten_when_accounting_root_cause_unresolved
test_restart_checklist_recommends_forensic_mode_for_fee_authority_drift
test_resume_guidance_does_not_recommend_flatten_when_accounting_evidence_unreliable
test_recovery_allows_flatten_when_market_risk_exposure_exists_and_accounting_evidence_reliable
```

Required assertions:

```text
accounting_root_cause_unresolved=1
→ recovery_mode=forensic_accounting
→ flatten_primary_recommendation=0
→ additional_orders_allowed=0
→ recommended_action includes repair-plan or forensic accounting diagnosis
```

And the positive market-risk case:

```text
actual executable exposure=1
accounting_evidence_reliable=1
no unresolved fee/accounting blockers
→ flatten may be recommended
```

### Repair-plan tests

If you add `repair-plan`, add tests such as:

```text
test_repair_plan_preview_is_non_mutating
test_repair_plan_lists_fee_pending_fee_gap_manual_flat_and_position_authority_candidates
test_repair_plan_marks_additional_orders_disallowed_for_accounting_root_cause
test_repair_plan_includes_preconditions_touched_tables_expected_after_and_why_safe
```

Required assertions:

```text
[REPAIR-PLAN]
plan_id=
primary_incident_class=
recommended_mode=
additional_orders_allowed=
flatten_primary_recommendation=
candidate_repairs:
preconditions=
touched_tables=
expected_after=
idempotency_key=
rollback_or_backup=
why_safe=
```

### Projection semantics tests

Add tests such as:

```text
test_position_authority_rebuild_treats_lots_as_projection_when_broker_and_portfolio_converge
test_projection_drift_does_not_recommend_flatten_when_accounting_evidence_is_unreliable
test_repair_plan_includes_position_projection_drift_candidate
```

Required assertions:

```text
broker_portfolio_converged=1
canonical_portfolio_qty matches broker_qty
open_position_lots_projected_qty differs
→ projection_drift incident
→ repair-plan / rebuild-position-authority route
→ flatten_primary_recommendation=0
```

### Observability tests

Add or strengthen tests for operator surfaces:

```text
test_health_surfaces_fee_rate_drift_summary
test_recovery_report_surfaces_fee_rate_drift_summary
test_restart_checklist_surfaces_fee_rate_drift_summary
```

Required output fields:

```text
fee_rate_drift=
configured_fee_bps=
observed_fee_bps_median=
startup_impact=
action=review_fee_diagnostics
```

## Required verification commands

Run targeted tests first, then relevant broader subsets.

At minimum attempt:

```bash
uv run pytest tests/test_fee_observation.py
uv run pytest tests/test_myorder_runtime_ingestion.py -k "paid_fee or expected_fee_rate"
uv run pytest tests/test_bithumb_private_api.py -k "fee or paid_fee or expected_fee_rate or single_fill"
uv run pytest tests/test_live_broker.py -k "fee or paid_fee or expected_fee_rate or accounting"
uv run pytest tests/test_operator_commands.py -k "repair_plan or recovery or restart or diagnostics or flatten or projection"
uv run pytest tests/test_accounting_projection.py -k "projection or lot or repair or fee"
```

If a command fails or is too broad/slow, report:

```text
exact command
failing test names
failure summary
whether it is related or unrelated
whether you added a narrower replacement test
```

## EC2 operational follow-up to preserve in final report

Even though repository-level safety must not depend on it, the EC2 live env should still be corrected:

```bash
LIVE_FEE_RATE_ESTIMATE=0.0004
```

Before live restart, the operator should verify:

```bash
cd ~/apps/bithumb-bot
git rev-parse HEAD
sudo systemctl cat bithumb-bot

set -a
source /home/ec2-user/bithumb-runtime/env/live.verify.env
set +a
export MODE=live
export ENV_ROOT=/home/ec2-user/bithumb-runtime/env

uv run python - <<'PY'
from bithumb_bot.config import settings
print("LIVE_FEE_RATE_ESTIMATE", settings.LIVE_FEE_RATE_ESTIMATE)
PY

MODE=live uv run bithumb-bot audit
MODE=live uv run bithumb-bot audit-ledger
MODE=live uv run bithumb-bot fee-diagnostics
MODE=live uv run bithumb-bot health
MODE=live uv run bithumb-bot recovery-report
MODE=live uv run bithumb-bot restart-checklist
MODE=live uv run bithumb-bot repair-plan
```

After the first real fill:

```bash
MODE=live uv run bithumb-bot health
MODE=live uv run bithumb-bot audit-ledger
MODE=live uv run bithumb-bot recovery-report
MODE=live uv run bithumb-bot repair-plan
```

Expected post-fill outcome:

```text
expected_fee_rate_warning may appear
fee_pending should not increase solely due to estimate mismatch
principal_applied_fee_pending should not increase solely due to estimate mismatch
no broker/local mismatch
broker_portfolio_converged=1
lot_projection_converged=1
blockers=none
if accounting_root_cause_unresolved appears:
  flatten_primary_recommendation=0
  recommended_mode=forensic_accounting
```

## Scope boundaries

Do:

```text
- Verify the current fee-authority patch through REST/private/live paths.
- Preserve coherent exchange paid_fee as settlement authority.
- Add accounting-root-cause recovery mode if missing.
- Suppress flatten as primary recommendation when accounting evidence is unreliable.
- Add a scoped non-mutating repair-plan preview if missing.
- Make open_position_lots projection semantics explicit.
- Improve health/recovery/restart observability for fee drift and accounting-root-cause mode.
- Preserve unsafe fee evidence fail-closed behavior.
- Keep changes deterministic and idempotent.
```

Do not:

```text
- Treat LIVE_FEE_RATE_ESTIMATE=0.0004 as the whole fix.
- Hide expected_fee_rate_mismatch.
- Let configured estimates act as authoritative live settlement data.
- Relax genuinely unsafe fee evidence.
- Add manual DB UPDATE workarounds.
- Make flatten the primary response to accounting drift.
- Rewrite the entire accounting engine unless a small safe abstraction already exists.
- Build a full repair-apply engine unless the repo infrastructure already supports it cleanly.
```

## Final report format

When finished, produce a report with exactly these sections:

```text
1. Summary
   - What changed.
   - Which previous fee-authority improvements were verified.
   - What new recovery/repair/projection gaps were fixed.

2. Hypothesis results
   - Hypothesis A: fee authority mostly fixed — confirmed / falsified / partially confirmed.
   - Hypothesis B: recovery lacks accounting-root-cause vs market-risk separation — confirmed / falsified / partially confirmed.
   - Hypothesis C: repair commands lack integrated repair-plan layer — confirmed / falsified / partially confirmed.
   - Hypothesis D: open_position_lots projection semantics remain under-specified — confirmed / falsified / partially confirmed.

3. Design assessment
   - Whether exchange paid_fee outranks configured fee estimates for settlement accounting.
   - Whether expected fee-rate mismatch is preserved as diagnostics.
   - Whether unsafe fee evidence still fails closed.
   - Whether recovery now avoids flatten as default for accounting-root-cause incidents.
   - Whether repair-plan gives operators a non-mutating plan.
   - Whether open_position_lots is treated as projection rather than canonical truth.
   - Remaining architectural risks.

4. Code changes
   - Files changed.
   - Key behavior changes.
   - Why this prevents a problem class rather than suppressing a symptom.

5. Tests
   - Tests added/updated.
   - Recovery policy tests.
   - Repair-plan tests.
   - Projection semantics tests.
   - Observability tests.
   - Existing fee-authority regression tests run.

6. Verification
   - Commands run.
   - Pass/fail results.
   - Any unrelated failures and why they are considered unrelated.

7. Operational follow-up
   - EC2 env change still required, if any.
   - Commands the operator should run before live restart.
   - Commands the operator should run after the first fill.
   - Follow-up issues recommended.
```

The goal is to evolve `WG-14/bithumb-bot` from a collection of safe individual repair commands into a recovery system that can clearly say:

```text
This is an accounting-root-cause incident.
Do not flatten as the first response.
Do not create new orders.
Here is the evidence.
Here is the non-mutating repair plan.
Here is why the plan is safe.
Here is how to verify it before live restart.
```

