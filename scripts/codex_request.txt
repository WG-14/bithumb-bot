You are Codex working directly in the GitHub repository `WG-14/bithumb-bot`.

Use the attached review notes as source context, but do not treat any claim as proven until you verify it against the current repository state. The latest review concluded that the core structural P0 issues are now largely closed, while one or two narrow regression hardening tasks remain. Preserve that priority: do not reopen the whole design unless current code disproves the latest review. The core context is summarized in the uploaded review notes.

## Core objective

Perform a final, narrow hardening pass on the research/runtime SMA decision-equivalence system.

The repository appears to now have the important promotion-grade loop:

```text
repo-owned research-export-decisions --profile
  -> repo-owned runtime-replay-decisions
  -> decision-equivalence over validated export artifacts
  -> profile transition evidence validation
```

The main remaining risk is not a major design gap anymore. It is regression exposure: public CLI dispatch and strengthened evidence-validation fields must be pinned by tests so future changes cannot silently break the loop.

Do not generalize this into trading advice. Inspect, patch, and test the actual repo.

## Files to inspect first

Inspect at minimum:

```text
src/bithumb_bot/app.py
src/bithumb_bot/profile_cli.py
src/bithumb_bot/decision_equivalence.py
src/bithumb_bot/approved_profile.py
src/bithumb_bot/canonical_decision.py
tests/test_profile_cli_decision_loop.py
tests/test_decision_equivalence_canonical.py
tests/test_approved_profile.py
docs/research-validation.md
docs/runbooks/research-to-paper.md
README.md
```

Also inspect the actual argparse parser and dispatch branches in `app.py`. Do not stop at import statements.

## Hypotheses to validate or falsify

Keep these as hypotheses until verified in the current repo.

### H1. The major P0 loop is probably closed

The current repo may already prove the positive repo-owned loop through a test such as:

```text
test_repo_owned_export_replay_artifacts_can_pass_positive_equivalence
```

Validate that this test actually uses repo-owned research export and runtime replay paths, validates export wrappers, and asserts:

```text
result.ok is True
promotion_grade_comparison is True
legacy_or_unverified_export is False
mismatch_count == 0
missing_research_decisions == []
missing_runtime_decisions == []
research_export_content_hash present
runtime_export_content_hash present
```

Falsify only if the test is merely synthetic and does not exercise repo-owned generation paths.

### H2. `decision-equivalence` public CLI dispatch may still be unpinned

There may already be `app.main()` dispatch tests for:

```text
research-export-decisions
runtime-replay-decisions
```

but not for:

```text
decision-equivalence
```

Validate whether `tests/test_profile_cli_decision_loop.py` or another test proves `app.main(["decision-equivalence", ...])` reaches `cmd_decision_equivalence()` with parsed arguments.

Falsify only if such a test already exists.

### H3. Strengthened evidence-validator rejection fields may be only partially pinned

The evidence fixture may include:

```text
promotion_grade_comparison=True
repo_owned_export_artifacts=True
legacy_or_unverified_export=False
research_export_content_hash
runtime_export_content_hash
```

Validate whether mutation tests prove profile transition evidence fails when these fields are missing or bad.

Falsify only if each relevant field has regression coverage, or if the validator code already has equivalent generic coverage that makes additional per-field tests redundant.

### H4. Candidate/profile binding is likely closed

The latest code may already validate source promotion candidate id and rebuild candidate profile hash inside `_validate_research_export_profile_binding()`.

Validate this; do not patch unless it is missing or weak.

### H5. Exit-rule scope should remain unchanged

The current SMA runtime exit-rule scope should remain:

```text
opposite_cross
max_holding_time
```

Standalone hard `stop_loss` and `take_profit` should remain unsupported unless deliberately implemented as named rules with profile parameters, docs, and research/runtime equivalence coverage.

This pass should not add stop-loss or take-profit rules.

## Required work

### P0: Confirm positive repo-owned loop test

If the positive test exists and is real, leave it intact and mention it in the final report.

If it does not exist, add it. It must use either the public command functions or the exact underlying repo-owned export/replay functions:

```text
cmd_research_export_decisions(...)
cmd_runtime_replay_decisions(...)
load_decision_export_artifact(...)
compare_decision_export_artifacts(...)
```

The test must use validated export artifacts, not only handcrafted decision dicts.

Minimum assertions:

```text
research_artifact.source == "research"
runtime_artifact.source == "runtime_replay"
both artifacts validate their content_hash
all decision profile_content_hash values equal approved profile hash
compare_decision_export_artifacts(...).ok is True
promotion_grade_comparison is True
legacy_or_unverified_export is False
mismatch_count == 0
missing_research_decisions == []
missing_runtime_decisions == []
```

### P1: Add public CLI dispatch test for `decision-equivalence`

If missing, add a focused test in `tests/test_profile_cli_decision_loop.py` or a new CLI test file.

Patch the command function and call `app.main()` so the test proves argparse/dispatch wiring, not only direct function behavior.

Example intent:

```python
def test_cli_dispatch_reaches_decision_equivalence(monkeypatch):
    calls = {}

    def fake_cmd(**kwargs):
        calls.update(kwargs)
        return 0

    monkeypatch.setattr(app, "cmd_decision_equivalence", fake_cmd)

    assert app.main([
        "decision-equivalence",
        "--research-decisions", "research.json",
        "--runtime-decisions", "runtime.json",
        "--profile-hash", "sha256:profile",
        "--market", "KRW-BTC",
        "--interval", "1m",
        "--data-fingerprint", "sha256:data",
    ]) == 0

    assert calls == {
        "research_decisions_path": "research.json",
        "runtime_decisions_path": "runtime.json",
        "profile_hash": "sha256:profile",
        "market": "KRW-BTC",
        "interval": "1m",
        "data_fingerprint": "sha256:data",
    }
```

Use the repo’s actual argument names if they differ.

### P1: Confirm or add evidence-validator rejection tests

Inspect `_validate_decision_equivalence_evidence()` in `approved_profile.py` and existing tests in `tests/test_approved_profile.py`.

If not already covered, add deterministic mutation tests proving profile transition evidence fails when:

```text
promotion_grade_comparison != true
legacy_or_unverified_export == true
repo_owned_export_artifacts != true
research_export_content_hash missing
runtime_export_content_hash missing
canonical_incomplete_decision_count > 0
canonical_missing_field_count > 0
binding_validation non-empty
missing_research_decisions non-empty
missing_runtime_decisions non-empty
mismatch_count != 0
comparison_contract_version != canonical_decision_v1
canonical_schema != true
legacy_schema == true
```

Important: recompute report `content_hash` after mutating the report so the test proves semantic validation, not only hash mismatch.

Do not overfit by adding dozens of redundant tests if a parametrized test can cover these cases.

### P1: Confirm candidate/profile binding is fully pinned

Inspect `_validate_research_export_profile_binding()`.

If current code already checks:

```text
source promotion candidate_id == selected candidate id
rebuilt candidate_profile_hash == approved profile candidate_profile_hash
strategy parameters match
cost model matches
manifest hash matches
dataset hash matches
```

then do not patch it. Mention in the report.

If any of those are missing, add the missing validation and tests.

### P2: Improve operator recovery language only if stale

Inspect `_recommended_next_action()` and report generation.

If it still emits older generic actions in cases that now have repo-specific recovery, add or adjust mappings such as:

```text
decision_export_artifact_unverified -> regenerate_decisions_with_repo_owned_export_commands
research export/profile mismatch -> reexport_research_decisions_with_approved_profile_hash
runtime replay/regime mismatch -> replay_runtime_with_approved_regime_policy
profile/market/interval/data binding mismatch -> bind_decisions_to_requested_profile_market_interval_data_fingerprint
flat/no-dust state mismatch -> align_flat_position_canonical_state
runtime-only state mismatch -> scope_runtime_only_state_as_fail_closed_or_extend_research_model
```

Do not spend time here if current report actions are already actionable and tested.

## Scope rules

Do not weaken existing safety gates.

Do not:

```text
drop canonical comparison fields to make tests pass
allow manual canonical JSON as promotion-grade evidence
allow unverified export wrappers as promotion-grade evidence
allow research/runtime profile hash mismatch
bypass approved profile validation
bypass evidence-chain validation
call live broker APIs from tests
submit real orders
add hidden stop_loss or take_profit behavior
perform a large shared-decision-kernel refactor in this pass
```

This is a final regression-hardening pass, not a redesign.

## Tests to run

Run targeted tests first:

```bash
pytest -q tests/test_profile_cli_decision_loop.py
pytest -q tests/test_decision_equivalence_canonical.py
pytest -q tests/test_approved_profile.py
pytest -q tests/test_research_backtest_reproducibility.py
pytest -q tests/test_sma_with_filter_strategy.py
pytest -q tests/test_strategy_exit_rules.py
pytest -q tests/test_research_promotion_gate.py
```

Then run the full suite if feasible:

```bash
pytest -q
```

If full pytest is too slow or fails due to unrelated environmental assumptions, run all changed tests and clearly report what was not run and why.

## Acceptance criteria

The patch is acceptable only if:

```text
1. The positive repo-owned export/replay/equivalence test exists and passes.
2. `decision-equivalence` public CLI dispatch is tested through `app.main()`.
3. Evidence validator rejection for non-promotion-grade or unverified decision-equivalence reports is either already covered or newly covered.
4. Candidate/profile binding is confirmed or strengthened.
5. Unsupported runtime-only states remain fail-closed.
6. Existing exit-rule scope remains unchanged.
7. No broad redesign or weakening of comparison semantics is introduced.
```

## Final report format

At the end, provide a concise but complete report with these sections:

```text
Summary
- What was confirmed
- What was patched
- Whether the repo now has a promotion-grade export/replay/equivalence success path

Verified Findings
- File-by-file findings
- Which hypotheses were validated or falsified
- Any stale assumptions from the review

Changes Made
- CLI dispatch tests
- Evidence validator tests, if added
- Candidate/profile binding validation, if changed
- Recovery action/report changes, if changed

Tests Added/Changed
- List each test and what it proves

Exit Rule Scope
- Confirm current implemented exit rules
- Confirm stop_loss/take_profit status
- State whether exit behavior changed

Commands Run
- Exact commands
- Pass/fail result
- Any skipped or not-run tests with reason

Remaining Risks / Follow-up
- Only repo-specific risks
- No generic trading disclaimers
- If shared decision kernel remains future work, state the exact research/runtime boundary still split
```

Be explicit. Do not claim the broader strategy-equivalence problem is solved unless the real repo-owned success path is tested and the remaining dispatch/evidence regressions are pinned.

