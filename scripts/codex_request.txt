k````text id="z7wq8n"
You are Codex working in the GitHub repository `WG-14/bithumb-bot`.

Task: perform the next focused hardening patch for live execution-quality analysis.

Do not dilute this into generic reporting. Do not change strategy thresholds or order policy as a substitute for measurement. The core problem remains:

EC2 DB and Bithumb trade-history consistency proves accounting stability. It does not prove profitability validity. Profitability depends on execution quality: whether live fills occur within the assumptions used by research/backtest/paper. The repo already has an execution-quality foundation, but it still needs sharper market-vs-limit cost comparison, stronger sent-but-error tests, and operator-facing documentation.

Current known context

The repo already appears to have:
- `execution_quality.py`
- `ExecutionQualityRecord`
- side-aware slippage calculations
- `execution_quality_events` upsert
- `execution-quality-report`
- manifest slippage comparison
- actual broker request/response timestamp evidence for successful/application live submit paths
- telemetry-first execution-quality config
- schema/idempotency tests
- historical fallback tests.

This task is not to rewrite those. Build on them.

Primary goals

1. Make market-vs-limit execution cost differences explicit in the execution-quality report.
2. Add tests for sent-but-error / timeout / submit-unknown paths so request timing evidence and recovery semantics are protected.
3. Add clear operator documentation for running and interpreting execution-quality reports.

Hypotheses to verify or refute

H1: Market-vs-limit cost comparison is currently under-exposed.
The data model likely has `order_type` and the report likely counts market/limit orders, but the report may not separately summarize slippage, latency, partial-fill rate, and unfilled rate by order type. Without this, operators cannot easily tell whether market orders are faster but more expensive, or limit orders are cheaper but less likely to fill.

Verify:
- Inspect `src/bithumb_bot/execution_quality.py`.
- Inspect `execution-quality-report` implementation in `src/bithumb_bot/app.py` or related CLI code.
- Inspect tests in `tests/test_execution_quality.py`.
- Determine whether output already includes grouped metrics for `market` vs `limit`.
- If not present or too weak, implement explicit order-type cost comparison.

H2: Sent-but-error / timeout / ambiguous response / submit-unknown paths are not sufficiently tested.
Successful live submit timing evidence appears implemented. The dangerous operational paths are failures after dispatch, especially timeout, transport error, ambiguous response, and submit unknown. These must preserve request timing evidence and not weaken existing recovery/unresolved-order semantics.

Verify:
- Inspect `src/bithumb_bot/broker/live_submit_orchestrator.py`.
- Inspect `src/bithumb_bot/broker/live_submission_execution.py`.
- Inspect broker exception classes and existing tests for submit failure paths.
- Determine whether tests prove:
  - request timestamp is recorded when dispatch was attempted,
  - response timestamp semantics are consistent,
  - timeout/temporary/ambiguous paths preserve existing `SUBMIT_UNKNOWN`, `FAILED`, or recovery behavior,
  - no fake fill/latency is produced without fills.

H3: Operator documentation for execution-quality workflows is insufficient or hidden.
The CLI may exist, but operators need explicit commands and interpretation rules. The docs should explain that `INSUFFICIENT_EVIDENCE` is not proof of healthy execution, and that accounting reconciliation is separate from execution-quality validation.

Verify:
- Inspect README, RUNBOOK, operations docs, or any docs that list live/recovery/reporting commands.
- Add a concise section if missing.

Scope

In scope:
- Add order-type grouped execution-quality metrics.
- Add text and JSON output fields for market/limit cost comparison.
- Add deterministic tests for grouped metrics.
- Add sent-but-error/timeout/submit-unknown evidence tests where feasible with existing architecture.
- Add operator documentation.
- Preserve historical fallback behavior.
- Preserve telemetry-first gate behavior.
- Preserve all existing safety/recovery/accounting semantics.

Out of scope:
- Rewriting `execution_quality.py` from scratch.
- Replacing the existing `execution_quality_events` table.
- Enabling hard live blocking by default.
- Changing live order policy from market to limit or vice versa.
- Tightening slippage thresholds.
- Changing strategy entry/exit logic.
- Adding heavyweight dependencies.
- Removing existing accounting/recovery gates.

Implementation requirements

A. Add explicit order-type grouped metrics

Enhance execution-quality summary logic to compute per-order-type metrics for at least:
- `market_order_count`
- `market_median_slippage_bps`
- `market_p90_slippage_bps`
- `market_p95_slippage_bps`
- `market_median_submit_to_fill_ms`
- `market_p90_submit_to_fill_ms`
- `market_p95_submit_to_fill_ms`
- `market_partial_fill_rate`
- `market_unfilled_rate`

and the same for limit:
- `limit_order_count`
- `limit_median_slippage_bps`
- `limit_p90_slippage_bps`
- `limit_p95_slippage_bps`
- `limit_median_submit_to_fill_ms`
- `limit_p90_submit_to_fill_ms`
- `limit_p95_submit_to_fill_ms`
- `limit_partial_fill_rate`
- `limit_unfilled_rate`

Use the existing fields:
- `order_type`
- `slippage_vs_signal_bps`
- `slippage_vs_submit_ref_bps`
- `full_fill_latency_ms`
- `partial_fill_flag`
- `unfilled_flag`

Prefer `slippage_vs_signal_bps` for the primary grouped cost metric unless existing report semantics clearly use another primary slippage field. Keep existing aggregate fields unchanged.

Handle:
- no market records,
- no limit records,
- missing slippage,
- missing latency,
- insufficient evidence,
- mixed case order types,
- unknown order types.

For unknown order types:
- do not crash,
- either exclude from market/limit grouped comparison or expose `unknown_order_type_count`.

B. Add an order-type cost delta classification

Add a concise field such as:

```text
order_type_cost_delta=market_fills_faster_but_costs_more
````

The classification should be deterministic and conservative. Suggested labels:

* `insufficient_order_type_samples`
* `market_fills_faster_but_costs_more`
* `market_fills_faster_and_costs_less`
* `limit_fills_faster_but_costs_more`
* `limit_fills_faster_and_costs_less`
* `market_costs_more`
* `limit_costs_more`
* `no_material_order_type_difference`
* `one_order_type_only`

Do not overfit this. A simple rule is acceptable:

* compare market vs limit p90 slippage when both are available,
* compare market vs limit p95 full-fill latency when both are available,
* use a small epsilon to avoid noisy differences,
* if one side lacks enough evidence, return `insufficient_order_type_samples` or `one_order_type_only`.

Make the report output directly useful, for example:

```text
market_order_count=42
market_p90_slippage_bps=18.5
market_p95_submit_to_fill_ms=2400
market_partial_fill_rate=0
market_unfilled_rate=0

limit_order_count=17
limit_p90_slippage_bps=4.2
limit_p95_submit_to_fill_ms=5100
limit_partial_fill_rate=0.23
limit_unfilled_rate=0.11

order_type_cost_delta=market_fills_faster_but_costs_more
```

C. Preserve report compatibility

Existing output keys must not be removed or renamed.

Text output:

* add grouped fields after existing aggregate fields or in a clear stable location.
* missing values should print as `NA`, matching existing style.

JSON output:

* include the new grouped fields and `order_type_cost_delta`.
* keep existing JSON keys unchanged.

D. Sent-but-error / timeout / submit-unknown tests

Add focused tests around the actual submit pipeline if feasible.

Test the real live submit orchestrator path with a fake broker and a valid request object. Prefer testing the smallest function that dispatches to broker and records submit evidence.

Required test cases if feasible:

1. Successful dispatch timing evidence

* fake broker returns an order.
* assert `request_ts`, `response_ts`, and `submit_elapsed_ms` are present in submit evidence.
* assert `response_ts >= request_ts`.
* assert execution-quality reconstruction uses evidence timestamps rather than fallback event timestamps.

2. Temporary transport error after dispatch

* fake broker raises `BrokerTemporaryError`.
* assert request timing is recorded if dispatch was attempted.
* assert existing failure/submission reason semantics are preserved.
* assert no fake fill metrics appear.

3. Timeout / submission unknown

* fake broker raises the repo’s timeout/submission-unknown exception or the closest existing equivalent.
* assert status transition and safety semantics remain unchanged.
* assert request timing evidence exists where a request was attempted.
* assert `SUBMIT_UNKNOWN` or existing equivalent remains intact.

4. Ambiguous response

* if there is a distinct ambiguous response path, test it.
* if not, document why it is covered by an existing exception path.

5. Planning failure before dispatch

* assert `request_ts=None`, `response_ts=None`, `submit_elapsed_ms=None`.
* assert no request timing is fabricated.
* assert existing failed-before-send semantics remain intact.

If the architecture makes one of these tests impractical, do not fake meaningless tests. Add the feasible tests and document the gap in the final report.

E. Recovery and safety constraints

Do not weaken:

* unresolved order gate,
* submit unknown handling,
* accounting pending handling,
* recovery required handling,
* fill fee validation,
* broker fill observation behavior,
* idempotent submit attempt/order event recording.

Any sent-but-error test must assert existing status/recovery behavior, not just timestamp presence.

F. Operator documentation

Add or update the most appropriate operator-facing doc. Prefer an existing RUNBOOK or operations guide. If there is no clear operations doc, use README.

Include this exact workflow, adjusted only if repo command syntax differs:

```bash
uv run bithumb-bot execution-quality-report --limit 100
uv run bithumb-bot execution-quality-report --format json
uv run bithumb-bot execution-quality-report --compare-manifest examples/research/sma_filter_manifest.example.json
```

Also include:

```bash
sqlite3 "$DB_PATH" ".schema execution_quality_events"
```

Document what the report answers:

* signal-time price vs submit-time price vs actual average fill price,
* submit-to-fill latency,
* realized slippage,
* partial/unfilled rates,
* market-vs-limit cost and fill behavior,
* live observed cost vs manifest/backtest assumptions.

Document interpretation:

* `PASS`: enough evidence and live execution is within configured/model assumptions.
* `WARN`: evidence exists but there are missing links, latency concerns, partial-fill concerns, or similar issues.
* `FAIL`: live observed cost/model breach exceeds threshold.
* `INSUFFICIENT_EVIDENCE`: not proof of safety; collect more live samples or repair missing links.

Document the distinction:

* accounting reconciliation proves local/exchange records agree,
* execution-quality analysis evaluates whether live fills are economically consistent with research/backtest/paper assumptions.

Testing requirements

Add or update tests for:

1. Grouped market/limit metrics

* one market order and one limit order.
* market has higher slippage but lower latency.
* limit has lower slippage but higher partial/unfilled rate.
* assert grouped fields and `order_type_cost_delta`.

2. Grouped metrics with only one order type

* assert no crash.
* assert missing side fields are `None`/`NA` as appropriate.
* assert `order_type_cost_delta=one_order_type_only` or equivalent.

3. Unknown order type

* assert no crash.
* assert unknown count if implemented.

4. JSON output

* assert grouped market/limit keys are included.
* assert existing keys still exist.

5. Sent-but-error / timeout / submit-unknown

* add feasible tests as described above.
* assert request timing evidence and existing recovery/status semantics.

6. Planning failure before dispatch

* assert request/response timestamps remain null.

7. Existing tests

* existing `tests/test_execution_quality.py` should still pass.
* relevant submit/recovery/accounting tests should still pass.

Suggested commands

Run focused tests first:

```bash
uv run pytest tests/test_execution_quality.py -q
```

Then run relevant submit/recovery tests. If unsure, run:

```bash
uv run pytest tests -q
```

If the repo has formatting/typecheck conventions, run them too.

Acceptance criteria

The patch is acceptable only if:

* `execution-quality-report` explicitly reports market-vs-limit cost and fill behavior.
* Text and JSON outputs expose grouped metrics.
* Existing aggregate report fields remain compatible.
* `order_type_cost_delta` or equivalent is deterministic and tested.
* sent-but-error/timeout/submit-unknown paths are tested where feasible.
* successful dispatch timing evidence remains intact.
* planning failure does not fabricate request timestamps.
* recovery/accounting/unresolved-order semantics are not weakened.
* operator documentation includes exact commands and interpretation.
* pytest passes.

Final response format

When done, report in this exact structure:

1. Summary

* One paragraph explaining what changed and why it improves live execution-quality analysis beyond accounting reconciliation.

2. Files changed

* List each changed file with one-line purpose.

3. Hypothesis results

* H1: supported/refuted/partially supported, with repo evidence.
* H2: supported/refuted/partially supported, with repo evidence.
* H3: supported/refuted/partially supported, with repo evidence.

4. Market vs limit metrics

* List the new output fields.
* Explain `order_type_cost_delta` semantics.

5. Sent-but-error coverage

* List tested paths.
* Explain any path not tested and why.
* State whether existing recovery/status semantics were preserved.

6. Operator workflow

* Exact commands to run.
* Example output shape.
* PASS/WARN/FAIL/INSUFFICIENT_EVIDENCE interpretation.

7. Tests

* Exact tests added or updated.
* Exact commands run.
* Pass/fail result.

8. Recovery and safety notes

* Explain why the patch does not change live order policy.
* Explain why hard blocking remains opt-in/telemetry-first.
* Explain historical fallback behavior remains safe.

9. Remaining risks / follow-up

* Only concrete follow-up items, such as hard gate rollout, paper-live-gap-report, or deeper readiness integration.

```
```

