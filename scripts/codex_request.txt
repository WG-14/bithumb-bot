You are working in the `WG-14/bithumb-bot` repository.

Your task is not to add another residual/dust workaround. The target-delta refactor is now close to done, but it still needs proof at the live path and run-loop integration level. The priority is to verify with repository tests that `target_delta` is truly the live execution authority when enabled.

Core operator goal:

```text id="tz1b09"
The bot may buy once and sell once.
The exchange may leave residual BTC due to fills, fees, rounding, min quantity, or min notional.
The bot may stop and restart.
After restart, it must read broker-verified current position, compare it to persisted target position, and converge by delta.
Executable leftover BTC is current position.
Only below-exchange-minimum delta is true dust.
Residual/dust/lot-native policy must not be the center of target_delta execution.
```

## Current hypothesis to verify

Treat this as a hypothesis, not a fact:

```text id="e3fbe1"
The latest patch likely improved:
  - execution_service.py target_submit_plan generation
  - target_delta residual_submit_plan=None behavior
  - LiveSignalExecutionService forwarding of target_submit_plan
  - broker/live.py awareness of target_position_delta
  - target_position.py target_delta decision model

But it is still not proven enough that:
  1. broker/live.py actually uses execution_submit_plan["qty"] as live order_qty in target_delta mode.
  2. target_delta SELL does not re-enter lot-native sell sizing as quantity authority.
  3. target_delta SELL is not blocked by sellable_executable_lot_count=0, no_executable_exit_lot, dust_only_remainder, harmless_dust, or RESIDUAL_LIVE_SELL_MODE=telemetry.
  4. run_loop actually loads persisted target state and passes previous_target_exposure_krw into execution summary.
  5. run_loop actually forwards target_submit_plan to live execution after restart.
```

Your job is to verify these points directly in the repo, add missing tests, and only patch code if a test exposes an actual gap.

Do not generalize this into a cleanup task. This is a verification-first closure task.

## Priority order

Work in this order:

```text id="u3dl9o"
Priority 1:
  Add/confirm broker/live.py target_delta plan consumption test.

Priority 2:
  Add/confirm run_loop target state integration test.

Priority 3:
  Add/confirm target_delta executable_delta bypasses old harmless dust / dust_only_remainder suppression.

Priority 4:
  Patch only if the tests fail or reveal an unproven path.
```

## Files to inspect

Inspect at least:

```text id="e1w4d2"
src/bithumb_bot/broker/live.py
src/bithumb_bot/execution_service.py
src/bithumb_bot/engine.py
src/bithumb_bot/target_position.py
src/bithumb_bot/db_core.py
src/bithumb_bot/dust.py
src/bithumb_bot/decision_context.py
src/bithumb_bot/runtime_readiness.py
tests/test_target_position_execution.py
tests/test_failsafe.py
```

Search for:

```text id="mhnrml"
target_delta
target_position_delta
target_delta_rebalance
target_submit_plan
execution_submit_plan
sellable_executable_lot_count
sellable_executable_qty
no_executable_exit_lot
dust_only_remainder
harmless_dust
RESIDUAL_LIVE_SELL_MODE
_load_previous_target_exposure_for_run_loop
_persist_target_position_state_for_run_loop
load_target_position_state
upsert_target_position_state
previous_target_exposure_krw
```

## Required verification before changing code

### 1. Verify execution_service.py target plan

Confirm whether `build_execution_decision_summary()` still creates:

```text id="q0zvd2"
target_submit_plan.source == "target_delta"
target_submit_plan.authority == "target_position_delta"
target_submit_plan.intent_type == "target_delta_rebalance"
target_submit_plan.strategy_context == "target_delta"
target_submit_plan.side in {"BUY", "SELL", "NONE"}
target_submit_plan.qty == target_decision.submit_qty
target_submit_plan.submit_expected == target_decision.would_submit
target_submit_plan.block_reason == target_decision.block_reason
```

Confirm that in `EXECUTION_ENGINE=target_delta`:

```text id="dafpt6"
residual_submit_plan is None
buy_submit_plan is None
target_submit_plan is present
```

If already correct, do not rewrite it.

### 2. Verify LiveSignalExecutionService forwarding

Confirm whether `LiveSignalExecutionService.execute()`:

```text id="p5k46f"
reads decision_context["execution_decision"]["target_submit_plan"]
requires source == "target_delta"
requires authority == "target_position_delta"
requires submit_expected == true
requires block_reason == "none"
requires side in {"BUY", "SELL"}
requires qty > 0
passes execution_submit_plan=target_submit_plan to live_execute_signal(...)
does not fall back to residual path for target_delta
```

If already correct, preserve it and add tests only if not covered.

### 3. Verify broker/live.py quantity authority

This is the most important point.

Confirm whether `broker/live.py` has a first-class path for:

```text id="wev3uu"
execution_submit_plan.source == "target_delta"
execution_submit_plan.authority == "target_position_delta"
```

That path must use:

```text id="taj2lx"
side = execution_submit_plan["side"]
order_qty = execution_submit_plan["qty"]
submit_qty_source = "target_position_delta"
intent_type = "target_delta_rebalance"
strategy_context = "target_delta"
idempotency_key = execution_submit_plan["idempotency_key"] if present
```

That path must not use these as target_delta SELL quantity authority:

```text id="lak4jd"
sellable_executable_lot_count
sellable_executable_qty
position_state.normalized_exposure.sellable_executable_lot_count
position_state.normalized_exposure.sellable_executable_qty
residual_sell_candidate
RESIDUAL_LIVE_SELL_MODE
```

Generic safety gates must remain active:

```text id="hzgjpt"
kill switch / order submission halt
LIVE_DRY_RUN / LIVE_REAL_ORDER_ARMED
unresolved order gate
open-order/dedup protections
min_qty / min_notional / qty_step / max decimals
standard submit pipeline
fill application / accounting
idempotency / dedup
```

If the code already does this, add or confirm tests proving it.
If it does not, patch `broker/live.py`.

### 4. Verify run_loop target state integration

Confirm whether `engine.py` run loop actually does this:

```text id="f099gv"
load target_position_state for settings.PAIR
pass previous_target_exposure_krw into build_execution_decision_summary
include execution_decision.target_submit_plan in decision_context
pass that decision_context to LiveSignalExecutionService / live execution
upsert target_position_state on BUY and SELL from target_new_exposure_krw
preserve target state on HOLD
fail closed on HOLD without persisted target
```

Helper tests are not enough. The integration path must be tested.

### 5. Verify dust suppression by engine mode

Confirm that old lot-native suppression behavior remains in `EXECUTION_ENGINE=lot_native`, but does not block executable target delta in `EXECUTION_ENGINE=target_delta`.

Target-delta behavior must be:

```text id="vrw7ix"
target_dust_classification == "executable_delta":
  do not record harmless dust suppression
  do not record no_executable_exit_lot suppression
  do not block because of dust_only_remainder
  proceed to target_delta submit if generic safety gates pass

target_dust_classification == "true_dust":
  no live submit
  safe no-op
```

## Required tests

Add tests if they do not already exist. Do not rely on summary-only tests.

### Test 1: broker/live.py consumes target_delta plan qty

Create a test against the smallest stable live execution entry point.

Setup:

```text id="yv400l"
EXECUTION_ENGINE=target_delta

execution_submit_plan = {
  "source": "target_delta",
  "authority": "target_position_delta",
  "intent_type": "target_delta_rebalance",
  "strategy_context": "target_delta",
  "side": "SELL",
  "qty": 0.0004998,
  "notional_krw": valid,
  "submit_expected": true,
  "block_reason": "none",
  "idempotency_key": "..."
}

Context simulates:
  sellable_executable_lot_count = 0
  exit_allowed = false or no strategy lot
  RESIDUAL_LIVE_SELL_MODE = telemetry
  optional harmless_dust / dust_only_remainder old context
```

Expected:

```text id="de4xd1"
live path uses execution_submit_plan["qty"] as order_qty
submit_qty_source == "target_position_delta"
intent_type == "target_delta_rebalance"
strategy_context == "target_delta"
does not require sellable_executable_lot_count
does not derive qty from sellable_executable_qty
does not route through residual policy
does not block on RESIDUAL_LIVE_SELL_MODE=telemetry
reaches standard submit pipeline or mocked broker submit with SELL qty=0.0004998
```

### Test 2: target_delta SELL does not call lot-native sell sizing as quantity authority

Mock or instrument `build_sell_execution_sizing` if possible.

Expected:

```text id="akbqsh"
target_delta SELL order_qty is execution_submit_plan["qty"]
build_sell_execution_sizing is not used to replace that qty
no_executable_exit_lot does not block executable target delta
```

If the existing live path still calls generic validation helpers, that is acceptable only if they do not replace the target_delta qty or require lot-native sell authority.

### Test 3: invalid target plans are rejected safely

Cases:

```text id="kahbqk"
missing target_submit_plan
source != target_delta
authority != target_position_delta
submit_expected=false
block_reason != "none"
side=NONE
qty <= 0
```

Expected:

```text id="ch5ix9"
executor not called
no residual fallback
no live submit
clear no-op or block reason
```

### Test 4: target_delta executable_delta bypasses harmless dust suppression

Setup:

```text id="iua4v2"
EXECUTION_ENGINE=target_delta
target_submit_plan.target_dust_classification = "executable_delta"
old context indicates harmless_dust or dust_only_remainder
sellable_executable_lot_count = 0
```

Expected:

```text id="w33a33"
target_delta submit proceeds
harmless dust suppression is not recorded
no no_executable_exit_lot suppression
```

### Test 5: true_dust no-op remains safe

Setup:

```text id="y6hy6x"
EXECUTION_ENGINE=target_delta
target_submit_plan.submit_expected = false
target_submit_plan.block_reason = "delta_below_exchange_min"
target_dust_classification = "true_dust"
```

Expected:

```text id="qgfuol"
no live submit
no error
observability clearly shows true_dust / delta_below_exchange_min
```

### Test 6: run_loop persisted target integration

Add integration-style tests around `engine.py`, not just helper functions.

Cases:

```text id="c3f9cv"
A. persisted target=0, broker_qty=0.0004998, signal=SELL or HOLD:
   expected:
     previous_target_exposure_krw=0 is used
     target_submit_plan side=SELL qty=0.0004998
     live execution receives execution_submit_plan

B. persisted target=0, broker_qty below exchange minimum, signal=HOLD or SELL:
   expected:
     true_dust no-op
     no state-machine corruption
     no live submit

C. persisted target=100000, broker has partial BTC exposure, signal=HOLD:
   expected:
     BUY only missing delta if executable
     not a full duplicate BUY

D. no persisted target, signal=HOLD:
   expected:
     missing_persistent_target_state
     no submit

E. unsafe readiness, open order or broker/local mismatch:
   expected:
     no submit
     explicit target_block_reason
```

### Test 7: lot_native regression

Preserve old behavior:

```text id="r2l0gz"
EXECUTION_ENGINE=lot_native
lot-native SELL still uses sellable_executable_lot_count
residual policy still behaves as before
harmless dust suppression still applies in lot_native
```

## Patch only if tests expose a gap

If the tests already exist and pass, report that no code patch is needed.

If tests are missing, add them.

If tests fail because target_delta live path still re-enters lot-native authority, patch the smallest relevant part of `broker/live.py`.

If tests fail because run_loop does not propagate persisted target state into execution summary/live execution, patch `engine.py`.

If tests fail because target_delta executable_delta is suppressed by old dust logic, patch suppression branching by `EXECUTION_ENGINE`.

## Observability requirements

Ensure target_delta logs or decision context expose:

```text id="ur8vzk"
execution_engine=target_delta
execution_submit_plan_source=target_delta
execution_submit_plan_authority=target_position_delta
target_delta_side
target_delta_qty
target_delta_notional_krw
target_delta_intent_type
target_delta_strategy_context
target_delta_idempotency_key
submit_qty_source=target_position_delta
target_dust_classification
target_position_truth_state
target_block_reason
```

The following should be easy to identify in logs or context:

Executable leftover:

```text id="or752j"
target_new_exposure_krw=0
target_current_qty=0.0004998
target_delta_side=SELL
target_submit_qty=0.0004998
target_dust_classification=executable_delta
block_reason=none
submit_expected=1
```

True dust:

```text id="cn2q78"
target_current_qty=0.00000004
target_delta_side=NONE
target_dust_classification=true_dust
target_block_reason=delta_below_exchange_min
submit_expected=0
```

Unsafe state:

```text id="aqdyv6"
target_position_truth_state=blocked
target_block_reason=open_order_count_nonzero
submit_expected=0
```

## Acceptance criteria

The task is complete only if all are true:

```text id="txqjc3"
1. target_delta summary creation is still correct.
2. LiveSignalExecutionService forwards valid target_submit_plan.
3. broker/live.py uses target_delta execution_submit_plan["qty"] as live order_qty.
4. target_delta SELL does not require sellable_executable_lot_count.
5. target_delta SELL does not derive qty from sellable_executable_qty.
6. target_delta SELL does not depend on RESIDUAL_LIVE_SELL_MODE.
7. target_delta executable_delta is not blocked by harmless_dust / dust_only_remainder / no_executable_exit_lot.
8. target_delta true_dust no-ops safely.
9. run_loop reads persisted target state and passes previous_target_exposure_krw into execution summary.
10. run_loop forwards target_submit_plan to live execution.
11. HOLD uses persisted target after restart.
12. HOLD without persisted target fails closed.
13. unsafe readiness blocks target_delta submit with explicit reason.
14. lot_native behavior remains backward compatible.
15. Tests prove actual live path behavior, not just summary creation.
```

## Non-goals

Do not rewrite strategy logic.

Do not delete `lot_native`.

Do not delete recovery gates.

Do not trust broker balance blindly.

Do not make live orders less safe.

Do not add new residual/dust policy switches.

Do not treat executable leftover BTC as dust in `target_delta`.

Do not require perfect zero BTC after SELL. The model is target convergence with true dust tolerated.

## Suggested commands

Run focused tests first:

```bash id="coez4d"
pytest tests/test_target_position_execution.py -q
pytest tests/test_failsafe.py -q
```

Run any new live path / engine integration tests.

Then, if feasible:

```bash id="llly5a"
pytest -q
```

If full suite cannot run because of environment constraints, report the exact command and failure.

## Final report format

Report exactly in this format:

```markdown id="lprbtc"
# Target-Delta Verification Closure Report

## Summary
- What was already correct:
- What tests were missing:
- What code changed, if any:

## Verification
### execution_service.py
- Findings:

### LiveSignalExecutionService
- Findings:

### broker/live.py
- Findings:
- Whether plan.qty is live order authority:

### engine.py run_loop
- Findings:
- Whether persisted target state reaches live execution:

### dust / suppression behavior
- Findings:

## Key Behavior Proven
### Executable leftover BTC with target=0
- Input:
- Expected:
- Actual:

### True dust leftover with target=0
- Input:
- Expected:
- Actual:

### HOLD with persisted target after restart
- Input:
- Expected:
- Actual:

### HOLD without persisted target
- Input:
- Expected:
- Actual:

### Unsafe readiness
- Input:
- Expected:
- Actual:

## Tests
- New/updated tests:
- Commands run:
- Results:

## Safety and Compatibility
- Safety gates preserved:
- lot_native compatibility:
- target_delta independence from residual policy:
- target_delta independence from lot-native sell quantity authority:

## Observability
- Verified/new fields:
- Example executable_delta context:
- Example true_dust context:
- Example unsafe_state context:

## Remaining Risks / Follow-ups
- Known limitations:
- Recommended cleanup:
```

Remember: this is not a new feature pass. It is a closure pass. The key question is whether `target_delta` is truly the live submit authority when enabled, and whether tests prove it against the original failure mode: executable leftover BTC with no strategy lot, residual mode telemetry, and old dust context present.

