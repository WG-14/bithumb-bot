You are working in GitHub repository `WG-14/bithumb-bot`.

The latest `main` appears to have the regime-aware research pipeline and live `sma_with_filter` regime policy gate implemented. Pytest reportedly passes. Do not redo the already implemented regime research/backtest/report/promotion work. Your job is to directly inspect the repo, verify the remaining operational ambiguity, and apply only the minimal structural patch needed to close it.

Core issue:

> The regime-aware pipeline now appears to work for `sma_with_filter`, but plain `create_sma_strategy()` / `sma_cross` may still be able to produce live BUY decisions without promoted candidate regime policy. If plain SMA is live-operable, this bypasses the regime-policy safety model. If plain SMA is intentionally research/test/demo-only, that must be explicit, tested, and visible in operator docs/config.

Treat this as a hypothesis. Verify it in the repo before changing code.

## Current state to verify

Recent inspection suggests the repo already has:

```text
promotion artifact includes live_regime_policy
candidate profile requires regime policy before promotion
strategy_config.py loads candidate_regime_policy from STRATEGY_CANDIDATE_PROFILE_PATH
market_regime.policy has load_candidate_regime_policy_from_path / normalize_live_regime_policy / evaluate_live_regime_policy
sma_with_filter tests cover missing policy, invalid policy, old artifact, version mismatch, blocked regime, not-allowed regime, allowed regime, and SELL not being blocked
```

Do not assume this is complete. Confirm directly.

Inspect at least:

```text
src/bithumb_bot/strategy/sma.py
src/bithumb_bot/strategy_config.py
src/bithumb_bot/config.py
src/bithumb_bot/app.py
src/bithumb_bot/strategy_replay.py
src/bithumb_bot/market_regime/policy.py
src/bithumb_bot/research/promotion_gate.py
tests/test_sma_with_filter_strategy.py
tests/test_strategy_config.py or equivalent config tests
.env.example
docs/
```

Search for:

```text
create_sma_strategy
create_sma_with_filter_strategy
sma_cross
sma_with_filter
STRATEGY_NAME
STRATEGY_CANDIDATE_PROFILE_PATH
candidate_regime_policy
regime_policy_missing
regime_decision
current_market_regime_snapshot
```

## Primary hypothesis

### Hypothesis A: plain `sma_cross` can still be selected for live operation and bypass candidate regime policy

The live app may construct strategies through settings such as `STRATEGY_NAME`, `SMA_*`, or factory functions. If plain `create_sma_strategy()` is selectable in live mode and does not require `candidate_regime_policy`, then regime-aware promotion is not a complete live safety boundary.

Verify:

```text
1. Which strategy factory does the live app use by default?
2. Can config select plain sma_cross in live mode?
3. Does plain sma_cross call evaluate_live_regime_policy?
4. Does plain sma_cross fail closed when STRATEGY_CANDIDATE_PROFILE_PATH is missing?
5. Are docs/env examples explicit that live regime gating requires sma_with_filter?
6. Are tests proving that plain SMA cannot bypass live regime policy, or that it is intentionally non-live?
```

Possible outcomes:

```text
A1. plain sma_cross is not live-selectable:
    Add or update tests/docs proving that.

A2. plain sma_cross is live-selectable but intentionally unsafe/dev-only:
    Make that impossible in live mode, or require an explicit dangerous override with loud docs/tests.

A3. plain sma_cross is live-selectable and should be allowed:
    Wire candidate regime policy into plain sma_cross too, preserving SELL behavior.

A4. live always uses sma_with_filter:
    Add tests and docs proving this invariant, especially around settings parsing and factory selection.
```

Do not leave this ambiguous.

## Required design principle

Regime-aware promotion must be a live safety boundary, not just an optional strategy feature.

If a strategy can submit or intend a live BUY, it must either:

```text
1. enforce promoted candidate regime policy fail-closed, or
2. be structurally impossible to use in live trading, with tests and docs proving that.
```

Do not silently allow old behavior because tests are green.

## Implementation requirements if Hypothesis A is supported

Choose the smallest correct fix based on what the repo reveals.

### Option 1: Make plain `sma_cross` non-live

If plain SMA is intended only for tests, replay, or development, enforce that.

Add a guard in the live strategy construction path such as:

```text
if MODE == "live" and STRATEGY_NAME == "sma_cross":
    fail startup or force HOLD with reason plain_sma_live_not_allowed
```

Prefer failing at startup/config validation over silently changing strategy behavior.

Add tests for:

```text
live mode refuses plain sma_cross
paper/dry-run behavior is explicit
docs explain sma_with_filter is required for promoted regime-gated live operation
```

### Option 2: Wire regime policy into plain SMA too

If plain SMA is still a valid live strategy, it must enforce the same candidate regime policy on new BUY entries.

Implement:

```text
SmaCrossStrategy has candidate_regime_policy field
SmaCrossStrategy.from_config passes config.candidate_regime_policy
create_sma_strategy accepts candidate_regime_policy or loads from config path consistently
BUY decision evaluates current_market_regime_snapshot + candidate policy
missing/invalid policy fails closed
current regime not allowed fails closed
current regime blocked fails closed
version mismatch fails closed
SELL/exit is not blocked
decision context includes the same observability fields as sma_with_filter
```

Do not duplicate policy logic unnecessarily. Reuse `evaluate_live_regime_policy`.

### Option 3: Prove live factory always selects `sma_with_filter`

If app config already guarantees `sma_with_filter`, add tests and docs so this cannot regress.

Tests should cover:

```text
live app strategy factory defaults to sma_with_filter
STRATEGY_NAME=sma_cross is rejected in live mode, or impossible
STRATEGY_CANDIDATE_PROFILE_PATH is required for live sma_with_filter
missing candidate profile leads to fail-closed BUY behavior
```

## Recovery and compatibility behavior

Old candidate artifacts without regime policy must remain rejected for promotion and fail closed for live/replay entry use.

Plain SMA compatibility must be explicit:

```text
research/test use: allowed if intentional
live use: either impossible or regime-gated
```

No missing-policy path should mean “allow BUY.”

## Observability requirements

If any new guard or policy wiring is added, decision context or startup/config error must be specific.

Use reasons like:

```text
plain_sma_live_not_allowed
regime_policy_missing
regime_policy_unreadable
regime_policy_invalid_json
regime_policy_missing_allowed_regimes
regime_policy_version_mismatch
current_regime_not_in_candidate_allowed_regimes
current_regime_in_candidate_blocked_regimes
```

For any BUY blocked by regime policy, context should include:

```text
current_market_regime_snapshot
current_regime
current_regime_classifier_version
candidate_regime_classifier_version
candidate_allowed_regimes
candidate_blocked_regimes
regime_decision
regime_block_reason
regime_policy_source
regime_policy_present
regime_policy_valid
```

If the fix is startup rejection instead of decision-time blocking, the error message must name the selected strategy and explain the safe alternative.

## Documentation/config requirements

Check and update `.env.example` and docs if needed.

At minimum, document:

```text
STRATEGY_CANDIDATE_PROFILE_PATH
required role of promotion artifact / candidate profile
missing/invalid policy fails closed
which strategy is safe for live regime-gated operation
whether plain sma_cross is non-live, test-only, or also regime-gated
```

Use repo style. Do not add vague trading advice.

## Testing requirements

Run or add tests depending on the chosen fix.

At minimum, add tests for the primary ambiguity:

```text
1. live config cannot select plain sma_cross without regime policy, OR plain sma_cross enforces regime policy.
2. missing STRATEGY_CANDIDATE_PROFILE_PATH cannot silently allow a live BUY through any live-selectable SMA path.
3. allowed candidate policy permits BUY only when all other gates pass.
4. blocked/not-allowed/version-mismatch policy blocks BUY.
5. SELL/exit is not blocked by entry regime policy.
6. operator context or startup error includes the exact reason.
7. .env.example/docs mention STRATEGY_CANDIDATE_PROFILE_PATH and live strategy safety policy.
```

Also rerun the existing regime tests:

```bash
pytest tests/test_sma_with_filter_strategy.py tests/test_market_regime.py tests/test_research_promotion_gate.py
```

Then run:

```bash
pytest
```

If full pytest fails for unrelated pre-existing reasons, report the narrow tests and the full-suite failure separately.

## Secondary checks

Do these only after the primary ambiguity is closed.

### Secondary B: `.env.example` completeness

Verify `STRATEGY_CANDIDATE_PROFILE_PATH` exists and explains fail-closed behavior.

### Secondary C: policy loader observability

Verify unreadable/invalid policy path errors appear in either decision context or startup logs.

### Secondary D: research/live rule alignment

Confirm docs or tests state whether research backtest uses regime as attribution/gate evidence while live uses it as entry policy. Do not change this unless there is a direct inconsistency.

## Final report format

When finished, report in this exact structure:

```text
1. Summary
2. What I verified before changing code
3. Hypotheses
   - Hypothesis A: supported / refuted / partially supported / still unknown
   - Secondary B/C/D: supported / refuted / partially supported / still unknown, if inspected
4. Files changed
5. Design decision
   - Plain SMA non-live, plain SMA regime-gated, or live factory always sma_with_filter
6. Live/replay regime safety behavior
7. Recovery and compatibility behavior
8. Observability behavior
9. Documentation/config updates
10. Tests added or changed
11. Tests run and results
12. Remaining risks or follow-up work
```

Be precise. Do not claim the strategy is profitable or “safe” financially. The goal is to ensure every live-operable SMA BUY path is structurally regime-policy-aware, fail-closed, reproducible, and auditable.
