```text
You are working in the GitHub repository `WG-14/bithumb-bot`.

Task: evolve the repository from a collection of SMA filters and safety gates into a more reproducible, observable, and operator-readable automated trading experiment system.

Do not treat this as “add two more filters.” The core problem to verify and address is that strategy judgment, market suitability, cost/risk, execution feasibility, and operator interpretation are not yet unified into a clear decision contract. The goal is not to make BUY appear more often. The goal is to make every BUY candidate, HOLD result, blocked order, and live-dry-run decision explainable and reproducible from one decision row plus summary reporting.

Important context to verify in the repo, not assume blindly:
- The repo already appears to have `sma_with_filter`.
- Existing SMA/filter logic appears to include gap, volatility, overextension, cost-edge, fee/slippage, live dry-run posture, decision telemetry, target shadow decision, and performance gate.
- Existing env/config likely exposes fields such as:
  - `STRATEGY_NAME=sma_with_filter`
  - `SMA_FILTER_GAP_MIN_RATIO`
  - `SMA_FILTER_VOL_WINDOW`
  - `SMA_FILTER_VOL_MIN_RANGE_RATIO`
  - `SMA_FILTER_OVEREXT_*`
  - `SMA_COST_EDGE_ENABLED`
  - `ENTRY_EDGE_BUFFER_RATIO`
  - `LIVE_FEE_RATE_ESTIMATE`
  - `PAPER_FEE_RATE`
  - `STRATEGY_ENTRY_SLIPPAGE_BPS`
- Existing strategy decision context likely already contains items such as `base_signal`, `final_signal`, `blocked_filters`, `entry`, `position_gate`, `position_state`, `fee_authority`, and cost-edge ratios.
- Existing execution/shadow decision logic likely already contains items such as `target_shadow_decision`, `target_would_submit`, `target_delta_notional_krw`, `target_block_reason`, and `target_submit_plan`.

First verify the actual repository structure, current code paths, and tests. Do not overwrite existing behavior blindly.

Primary design objective:
Create a layered `decision_v2` style contract that allows an operator to answer these questions from live dry run output:

1. Is the current market regime suitable for this SMA strategy?
2. Did the strategy produce a BUY candidate?
3. Was the candidate blocked?
4. If blocked, was it blocked by market regime, strategy filters, cost edge, position state, order rules, execution sizing, fee authority, or performance gate?
5. If it would become a real order, does the order-sized expected edge beat fee/slippage/buffer in KRW terms?
6. Can the decision be reproduced from stored context?

Keep these hypotheses as hypotheses and implement verification support for them.

Hypothesis A:
BUY absence may not mean the strategy is broken. It may mean block causes are not being aggregated in an operator-readable way.

Verification:
Inspect recent or testable `strategy_decisions`-style records and determine whether `base_signal=BUY` exists but `final_signal=HOLD`, or whether `base_signal=BUY` itself is rare.

Expected interpretation:
- `base_signal=BUY` exists but `final_signal=HOLD` often: filters, position gate, execution gate, or policy block is the likely cause.
- `base_signal=BUY` rarely exists: SMA parameters or current market regime may be the cause.
- `base_signal=BUY`, `final_signal=BUY`, but `target_would_submit=false`: execution sizing or order-rule layer is the cause.

Hypothesis B:
Cost-edge may exist as a ratio-level check, but small live orders may still be economically meaningless in KRW terms.

Verification:
For BUY candidates, combine:
- expected edge ratio
- required edge ratio
- roundtrip fee ratio
- slippage ratio
- buffer ratio
- actual planned order KRW / target delta notional KRW

Then calculate:
- expected_edge_krw
- expected_cost_krw
- net_edge_krw
- meaningful_edge

Expected interpretation:
- `expected_edge_ratio > required_edge_ratio` but `net_edge_krw < MIN_NET_EDGE_KRW` means the trade may pass ratio checks but still be operationally meaningless.
- If `net_edge_krw` is sufficient but BUY is blocked, the cause is probably market regime, position state, order rules, or performance gate, not cost.

Hypothesis C:
Existing gap/volatility/overextension filters are local conditions, not a stable operator-readable market regime classifier.

Verification:
Create a separate market regime snapshot and compare it with existing blocked filters:
- chop + BUY candidate + cost-edge pass + later poor outcome suggests a regime filter may be useful.
- trend_up + cost-edge pass + only volatility blocked suggests existing volatility threshold may be too conservative.

Do not conclude this automatically. Add observability and tests so this hypothesis can be supported or refuted.

Hypothesis D:
Live dry run may be safe but not yet sufficiently reproducible as an experiment.

Verification:
From a single decision context, check whether the following can be reconstructed:
- candle timestamp / input window
- SMA short/long
- gap threshold
- volatility window and thresholds
- overextension lookback and threshold
- fee authority source
- slippage setting
- order sizing result
- performance gate result
- final primary block reason

If not, add missing fields to the decision context or replay fingerprint.

Implementation priorities:

Priority 1: Decision Contract v2

Introduce a stable, backward-compatible decision contract layer. Do not remove existing context fields unless tests prove they are unused and safe to preserve elsewhere.

Add or normalize a top-level structure like:

{
  "decision_contract_version": "decision_v2",
  "signal_flow": {
    "base_signal": "BUY",
    "strategy_signal": "BUY",
    "final_signal": "HOLD",
    "final_action": "HOLD",
    "primary_block_layer": "market_regime",
    "primary_block_reason": "chop_market",
    "all_block_reasons": [
      "market_regime.chop_market",
      "strategy_filters.cost_edge"
    ]
  },
  "market_regime": {},
  "strategy_features": {},
  "strategy_filters": {},
  "pre_trade_economics": {},
  "position_gate": {},
  "execution_plan": {},
  "performance_gate": {},
  "replay_fingerprint": {}
}

The exact field names may be adapted to fit existing repository conventions, but the meaning must remain intact.

Primary block layer priority should be explicit and tested. Suggested priority:

1. `fee_authority`
2. `market_regime`
3. `position_gate`
4. `strategy_filters`
5. `pre_trade_economics`
6. `execution_order_rule`
7. `performance_gate`

If the existing architecture requires a different order, document why.

Priority 2: MarketRegimeClassifier module

Do not hardcode market regime as another ad hoc filter inside `sma.py`.

Add a separate module, for example:

`src/bithumb_bot/strategy/market_regime.py`

Implement a small, deterministic snapshot model, for example:

@dataclass(frozen=True)
class MarketRegimeSnapshot:
    version: str
    regime: str
    regime_score: float
    trend_strength: float
    chop_score: float
    volatility_ratio: float
    volatility_state: str
    overextension_ratio: float
    allows_entry: bool
    block_reason: str
    inputs: dict[str, object]

Initial regime policy can be simple and should use existing SMA/filter features where possible:

- trend_strength = abs(short_sma - long_sma) / long_sma
- trend_direction = sign(short_sma - long_sma)
- volatility_ratio = recent high-low or close range / last close, matching or reusing existing volatility filter semantics where possible
- overextension_ratio = recent return or existing overextension measure
- chop_score = inverse/normalized trend strength

Suggested regime labels:
- `trend_up`
- `trend_down`
- `chop`
- `low_vol`
- `high_vol`
- `overextended_up`
- `unknown`

Suggested initial entry policy:
- allow BUY only when `regime == "trend_up"` and volatility is acceptable and overextension is not excessive.
- `chop`, `low_vol`, and `overextended_up` should block entry.
- `high_vol` should initially block or warn, not immediately resize orders, unless existing architecture already supports safe deterministic scaling.

Do not introduce high-volatility order-size reduction as the first step unless it can be cleanly tested and explained. First make regime-based allow/block observable and reproducible.

Add `market_regime` to strategy decision context.

Priority 3: PreTradeEconomics after execution planning

Do not calculate KRW-level expected edge only inside the strategy layer. Strategy-level cost-edge ratio can remain where it is.

Order-sized economics must be calculated after the planned order size or target delta notional is known, likely near the existing `target_submit_plan` / shadow execution decision path.

Add a module, for example:

`src/bithumb_bot/execution/pre_trade_economics.py`

Implement a deterministic snapshot model, for example:

@dataclass(frozen=True)
class PreTradeEconomicsSnapshot:
    version: str
    side: str
    order_krw: float
    expected_edge_ratio: float
    required_edge_ratio: float
    roundtrip_fee_ratio: float
    slippage_ratio: float
    buffer_ratio: float
    margin_after_cost_ratio: float
    expected_edge_krw: float
    expected_cost_krw: float
    net_edge_krw: float
    meaningful_edge: bool
    reason: str
    inputs: dict[str, object]

Formula guidance:
- `expected_edge_krw = order_krw * expected_edge_ratio`
- `expected_cost_krw = order_krw * (roundtrip_fee_ratio + slippage_ratio + buffer_ratio)` unless existing code already has a better cost-floor model
- `net_edge_krw = expected_edge_krw - expected_cost_krw`
- `margin_after_cost_ratio = expected_edge_ratio - required_edge_ratio`
- `meaningful_edge = net_edge_krw >= MIN_NET_EDGE_KRW and margin_after_cost_ratio >= MIN_MARGIN_AFTER_COST_RATIO`

Add config/env defaults if missing:
- `MIN_NET_EDGE_KRW`
- `MIN_MARGIN_AFTER_COST_RATIO`

Use conservative defaults. Avoid breaking current dry-run behavior. If the new meaningful-edge gate is too invasive for the first patch, make it observable first and only blocking when explicitly enabled by config. However, still compute and report it.

Priority 4: Live dry-run operator summary

Find the current live dry-run report or CLI output path. Add an operator-readable summary that aggregates recent decision rows by block cause and market regime.

Target output shape:

[DRY-RUN DECISION SUMMARY]
window: last 300 decisions
base BUY candidates: 14
final BUY: 0

block layers:
- market_regime.chop: 8
- strategy_filters.cost_edge: 4
- position_gate.already_in_position: 1
- execution_order_rule.min_notional: 1

regime distribution:
- trend_up: 18%
- chop: 61%
- low_vol: 13%
- overextended_up: 8%

BUY candidate economics:
- avg_order_krw: 10000
- avg_expected_edge_krw: 12
- avg_expected_cost_krw: 15
- avg_net_edge_krw: -3
- meaningful_edge_count: 0

If the repository already has a report format, preserve it and add these sections without removing existing diagnostics.

Testing requirements:

Add focused tests. Do not only test that fields exist. Test cause separation and reproducibility.

Required tests:

1. Market regime allows trend entry:
   Given a deterministic trend-up candle sequence, cost-edge pass, and no open position:
   - `market_regime.regime == "trend_up"`
   - `market_regime.allows_entry is True`
   - `signal_flow.primary_block_layer` is not `market_regime`

2. Chop blocks a BUY candidate:
   Given a sequence where SMA creates a golden cross but trend strength is below threshold:
   - `base_signal == "BUY"`
   - `final_signal == "HOLD"`
   - `market_regime.regime == "chop"`
   - `signal_flow.primary_block_layer == "market_regime"`
   - `signal_flow.primary_block_reason == "chop_market"` or equivalent

3. Cost-edge remains distinguishable from market regime:
   Given trend-up regime but insufficient expected edge:
   - `market_regime.allows_entry is True`
   - cost-edge filter blocks
   - `signal_flow.primary_block_layer == "strategy_filters"`
   - `signal_flow.primary_block_reason` identifies cost-edge

4. KRW economics can mark a ratio-passing candidate as operationally meaningless:
   Given ratio-level cost-edge pass but very small order KRW:
   - `pre_trade_economics.net_edge_krw` is below threshold
   - `pre_trade_economics.meaningful_edge is False`
   - if blocking is enabled, `primary_block_layer == "pre_trade_economics"`
   - if blocking is not enabled by default, report clearly that it is observation-only

5. Execution order-rule block remains separate:
   Given final strategy BUY but execution plan cannot submit due to min notional/order rule:
   - `final_signal == "BUY"` or strategy-level action remains BUY as appropriate
   - `target_would_submit is False`
   - `signal_flow.primary_block_layer == "execution_order_rule"` or equivalent

6. Replay fingerprint coverage:
   Verify `replay_fingerprint` or equivalent context includes enough information to reconstruct:
   - strategy name
   - strategy version or decision contract version
   - pair
   - interval
   - candle timestamp / through timestamp
   - SMA short/long
   - regime feature version
   - key thresholds
   - fee/slippage source
   - order sizing result if available

7. Backward compatibility:
   Existing tests for `blocked_filters`, `filters`, `entry`, `position_gate`, `position_state`, `fee_authority`, `execution_decision`, and dry-run safety must still pass or be intentionally updated with a clear reason.

Recovery and safety requirements:

- Do not weaken live-order safety.
- Do not make live real orders easier to arm.
- Do not remove `LIVE_DRY_RUN` / `LIVE_REAL_ORDER_ARMED` protections.
- New gates should default to conservative or observation-only behavior unless the repository already has a clear pattern for enabling blocking filters.
- Preserve old context fields while adding the new contract unless there is a strong reason not to.
- Keep all calculations deterministic and side-effect-free.
- Avoid network calls in tests.
- Use fixture candle data for regime and cost/economics tests.

Observability requirements:

Every decision involving a BUY candidate or blocked BUY candidate should expose:

- `base_signal`
- `final_signal`
- `primary_block_layer`
- `primary_block_reason`
- `all_block_reasons`
- `market_regime.regime`
- `market_regime.regime_score`
- `market_regime.trend_strength`
- `market_regime.chop_score`
- `market_regime.volatility_state`
- `market_regime.allows_entry`
- `strategy_filters`
- `pre_trade_economics` if an order size is known or can be estimated
- `execution_plan` / target shadow decision if available
- `replay_fingerprint`

Scope boundaries:

Do not:
- Replace the SMA strategy with a different strategy.
- Optimize thresholds based on a few dry-run samples.
- Add complex ML or opaque scoring.
- Make high-volatility order-size reduction the first mandatory behavior.
- Treat `net_edge_krw > 0` as sufficient by itself.
- Hide existing filter-level details under a single summary field.

Do:
- Add a layered decision contract.
- Add market regime as a separate, testable classifier.
- Add KRW-level pre-trade economics after execution sizing/planning.
- Add primary block cause selection.
- Add dry-run summary aggregation.
- Add deterministic tests.
- Preserve or improve safety.

Suggested implementation sequence:

1. Inspect repository structure:
   - strategy modules
   - execution planning modules
   - config/env parsing
   - dry-run CLI/reporting
   - tests around SMA filters, decision telemetry, execution shadow decisions, and performance gate

2. Add `MarketRegimeSnapshot` and classifier module.

3. Wire market regime into `sma_with_filter` decision context without destroying existing `filters` / `blocked_filters`.

4. Add `decision_contract_version`, `signal_flow`, `primary_block_layer`, `primary_block_reason`, and `all_block_reasons`.

5. Add `PreTradeEconomicsSnapshot` near execution planning after planned order KRW/target delta is available.

6. Add or extend dry-run summary aggregation.

7. Add tests for cause separation, economics, replay context, and backward compatibility.

8. Run the test suite.

9. If tests fail, fix regressions without weakening safety gates.

Final report format:

Return a concise but complete implementation report with these sections:

1. Summary
   - What changed
   - Whether this was a design-contract change, regime classifier change, economics change, reporting change, or tests-only change

2. Repository findings
   - Which files/modules contained existing SMA filters, cost-edge logic, execution shadow decisions, and dry-run reporting
   - Which assumptions were confirmed
   - Which assumptions were false or only partially true

3. Hypothesis status
   - Hypothesis A: supported / refuted / not fully tested, with evidence
   - Hypothesis B: supported / refuted / not fully tested, with evidence
   - Hypothesis C: supported / refuted / not fully tested, with evidence
   - Hypothesis D: supported / refuted / not fully tested, with evidence

4. Design decisions
   - Why market regime was separated from SMA filter logic
   - Why KRW economics was placed after execution planning
   - How primary block layer priority is resolved
   - Whether pre-trade economics is blocking or observation-only by default

5. Tests
   - List new tests
   - List updated tests
   - Include exact test command and result

6. Safety and recovery
   - Confirm live-order safety was not weakened
   - Confirm dry-run behavior remains safe
   - Confirm rollback path or config disable path for new behavior

7. Remaining work
   - Any thresholds that should be tuned later
   - Any summary/reporting limitations
   - Any replay gaps that still remain
```

