You are working inside the `WG-14/bithumb-bot` repository. Treat this as a live-incident hardening task, not a one-off bugfix.

Context to verify inside the repo before changing anything:

* A live EC2 incident produced this state:

  * `portfolio.asset_qty = 0.00099986`
  * `open_position_lots` had 14 `dust_tracking` rows totaling `0.001788`
  * runtime/readiness classified the system as `AUTHORITY_PROJECTION_NON_CONVERGED_PENDING`
  * `rebuild-position-authority --apply --yes` was refused as unsafe
* Do **not** assume that diagnosis is fully correct. Start from it as a working hypothesis and verify or falsify it from the repository, tests, and current code behavior.
* Recent code appears to intentionally block unsafe auto-repair for projection divergence/stale dust incidents. Do not weaken that safety posture just to make the incident “go away.”

Mission:
Analyze, verify, and improve the repository so that this problem is addressed as a **system-design weakness**, not as a narrow symptom patch. The end state should move `WG-14/bithumb-bot` toward a more robust, reproducible, and operable automated trading experiment system.

Working rules:

1. Prefer design changes over local condition hacks.
2. Keep hypotheses as hypotheses until validated by code/tests.
3. Scope the work explicitly. If a larger redesign is needed, implement the safest high-value slice and document the next slice.
4. Include tests, recovery behavior, and observability in the solution.
5. Do not “fix” the incident by merely relaxing `safe_to_apply`, bypassing safety gates, or changing operator-facing text without fixing the underlying model.
6. Keep the system conservative under uncertainty: if truth sources disagree, the system must fail closed for trading, but diagnostics and recovery plans must become clearer and more deterministic.

What to investigate first:

1. Verify whether the repo already models this incident class in tests or fixtures.
2. Trace the full authority/readiness path, including at least:

   * `position_authority_repair.py`
   * `position_authority_state.py`
   * `runtime_readiness.py`
   * lot/projection lifecycle code
   * any quantity-contract / lot-contract code
3. Determine whether the real issue is:

   * stale projection residue accumulation,
   * replay/rebuild algorithm error,
   * ambiguous authority truth model,
   * stale repair evidence being treated like current remediation guidance,
   * cross-surface inconsistency (`health` vs `recovery-report` vs `restart-checklist` vs `ops-report`),
   * or a combination.

Primary hypotheses to test explicitly:

* H1: The immediate incident is caused by stale persisted projection residue in `open_position_lots`, not by broker balance or recent fill ingestion errors.
* H2: A higher-level design problem exists because persisted projection state is treated too much like an authoritative runtime truth instead of a rebuildable/materialized view.
* H3: Recent changes intentionally hardened unsafe auto-repair, but operator-facing recovery/reporting surfaces are not fully aligned with that behavior.
* H4: Repair events are acting more like historical side-effect logs than deterministic replayable recovery units, which weakens reproducibility and operator trust.

For each hypothesis:

* Say whether current evidence supports it, weakens it, or leaves it unresolved.
* Add or update tests that can verify or falsify it deterministically.
* Avoid hand-wavy conclusions.

Concrete tasks:

1. Reproduce the incident in tests

   * Find existing fixtures/tests that resemble:

     * `portfolio.asset_qty = 0.00099986`
     * stale dust projection totaling `0.001788`
     * projection non-convergence
     * unsafe rebuild refusal
   * Reuse them if present; otherwise add a minimal, isolated regression fixture.
   * Create at least one end-to-end regression test that demonstrates the exact class of incident:

     * portfolio truth matches broker-side truth,
     * recent fills/trades are correct,
     * projection/lot truth diverges,
     * runtime becomes blocked,
     * rebuild apply is refused,
     * diagnostics clearly explain why.

2. Determine whether `open_position_lots` is effectively acting as authoritative state

   * Inspect where it is read for:

     * readiness classification,
     * dust operability,
     * entry/closeout permissions,
     * recovery-stage selection.
   * Determine whether the codebase currently treats it as:

     * canonical truth,
     * repairable projection,
     * cached materialization,
     * or an ambiguous hybrid.
   * If it is an ambiguous hybrid, design a safer separation.

3. Strengthen the truth model

   * Evaluate whether the system should center authority on:

     * `fills + orders + trades + portfolio`,
     * with `open_position_lots` demoted to a deterministic materialized projection/cache.
   * Implement the highest-value safe step toward that model, not a massive rewrite.
   * Good outcomes include:

     * deterministic rebuild from canonical evidence,
     * clearer separation between canonical truth and derived projection,
     * less chance that stale projection residue alone blocks the system indefinitely.

4. Strengthen quantity/lot contract invariants

   * Review recent quantity-contract / lot-contract logic.
   * Determine whether `QuantityContractSnapshot` (or equivalent) can become a cross-layer invariant instead of a diagnostic-only artifact.
   * If appropriate, extend the invariant so the same semantic contract is visible and comparable across:

     * execution planning,
     * fills,
     * orders,
     * open lots,
     * lifecycle projections,
     * readiness/reporting.
   * Add tests that prove when contracts align and when they diverge.

5. Unify operator/reporting surfaces

   * Audit `health`, `recovery-report`, `restart-checklist`, and `ops-report`.
   * If they can emit conflicting operator advice for the same DB snapshot, fix that.
   * They may differ in detail, but must not contradict each other about:

     * whether trading may start,
     * whether only process resume is allowed,
     * whether new entry is blocked,
     * whether closeout is blocked,
     * whether auto-repair is allowed,
     * whether inspection-only is required.
   * Prefer a shared decision object / shared structured blocker representation over ad hoc string logic.

6. Improve recovery determinism

   * Inspect whether repair records are durable proofs of state transition or just audit logs.
   * If they are just logs, improve the design so that recovery planning and applied recovery are more reproducible.
   * Good direction:

     * explicit preview/plan/applied/proof phases,
     * idempotent recovery semantics,
     * replayable evidence,
     * clear distinction between “historical repair event exists” and “current snapshot is safe to apply the same repair.”

7. Improve observability

   * Add structured diagnostics for projection divergence and truth-source disagreement.
   * At minimum, make sure the system exposes, in a machine-testable way:

     * canonical asset qty,
     * projected lot qty,
     * divergence delta,
     * convergence status,
     * repair action state,
     * structured blocker codes,
     * whether the system is in inspect-only mode.
   * Prefer enums / structured fields over brittle prose matching.

Constraints:

* Do not solve this by simply changing a threshold or removing an `unsafe` check.
* Do not make trading more permissive unless the underlying truth and replay model is actually improved.
* Avoid a giant speculative rewrite. Implement a coherent, test-backed step that clearly improves architecture and recurrence resistance.
* Preserve backwards-safe behavior where possible, but not at the expense of continuing ambiguity.

Expected deliverables:

1. Code changes.
2. New or updated tests covering:

   * the live-incident class,
   * truth divergence,
   * repair refusal conditions,
   * reporting surface consistency,
   * any new invariants introduced.
3. A short developer note or inline documentation update if needed to explain the new model.
4. Final report in this exact format:

FINAL REPORT

Summary

* What you changed and why.

Validated hypotheses

* H1: supported / weakened / unresolved

  * Evidence:
  * Test(s):
* H2: supported / weakened / unresolved

  * Evidence:
  * Test(s):
* H3: supported / weakened / unresolved

  * Evidence:
  * Test(s):
* H4: supported / weakened / unresolved

  * Evidence:
  * Test(s):

Root cause

* Immediate cause:
* Higher-level structural cause:
* What remains uncertain:

Design changes

* Structural changes implemented:
* Why these are preferable to symptom patches:
* What problem class they prevent:

Safety / operability impact

* How trading safety changed:
* How recovery behavior changed:
* How observability changed:

Tests

* New tests:
* Updated tests:
* Incident/regression coverage added:

Open follow-ups

* Follow-up items that should be done later, clearly separated from this change set.

Now inspect the repo, verify the hypotheses from code and tests, implement the most defensible structural improvement, and run the relevant tests before reporting back.

