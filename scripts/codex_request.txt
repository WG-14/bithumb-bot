You are Codex working directly in the GitHub repository `WG-14/bithumb-bot`.

Use the attached post-patch review as source context, but do not treat it as proven fact. Verify every claim against the repository before changing code. 

Your task is to harden the current research execution-timing system after the latest pending-fill patch. The repository has already moved in the right direction: `run_sma_backtest()` appears to use `_PendingFill`, delayed portfolio application, event-time boundaries, execution timing metadata, and explicit trade record annotations. Do not revert that architecture. Your job is to close the remaining semantic gap: **an exchange/execution fill and a backtest portfolio-applied trade must never be confused in regime evidence, reports, promotion gates, or operator-facing summaries.**

The core invariant to preserve is:

> A backtest must distinguish an execution attempt, an execution-side fill, a pending execution, and a portfolio-applied trade. Promotion evidence must be based on events that are actually effective in the simulated portfolio timeline, not merely on fills that occurred after the dataset’s final mark or before the portfolio state was updated.

Do not soften this into general advice. Inspect, patch, and test the actual `WG-14/bithumb-bot` code.

Inspect at least these files first:

* `src/bithumb_bot/research/backtest_engine.py`
* `src/bithumb_bot/research/execution_timing.py`
* `src/bithumb_bot/research/dataset_snapshot.py`
* `src/bithumb_bot/research/execution_model/base.py`
* `src/bithumb_bot/research/execution_model/fixed_bps.py`
* `src/bithumb_bot/research/execution_model/stress.py`
* `src/bithumb_bot/research/validation_protocol.py`
* `src/bithumb_bot/research/promotion_gate.py`
* `src/bithumb_bot/research/cli.py`
* `src/bithumb_bot/market_regime/metrics.py`
* `tests/test_orderbook_top_research.py`
* `tests/test_research_backtest_reproducibility.py`
* `tests/test_research_promotion_gate.py`
* `tests/test_dataset_quality_report.py`
* Any new execution timing / pending fill / promotion tests

Keep the following as hypotheses until verified or falsified:

H1. `_pending_trade_from_fill()` may create records where `record_type="filled_trade"` and `is_filled_trade=True` while `portfolio_applied=False`. That may be technically true from an execution-fill perspective but semantically unsafe for portfolio/regime/promotion evidence.

H2. `aggregate_regime_coverage()` may still count `portfolio_applied=False` pending fills as regime `trade_count` evidence because it filters on `_is_filled_trade()` but may not require portfolio application.

H3. `aggregate_regime_performance()` appears safer because it requires `portfolio_applied=True` for closed SELL performance, but coverage and performance may now disagree when pending fills exist.

H4. Report-level and candidate-level summaries may not yet expose explicit counts for execution attempts, execution fills, portfolio-applied trades, pending executions, skipped executions, failed executions, and closed trades.

H5. Pending fills that remain unapplied after the final dataset mark may be safe for portfolio equity, but promotion-grade candidates should not silently ignore them. They should be visible and either fail-closed or be explicitly non-promotable.

H6. Operator-facing CLI output may show signal quote coverage and skipped/warning counts, but may not yet show the new execution-event vs portfolio-trade distinction.

H7. Quote lookup may still be linear over sorted top-of-book quotes. This is lower priority than semantics, but dense top-of-book data can make research runs scale poorly.

Implementation priorities:

P0. Separate execution-fill semantics from portfolio-applied trade semantics.

Inspect `_trade_from_fill()`, `_pending_trade_from_fill()`, `_mark_trade_applied()`, and `_annotate_execution_record_type()` in `backtest_engine.py`.

If pending records can show:

```text
record_type = "filled_trade"
is_filled_trade = true
portfolio_applied = false
```

then fix the naming and metadata so the distinction is impossible to miss.

Add or normalize these fields on every execution/trade record:

```python
trade["is_execution_attempt"] = True
trade["is_execution_filled"] = bool(fill.filled_qty > 0 and fill.fill_status in {"filled", "partial"})
trade["is_portfolio_applied_trade"] = bool(trade["is_execution_filled"] and trade.get("portfolio_applied") is True)
trade["is_effective_trade"] = trade["is_portfolio_applied_trade"]
trade["is_skipped_execution"] = bool(fill.fill_status in {"skipped", "skipped_with_warning"})
trade["is_failed_execution"] = bool(fill.fill_status == "failed")
trade["portfolio_application_status"] = "pending" | "applied" | "not_applicable"
```

Keep legacy fields such as `is_filled_trade` if existing consumers/tests depend on them, but either redefine them safely or add clear compatibility semantics. A safe compatibility approach is:

```python
trade["is_filled_trade"] = trade["is_portfolio_applied_trade"]
trade["is_execution_filled"] = execution-side fill status
```

If that would break too much, keep `is_filled_trade` but add `is_portfolio_applied_trade` and make all internal evidence/gates use the portfolio-applied field.

Use explicit `record_type` values, for example:

```text
portfolio_trade
pending_execution
skipped_execution
failed_execution
```

Do not leave `pending_execution` rows labeled as `filled_trade` unless a separate field makes the portfolio distinction unambiguous.

Add tests:

```text
test_pending_execution_is_not_labeled_as_portfolio_trade
test_pending_execution_has_execution_filled_but_not_portfolio_applied_trade
test_applied_fill_becomes_portfolio_trade_after_effective_ts
test_skipped_and_failed_executions_have_not_applicable_portfolio_application_status
```

P0. Ensure regime coverage excludes pending unapplied fills.

Inspect `aggregate_regime_coverage()` and `_is_filled_trade()` in `src/bithumb_bot/market_regime/metrics.py`.

If coverage uses execution-filled pending rows as regime trade evidence, change it. Regime evidence should count portfolio-effective trades, not merely execution fills that never affected portfolio state during the dataset.

Add a helper such as:

```python
def _is_effective_trade(trade: dict[str, Any]) -> bool:
    if "is_portfolio_applied_trade" in trade:
        return bool(trade["is_portfolio_applied_trade"])
    if "portfolio_applied" in trade:
        return _is_filled_trade(trade) and bool(trade["portfolio_applied"])
    return _is_filled_trade(trade)
```

Then use `_is_effective_trade()` in regime coverage and performance where appropriate. Coverage BUY counts must not include pending unapplied BUY fills. Performance SELL counts must continue to require portfolio-applied SELLs.

Add tests:

```text
test_regime_coverage_excludes_pending_unapplied_fills
test_pending_unapplied_fill_does_not_increase_regime_trade_count
test_regime_coverage_and_performance_do_not_disagree_due_to_pending_execution
```

P0. Report pending executions at dataset end and fail or warn for promotion-grade evidence.

Inspect `run_sma_backtest()` end-of-loop pending fill handling and report generation in `validation_protocol.py`.

If fills remain pending after the final mark, they must be counted and surfaced. Add deterministic summary fields at run/candidate/report level:

```text
pending_execution_at_end_count
pending_execution_after_dataset_end_count
execution_event_timeline_incomplete
```

Promotion-grade candidates should not silently pass when they have pending executions after the dataset’s final effective mark. Implement one of the following:

Preferred:

```text
fail candidate gate with pending_execution_after_dataset_end
```

Acceptable only for explicitly non-promotion research mode:

```text
warning pending_execution_after_dataset_end
promotion_allowed = false
```

Do not let final pending executions disappear only because final equity ignores them.

Add tests:

```text
test_pending_execution_after_final_mark_is_reported
test_pending_execution_after_final_mark_is_not_promotable
test_pending_execution_after_dataset_end_affects_candidate_profile_hash
```

P1. Add report-level execution event summaries.

Inspect `_execution_metadata()`, `_report_payload()`, `_evaluate_candidates()`, run summary helpers, and CLI output.

Add an `execution_event_summary` to scenario results, candidates, and top-level reports where useful. Include at least:

```text
execution_attempt_count
execution_filled_count
portfolio_applied_trade_count
pending_execution_count
skipped_execution_count
failed_execution_count
closed_trade_count
pending_execution_at_end_count
execution_event_timeline_incomplete
```

Make the source of truth explicit:

* `execution_attempt_count`: all records with `is_execution_attempt=True`
* `execution_filled_count`: execution-side fills, regardless of portfolio application
* `portfolio_applied_trade_count`: fills applied to portfolio state
* `closed_trade_count`: closed SELL trades counted in PnL metrics
* `pending_execution_count`: execution-side fills not applied to portfolio
* `skipped_execution_count`: skipped/skipped_with_warning
* `failed_execution_count`: failed

Include the summary in candidate profile hashing. A candidate with pending execution evidence must have a different profile hash than the same candidate with fully applied portfolio evidence.

Add tests:

```text
test_report_separates_execution_attempt_count_from_closed_trade_count
test_execution_event_summary_counts_pending_skipped_failed_and_applied_events
test_candidate_profile_hash_changes_when_pending_execution_summary_changes
```

P1. Update promotion gate to use portfolio-applied evidence, not execution-filled evidence.

Inspect `promotion_gate.py`, especially:

* `build_candidate_profile()`
* `evaluate_candidate_for_promotion()`
* `validate_backtest_candidate_for_promotion()`
* `_extend_execution_reality_reasons()`
* scenario policy handling

Ensure promotion refuses or marks non-promotable candidates when:

```text
pending_execution_after_dataset_end_count > 0
execution_event_timeline_incomplete is true
portfolio_applied_trade_count is insufficient even if execution_filled_count is nonzero
execution_reality_summary is missing or inconsistent
```

Do not allow `allow_legacy_lineage=True` to bypass execution event completeness or execution reality refusal.

Add tests:

```text
test_allow_legacy_lineage_does_not_bypass_pending_execution_refusal
test_promotion_uses_portfolio_applied_trade_count_not_execution_filled_count
test_promotion_refuses_execution_event_timeline_incomplete
```

P1. Make CLI and operator summaries show execution-event distinction.

Inspect `research/cli.py` and `research/run_summary.py`.

Add concise CLI output fields:

```text
execution_attempt_count
execution_filled_count
portfolio_applied_trade_count
pending_execution_count
skipped_execution_count
failed_execution_count
closed_trade_count
execution_event_timeline_incomplete
```

Keep existing signal quote coverage output, but do not rely on it as the only execution observability surface.

P2. Improve quote lookup with bisect or cursor if low-risk.

Inspect `first_quote_after_or_equal()` and `DatasetSnapshot.sorted_execution_top_of_book_quotes()`.

If lookup still scans all sorted quotes for every signal, replace it with deterministic indexed lookup if low-risk:

* build sorted quote keys once;
* use `bisect_left(target_ts)` or `(ts, source)` keys;
* preserve first-after-or-equal semantics;
* preserve deterministic tie-break by timestamp then source.

Add a focused test if changed:

```text
test_quote_lookup_preserves_first_after_or_equal_tie_break
```

Do not let this optimization distract from P0/P1 correctness.

Scope rules:

* Do not revert the event-time execution architecture.
* Do not reintroduce candle-close or same-candle execution as promotion-grade evidence.
* Do not treat execution-side fill and portfolio-applied trade as the same concept.
* Do not use live network calls in tests.
* Do not modify live trading behavior unless clearly necessary and covered by tests.
* Preserve deterministic reproducibility.
* Prefer additive fields over breaking JSON consumers, but do not preserve unsafe semantics internally.
* If legacy fields remain, document their compatibility role in code/tests and route new internal logic through the safer fields.
* Do not implement full-depth orderbook replay unless the repository already has full-depth structures and tests. Keep `top_of_book_is_full_depth=false` visible where applicable.

Required tests to run:

At minimum run:

```bash
pytest -q tests/test_research_backtest_reproducibility.py tests/test_orderbook_top_research.py tests/test_research_promotion_gate.py tests/test_dataset_quality_report.py
```

Also run every new or modified test.

If feasible, run:

```bash
pytest -q
```

Do not claim the full suite passed unless it was actually run.

Expected final report format:

Return a concise but complete implementation report with exactly these sections:

1. `Hypotheses checked`

   * For H1 through H7, state `verified`, `falsified`, or `partially verified`.
   * Include the concrete files/functions/tests used as evidence.

2. `Design changes made`

   * Summarize execution-fill vs portfolio-applied trade separation.
   * Summarize regime coverage/performance changes.
   * Summarize pending execution end-of-dataset handling.
   * Summarize execution event summaries and candidate profile hashing.
   * Summarize promotion gate changes.
   * Summarize quote lookup optimization if implemented.

3. `Behavior before vs after`

   * Explain how pending fills were represented before.
   * Explain how pending, skipped, failed, execution-filled, and portfolio-applied records are represented now.
   * Explain how regime coverage counts trade evidence now.
   * Explain how final pending executions affect reports and promotion.
   * Explain how CLI/operator summaries changed.

4. `Tests added/updated`

   * List each test name and the invariant it locks.
   * Include any existing tests updated.

5. `Promotion and safety impact`

   * State when pending executions or incomplete execution timelines block promotion.
   * State how portfolio-applied trade counts differ from execution-filled counts in promotion evidence.
   * State whether any legacy/non-promotable behavior remains and how it is labeled.

6. `Observability and reproducibility`

   * List new or changed metadata/report fields.
   * State whether manifest hash, dataset hash, candidate profile hash, or content hash behavior changed.
   * State how pending/skipped/failed/execution-filled/portfolio-applied events remain observable.

7. `Limitations and follow-up`

   * Explicitly mention no full-depth or intra-candle-path replay unless implemented.
   * Mention any remaining event-time accounting approximation.
   * Mention quote lookup performance if optimization was deferred.

8. `Commands run`

   * Include exact commands and pass/fail results.
   * If any command was not run, say so and why.

Do not submit a final answer that only says tests pass. The final report must prove that execution-side fills and backtest portfolio-effective trades are no longer conflated in regime evidence, reports, promotion gates, or operator-facing summaries.
