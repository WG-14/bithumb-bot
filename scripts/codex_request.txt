````text
You are working in the GitHub repository `WG-14/bithumb-bot`.

The previous patch appears to have successfully introduced the core structure:
- `decision_contract.py` with `decision_v2`, `signal_flow`, `primary_block_layer`, `primary_block_reason`, `all_block_reasons`, and block-layer priority.
- `strategy/market_regime.py` with `MarketRegimeSnapshot` and `classify_sma_market_regime()`.
- Strategy integration in `sma.py` using `apply_decision_contract`, `build_replay_fingerprint`, and `classify_sma_market_regime`.
- `pre_trade_economics.py` with order-sized expected edge/cost/net-edge calculations.
- Execution-stage pre-trade economics wiring, apparently in or near `execution_service.py`.
- Config/env support for `SMA_MARKET_REGIME_ENABLED`, `MIN_NET_EDGE_KRW`, `MIN_MARGIN_AFTER_COST_RATIO`, and `PRE_TRADE_ECONOMICS_BLOCKING_ENABLED`.
- Tests for regime blocking, cost-edge separation, replay fingerprint, and pre-trade economics observation/blocking modes.

Do not redo the previous patch from scratch. Verify the current repository state first, then close the remaining operational gaps.

Core problem statement:
The repository now has better per-decision explanation, but it may still fail the original operational goal: an operator running live dry run should be able to see, in one CLI/report view, why BUY is not happening and whether the system is waiting correctly, over-filtering, blocked by execution/order rules, or seeing economically meaningless candidates.

This is not a general reporting improvement. It is the missing final layer of the decision-contract design: converting `decision_v2` fields into an operator-readable dry-run summary and ensuring those fields are visible through telemetry, not buried in raw JSON.

Keep the following hypotheses as hypotheses and implement verification support for them.

Hypothesis A:
The previous patch improved individual decision rows, but the operator summary still does not aggregate `decision_v2` block causes.

Verification:
Search the repo for actual implemented CLI/reporting output, not just `scripts/codex_request.txt`.
Look for modules such as:
- `reporting.py`
- CLI command files
- decision telemetry commands
- live dry-run reporting
- scripts that read `strategy_decisions`

Check whether any real command prints a summary like:
- base BUY candidates
- final BUY count
- block layer counts
- block reason counts
- market regime distribution
- BUY candidate pre-trade economics averages

If this does not exist, implement it.

Hypothesis B:
The new fields exist in context JSON but are not surfaced in decision telemetry CLI/report output.

Verification:
Inspect decision telemetry tests and CLI output.
Determine whether the following fields are visible to an operator without manually digging through raw JSON:
- `decision_contract_version`
- `primary_block_layer`
- `primary_block_reason`
- `all_block_reasons`
- `market_regime.regime`
- `market_regime.volatility_state`
- `market_regime.allows_entry`
- `pre_trade_economics.order_krw`
- `pre_trade_economics.net_edge_krw`
- `pre_trade_economics.meaningful_edge`

If not, add output and tests.

Hypothesis C:
`replay_fingerprint.through_ts_ms` currently conflates candle timestamp and strategy cutoff timestamp.

Verification:
Inspect `build_replay_fingerprint()` and where it is called from `sma.py`.
If `through_ts_ms` is set equal to `candle_ts`, but `signal_through_ts_ms` can differ from the last candle timestamp, fix the API and callsite so both values are preserved:
- `candle_ts`: actual final candle timestamp used for the decision.
- `through_ts_ms`: actual candle-query cutoff / strategy decision cutoff.

Add a test with an explicit `through_ts_ms` that differs from the last candle timestamp.

Hypothesis D:
High-volatility regime is not yet observable.

Verification:
Inspect `classify_sma_market_regime()`.
If volatility only supports `low` and `normal`, consider adding high-volatility observability, but do not make high-vol order-size reduction mandatory in this patch.

This is P2. Do not prioritize it over dry-run summary and telemetry surfacing.

Primary implementation priorities:

P1 — Required: Add `decision_v2` dry-run/operator summary aggregation

Find the current best location for dry-run or decision telemetry reporting. Prefer extending an existing command over creating a duplicate command, unless the existing structure makes that messy.

The summary must aggregate recent decision rows, preferably from the same source/table used by current decision telemetry.

Target output shape:

[DRY-RUN DECISION SUMMARY]
window: last 300 decisions
base BUY candidates: 14
final BUY: 0

block layers:
- market_regime: 8
- strategy_filters: 4
- position_gate: 1
- execution_order_rule: 1
- pre_trade_economics: 0
- fee_authority: 0
- performance_gate: 0

block reasons:
- market_regime.chop_market: 8
- strategy_filters.cost_edge: 4
- position_gate.already_in_position: 1
- execution_order_rule.min_notional: 1

regime distribution:
- trend_up: 18%
- chop: 61%
- low_vol: 13%
- overextended_up: 8%
- unknown: 0%

BUY candidate economics:
- count_with_economics: 10
- avg_order_krw: 10000
- min_order_krw: 5000
- max_order_krw: 20000
- avg_expected_edge_krw: 12
- avg_expected_cost_krw: 15
- avg_net_edge_krw: -3
- meaningful_edge_count: 0

Implementation details:
- Parse context defensively. Older decision rows may not have `decision_v2`.
- If a row lacks `signal_flow`, fall back to legacy fields where possible.
- Do not crash on malformed or missing JSON.
- Count `base_signal=BUY` as BUY candidates, even if final signal is HOLD.
- Count `final_signal=BUY` separately.
- For block layer/reason counts, prefer:
  - `context.signal_flow.primary_block_layer`
  - `context.signal_flow.primary_block_reason`
  - `context.signal_flow.all_block_reasons`
  - fall back to top-level `primary_block_layer`, `primary_block_reason`, `all_block_reasons`.
- For market regime distribution, read `context.market_regime.regime`.
- For economics, read `context.pre_trade_economics`.

Add tests for:
- Aggregating mixed decision_v2 rows.
- Handling older legacy rows without decision_v2.
- Handling missing/malformed context safely.
- Correctly counting BUY candidates vs final BUY.
- Correctly aggregating block layers and block reasons.
- Correctly aggregating market regimes.
- Correctly aggregating economics only when present.

P1 — Required: Surface decision_v2 fields in decision telemetry CLI/report output

Find the existing decision telemetry command or report output. Add visible fields so the operator does not need to inspect raw JSON.

At minimum, each relevant decision display or compact summary should surface:
- `primary_block_layer`
- `primary_block_reason`
- `all_block_reasons`
- `market_regime.regime`
- `market_regime.volatility_state`
- `market_regime.allows_entry`
- `pre_trade_economics.order_krw`
- `pre_trade_economics.net_edge_krw`
- `pre_trade_economics.meaningful_edge`

Add or update a test named similarly to:

`test_decision_telemetry_cli_exposes_decision_v2_fields`

The test should verify that CLI/report output includes:
- `primary_block_layer`
- `primary_block_reason`
- `market_regime`
- `volatility_state`
- `net_edge_krw`
- `meaningful_edge`

Do not merely assert that these keys exist in raw context JSON. Assert that the operator-facing output includes them.

P2 — Recommended: Fix replay fingerprint timestamp separation

Inspect current `build_replay_fingerprint()` signature and callsite.

If it currently sets:

`through_ts_ms = candle_ts`

change it to accept separate values:

```python
build_replay_fingerprint(
    *,
    strategy_name: str,
    pair: str,
    interval: str,
    candle_ts: int | None,
    through_ts_ms: int | None,
    short_n: int,
    long_n: int,
    thresholds: dict[str, object],
    fee_authority: dict[str, object],
    slippage_bps: float,
    regime_version: str,
    order_sizing: dict[str, object] | None = None,
)
````

Then call it from strategy code with:

* `candle_ts=ts_list[-1]`
* `through_ts_ms=signal_through_ts_ms`

Add a focused test:

* Set up a decision with explicit `through_ts_ms`.
* Ensure `replay_fingerprint["candle_ts"]` equals the last candle timestamp.
* Ensure `replay_fingerprint["through_ts_ms"]` equals the explicit cutoff timestamp.
* Ensure existing replay fingerprint fields still exist.

P2 — Optional: Add high-volatility observability only if low-risk

If straightforward, add:

* `SMA_FILTER_VOL_MAX_RANGE_RATIO=0` default, disabled when zero.
* If `volatility_ratio > max`, classify `volatility_state="high"` and `regime="high_vol"`.

Initial behavior:

* Do not implement high-vol order-size scaling.
* Do not tune thresholds.
* Do not make high-vol behavior complex.
* Either make `high_vol` block entry conservatively or observation-only, following existing repo conventions. Document the behavior clearly.
* Add tests if implemented.

If this expands scope too much, skip it and explicitly report that high-vol observability remains future work.

P2 — Repository hygiene: Handle `scripts/codex_request.txt`

Search whether `scripts/codex_request.txt` is intentionally kept.
If it is just a temporary prompt file, remove it.
If it should remain as documentation, move or convert the relevant durable content into something like:

`docs/decision_v2_design.md`

Do not keep temporary Codex prompt text in `scripts/` if it creates search noise for operational code work.

Safety and recovery requirements:

Do not:

* Weaken live order safety.
* Enable real orders.
* Change `LIVE_DRY_RUN` / `LIVE_REAL_ORDER_ARMED` protections.
* Make `PRE_TRADE_ECONOMICS_BLOCKING_ENABLED` default to true.
* Make high-vol order-size reduction mandatory.
* Tune strategy thresholds based on guesses.
* Remove legacy context fields.
* Break old telemetry rows.

Do:

* Preserve backward compatibility.
* Parse older rows defensively.
* Keep new summary/reporting side-effect-free.
* Keep tests deterministic.
* Avoid network calls in tests.
* Prefer additive CLI/report output over destructive format changes unless existing tests require adaptation.
* Ensure malformed context does not crash summary commands.

Suggested implementation sequence:

1. Inspect current reporting/CLI/telemetry code paths.
2. Locate where `strategy_decisions` or equivalent decision records are read.
3. Add a small pure function for summary aggregation, for example:

   * `build_decision_v2_summary(rows: Sequence[...]) -> dict`
   * or similar, matching repo style.
4. Add formatter function for operator-readable output.
5. Wire formatter into existing decision telemetry or dry-run summary command.
6. Add tests for the pure aggregation function.
7. Add CLI/report output tests for visible decision_v2 fields.
8. Fix replay fingerprint timestamp separation if needed.
9. Optionally add high-vol observability only if small and testable.
10. Clean up or document `scripts/codex_request.txt`.
11. Run the test suite.

Testing requirements:

Run the existing test suite with the repository’s normal test command, likely:

`pytest`

Also run any narrower relevant test files first if useful.

Required new or updated tests:

* `test_decision_v2_summary_counts_buy_candidates_and_final_buys`
* `test_decision_v2_summary_counts_block_layers_and_reasons`
* `test_decision_v2_summary_counts_market_regimes`
* `test_decision_v2_summary_aggregates_pre_trade_economics`
* `test_decision_v2_summary_handles_legacy_or_malformed_context`
* `test_decision_telemetry_cli_exposes_decision_v2_fields`
* `test_replay_fingerprint_preserves_distinct_through_ts_ms` if timestamp bug exists

The exact names may vary, but the coverage must be present.

Final report format:

Return a concise implementation report with these sections:

1. Summary

   * What was changed.
   * Whether the main patch was summary aggregation, telemetry surfacing, replay fingerprint fix, high-vol observability, or cleanup.

2. Repository findings

   * Where existing decision contract, market regime, pre-trade economics, execution wiring, and telemetry/reporting code live.
   * Which assumptions were confirmed.
   * Which assumptions were false or only partially true.

3. Hypothesis status

   * Hypothesis A: supported / refuted / partially supported, with evidence.
   * Hypothesis B: supported / refuted / partially supported, with evidence.
   * Hypothesis C: supported / refuted / partially supported, with evidence.
   * Hypothesis D: supported / refuted / deferred, with evidence.

4. Design decisions

   * Why the summary aggregation was placed where it was.
   * How old/non-decision_v2 rows are handled.
   * How block layer/reason priority is resolved in summary.
   * Whether high-vol observability was implemented or deferred.
   * Whether `scripts/codex_request.txt` was removed, moved, or kept.

5. Tests

   * List new tests.
   * List updated tests.
   * Include exact commands run and results.

6. Safety and recovery

   * Confirm live-order safety was not weakened.
   * Confirm pre-trade economics blocking default remains unchanged.
   * Confirm dry-run behavior remains safe.
   * Explain how to disable or ignore the new reporting if needed.

7. Remaining work

   * Any known limitations in summary/reporting.
   * Whether high-vol regime support remains future work.
   * Any replay or telemetry gaps that still remain.

```
```

