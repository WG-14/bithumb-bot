You are working in the GitHub repository `WG-14/bithumb-bot`.

Your task is not to apply a narrow symptom patch. Treat the latest EC2 incident as evidence of a broader post-fill state-transition design problem in a live automated trading experiment system. Investigate, verify, modify, and test the repository so that the system becomes more robust, reproducible, observable, and operable.

Do not assume the diagnosis below is automatically true. Use it as a starting set of hypotheses. Confirm or refute each hypothesis from the repository code, tests, and any available fixtures/log evidence. Preserve safety: do not make changes that blindly trust exchange data, hide uncertainty, or bypass startup/readiness gates.

Context from the incident:

A live Bithumb KRW-BTC order filled successfully, but the bot halted after one BUY/SELL because fill accounting became fee-pending. EC2 DB/log evidence showed:

```text
broker_fill_observations:
  fee=27.86
  fee_status=order_level_candidate
  accounting_status=fee_pending
  parse_warnings=["missing_fee_field", "order_level_fee_candidate:paid_fee"]

raw_payload:
{
  "order_fee_fields": {"paid_fee": "27.86"},
  "trade": {
    "created_at": "2026-04-24T23:57:03+09:00",
    "funds": "69664.8389",
    "market": "KRW-BTC",
    "price": "116110000",
    "side": "bid",
    "uuid": "C0101000000983750807",
    "volume": "0.00059999"
  }
}
```

Manual repair with `fee=27.86` succeeded and accounting converged:

```text
replay_cash=307,223.267
replay_qty=0.0007998500
portfolio_cash=307,223.267
portfolio_qty=0.0007998500
unresolved_fee_state=0
accounting_projection_ok=1
```

But the next readiness blocker became authority projection drift:

```text
recovery_stage=AUTHORITY_PROJECTION_NON_CONVERGED_PENDING
blocking_incident_class=HISTORICAL_FRAGMENTATION_PROJECTION_DRIFT
projected_total_qty=0.002587970000
portfolio_qty=0.000799850000
```

The current high-confidence suspicion is:

1. Bithumb is not simply omitting fee data. The bot receives `paid_fee`.
2. The repository currently treats `paid_fee` as an order-level candidate, not an accounting-authoritative fee.
3. `order_level_candidate` is not in the accounting-complete fee status set, so normal fills become `fee_pending`.
4. This turns normal live fills into `RECOVERY_REQUIRED` incidents.
5. Separately, after fee repair, `open_position_lots` / position authority projection can remain non-converged against canonical portfolio quantity.

Repository areas to inspect first:

```text
src/bithumb_bot/broker/bithumb_order_queries.py
src/bithumb_bot/broker/myorder_events.py
src/bithumb_bot/fee_observation.py
src/bithumb_bot/broker/live_submission_execution.py
src/bithumb_bot/recovery.py
src/bithumb_bot/runtime_readiness.py
src/bithumb_bot/position_authority_state.py
tests/test_myorder_events.py
tests/test_myorder_runtime_ingestion.py
tests/test_bithumb_private_api.py
tests/test_recovery_restart_regression.py
tests/test_position_authority_recovery_state.py
tests/test_accounting_projection.py
```

Important constraints:

Do not simply change every `paid_fee` into `complete`.
Do not weaken readiness gates.
Do not make multi-fill aggregate `paid_fee` automatically apply to individual fills unless you implement a validated allocation policy with tests.
Do not hide `fee_pending`; replace unnecessary operator intervention with explicit validation and observable provenance.
Do not treat historical repair records as proof of current state convergence.
Do not make `rebuild-position-authority` mutate live state unless the command already supports safe preview/apply semantics and tests prove idempotence.

Primary goal:

Design and implement a fee evidence validation pipeline that can safely auto-promote a single-fill order-level `paid_fee` into accounting-complete evidence when, and only when, the surrounding evidence proves it is safe.

Expected design direction:

Introduce or refactor toward an explicit fee authority model. Avoid leaving accounting eligibility encoded only in a flat `fee_status` string.

A useful model would distinguish:

```text
fee_source:
  trade_level_fee
  order_level_paid_fee
  configured_fee_rate
  operator_confirmed
  missing

fee_confidence:
  authoritative
  validated
  estimated
  ambiguous
  invalid

accounting_eligibility:
  complete
  pending
  blocked
```

Implement the minimum viable version that fits the existing codebase.

Required validation policy for the incident class:

For a REST `/v1/order` or private stream event where trade-level fee is missing but order-level `paid_fee` exists, auto-promote only if all of these are true:

```text
- exactly one fill/trade is present for the order, or the event is otherwise provably single-fill
- order-level paid_fee is present, finite, non-negative
- fill volume and order executed_volume match within strict quantity tolerance, when both are available
- fill funds and order executed_funds match within KRW tolerance, when both are available
- paid_fee is consistent with the configured or observed fee-rate expectation within a documented KRW rounding/tolerance policy
- identifiers are consistent: client_order_id, exchange_order_id, fill_id/trade uuid
- notional is material enough that zero/missing fee would still be suspicious
```

Expected output status/provenance for this case:

```text
fee_status = validated_order_level_paid_fee
fee_source = order_level_paid_fee
fee_confidence = validated
accounting_status = accounting_complete
provenance = order_level_paid_fee_validated_single_fill
```

The exact field names may differ if the existing schema makes another naming better, but the semantics must remain explicit and observable.

Multi-fill behavior:

For multiple fills with only aggregate `paid_fee`, keep the result pending unless you implement and test a correct allocation policy. The system should report this as:

```text
fee_confidence=ambiguous
accounting_eligibility=pending
reason=multi_fill_order_level_fee_ambiguous
```

Do not regress existing safety behavior for ambiguous or invalid fees.

Required tests:

Add a real incident fixture. Create a fixture similar to:

```text
tests/fixtures/bithumb/live_paid_fee_single_fill_buy_2026_04_24.json
```

with:

```json
{
  "order_fee_fields": {"paid_fee": "27.86"},
  "trade": {
    "created_at": "2026-04-24T23:57:03+09:00",
    "funds": "69664.8389",
    "market": "KRW-BTC",
    "price": "116110000",
    "side": "bid",
    "uuid": "C0101000000983750807",
    "volume": "0.00059999"
  }
}
```

Add or update tests proving:

1. Existing behavior is reproduced before the fix or clearly documented in test history: `paid_fee` was previously classified as `order_level_candidate`.
2. Single-fill `paid_fee` with matching quantity/funds/expected fee rate is validated and accounting-complete.
3. `fee_accounting_status` recognizes the new validated status as complete.
4. `live_submission_execution` does not transition the order to `RECOVERY_REQUIRED` for this validated single-fill `paid_fee` case.
5. Multi-fill aggregate `paid_fee` remains pending or operator-review-required.
6. Missing fee remains pending.
7. Invalid, negative, NaN, unparseable, or suspicious zero fee remains blocked/pending according to existing safety rules.
8. Fee-rate mismatch beyond tolerance remains operator-review-required.
9. REST `/v1/order` and private `myOrder` normalization use the same fee authority semantics or have tests documenting why they differ.
10. Recovery report or broker diagnostics exposes why an order-level fee was auto-validated or why auto-validation failed.

Required observability changes:

Add structured evidence to broker fill observations or related diagnostic output. At minimum, make it possible to answer from logs/recovery-report:

```text
fee_auto_validation:
  eligible=true/false
  result=validated|pending|blocked
  fee_source=...
  fee_confidence=...
  reason=...
  checks:
    single_fill=...
    paid_fee_present=...
    executed_volume_match=...
    executed_funds_match=...
    expected_fee_rate_match=...
    identifiers_match=...
```

Do not require operators to inspect raw JSON to understand why fee accounting did or did not apply.

Required recovery/readiness work:

Do not stop at the fee fix. Investigate the second blocker class:

```text
AUTHORITY_PROJECTION_NON_CONVERGED_PENDING
projected_total_qty=0.002587970000
portfolio_qty=0.000799850000
```

Treat this as a separate hypothesis: `open_position_lots` is a materialized authority projection that can drift from canonical accounting/portfolio after manual repairs, dust handling, partial sells, or historical fragmentation.

Verify from the repository whether:

```text
- canonical truth is orders/fills/trades plus portfolio
- open_position_lots is intended as a rebuildable materialized projection
- repair events are historical evidence, not current-state proof
- full projection rebuild is idempotent and portfolio-anchored
- readiness correctly blocks when projection and portfolio do not converge
```

Add or update tests for authority projection invariants where feasible:

```text
SUM(open_position_lots.qty_open where qty_open > eps)
  == portfolio.asset_available + portfolio.asset_locked
  within tolerance

SUM(open_exposure qty) + SUM(dust_tracking qty)
  == SUM(open_position_lots.qty_open)

sellable_executable_qty <= broker/portfolio asset_available

fills/trades replay portfolio == portfolio table

position authority rebuild is idempotent

repair event recorded != current state converged unless projection actually matches
```

If a full fix for position authority projection is too large, do not implement a risky partial mutation. Instead, deliver a scoped diagnostic/reporting improvement plus a precise follow-up plan. But do implement the fee authority fix if the hypotheses are confirmed.

Required commands to run:

Run targeted tests first, then broader tests relevant to live recovery:

```bash
uv run pytest -q tests/test_myorder_events.py
uv run pytest -q tests/test_myorder_runtime_ingestion.py
uv run pytest -q tests/test_bithumb_private_api.py -k "fee or paid_fee or order_level_candidate or get_fills"
uv run pytest -q tests/test_recovery_restart_regression.py
uv run pytest -q tests/test_position_authority_recovery_state.py
uv run pytest -q tests/test_accounting_projection.py
```

If the test suite is large or slow, run the most relevant subsets first, but report exactly what was and was not run.

Also run static or formatting checks if the repo has them configured. Inspect `pyproject.toml`, CI config, or Makefile to find the canonical commands.

Deliverables:

1. Code changes implementing validated fee evidence handling.
2. Tests using the real incident fixture and negative cases.
3. Observability/reporting changes showing fee validation result and failed checks.
4. A written analysis in the final response that keeps hypotheses distinct from confirmed findings.
5. A clear statement of what remains unresolved about position authority projection drift, unless fully fixed and tested.
6. A safe operator note describing how this changes live behavior.

Final report format:

Use this exact structure:

```markdown
## Summary

## Confirmed Findings

## Refuted or Unconfirmed Hypotheses

## Design Changes Made

## Fee Authority Policy

## Recovery and Readiness Impact

## Position Authority Projection Findings

## Tests Added or Updated

## Commands Run

## Remaining Risks

## Follow-up Work
```

In `Confirmed Findings`, cite concrete files/functions and explain the pre-change behavior.

In `Design Changes Made`, explain why the change prevents the problem class rather than just suppressing this one incident.

In `Remaining Risks`, explicitly cover multi-fill order-level fee ambiguity and any unresolved projection drift.

In `Follow-up Work`, include longer-term improvements such as:

```text
- shared fee authority module across REST and private stream ingestion
- DB schema columns for fee_source, fee_confidence, validation_checks_json
- broker-diagnose warning when live fee policy would halt after next fill
- fee schema drift canary for paid_fee, reserved_fee, remaining_fee, trade.fee, executed_funds, executed_volume
- idempotent canonical replay-based rebuild of open_position_lots
- invariant-driven readiness checks after every live fill
```

Be strict: if the repo already has an intended policy that forbids automatic accounting from order-level `paid_fee`, do not silently override it. Document the policy conflict and either implement the safer validated intermediate state or report why the current repo cannot support it safely without a larger design change.

