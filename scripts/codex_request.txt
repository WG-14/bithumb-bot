You are working in the GitHub repository `WG-14/bithumb-bot`.

Your task is to investigate and implement a structural fix for a live-readiness incident observed on EC2. Do not treat the current diagnosis as proven fact. Use it as the starting hypothesis, verify or falsify it from the repository code and tests, then implement the smallest coherent design change that moves the repo toward a more robust, reproducible, operable automated trading experiment system.

Do not solve this by flattening the position, mutating the DB manually, weakening startup gates, or adding a narrow one-off suppression. The goal is to model the state correctly and prevent this entire class of incidents from being misclassified or mis-recommended in the future.

Incident facts from EC2 to encode as a reproducible fixture or test scenario:

```text
broker-diagnose:
  overall=PASS
  cash_available≈341,778 KRW
  asset_available=0.00049980 BTC
  open orders=0

open_position_lots:
  lot 353:
    entry_trade_id=32
    qty_open=0.00039982
    position_state=dust_tracking
    executable_lot_count=0
    dust_tracking_lot_count=1
    internal_lot_size=0.00040000
    lot_min_qty=0.0002
    lot_qty_step=0.0001
    lot_min_notional_krw=5000
    lot_rule_source_mode=full_projection_rebuild_portfolio_anchor
    entry_decision_linkage=degraded_recovery_unattributed

  lot 355:
    entry_trade_id=34
    qty_open=0.00009998
    position_state=dust_tracking
    executable_lot_count=0
    dust_tracking_lot_count=1
    internal_lot_size=0.00040000
    lot_min_qty=0.0002
    lot_qty_step=0.0001
    lot_min_notional_krw=5000
    lot_rule_source_mode=ledger
    entry_decision_linkage=direct

sum(open_position_lots.qty_open)=0.00049980 BTC
broker_qty=0.00049980 BTC
portfolio_qty=0.00049980 BTC

recent lifecycle:
  trade 32 BUY residual consumed by trade 33 SELL 0.00040000
  trade 34 BUY consumed by trade 35 SELL 0.00040000
  remaining residuals total 0.00049980 BTC

fills.trade_id linkage:
  29 null trade_id rows are 1:1 matchable to trades by client_order_id + qty + price + fee
  1 null trade_id row is unmatched and must remain operator-review, not auto-mutated
```

Primary working hypothesis:

```text
The incident is not primarily a broker/API failure, not a broker/portfolio quantity mismatch, and not necessarily a position-authority projection divergence.

The likely direct issue is:
  broker_qty == portfolio_qty == sum(open_position_lots.qty_open)
  executable_lot_count == 0
  dust_tracking_qty > 0
  no unresolved orders

but the repo does not classify this as a first-class tradeability state. It is currently liable to be represented as POSITION_AUTHORITY_CORRECTION_REQUIRED / LOT_AUTHORITY_CONFLICT / repair-oriented recovery, which pushes operators toward rebuild-position-authority, flatten, or manual repair even though the better response is residual policy review.

The intended state name is:
  NON_EXECUTABLE_RESIDUAL_HOLDINGS
or:
  BROKER_MATCHED_RESIDUAL_ONLY_POSITION
```

Keep these as hypotheses until verified. For each hypothesis below, either verify it with tests/code evidence or clearly falsify it in your final report.

Hypotheses to verify or falsify:

1. Direct classification hypothesis

```text
When:
  broker_qty == portfolio_qty == sum(open_position_lots.qty_open)
  projection_converged == true
  executable_lot_count == 0
  dust_tracking_qty > 0
  open_order_count == 0
  recovery_required_count == 0

Then:
  runtime readiness should fail closed with reason NON_EXECUTABLE_RESIDUAL_HOLDINGS
  not POSITION_AUTHORITY_CORRECTION_REQUIRED
  not LOT_PROJECTION_DIVERGENCE
  not a rebuild-position-authority recommendation
  not flatten-position
```

Verification:
Add or update tests around `compute_runtime_readiness_snapshot()`, recovery-report, and repair-plan output using a fixture equivalent to the EC2 state above.

Falsification:
If the code proves there is actual projection divergence, broker/portfolio mismatch, unresolved order state, or true correction needed, document it and adjust the classification accordingly.

2. Residual modeling hypothesis

```text
dust_tracking is too coarse. It currently conflates:
  true dust
  ledger split residual
  near-lot residual
  portfolio-anchor projection residual
  degraded recovery residual
```

Verification:
Inspect `dust.py`, `lifecycle.py`, `position_authority_state.py`, and `runtime_readiness.py`. Add a residual inventory/classification layer if the current model cannot distinguish these cases.

Expected classification for the EC2 fixture:

```text
lot 353:
  NEAR_LOT_RESIDUAL
  PORTFOLIO_ANCHOR_RESIDUAL
  DEGRADED_RECOVERY_RESIDUAL

lot 355:
  LEDGER_SPLIT_RESIDUAL
  possibly TRUE_DUST depending on exchange min qty/notional policy
```

Falsification:
If an existing model already exposes these distinctions, wire that model into runtime readiness, repair-plan, and operator output instead of creating a duplicate abstraction.

3. Repair recommendation hypothesis

```text
repair_plan currently does not adequately distinguish quantity/projection repair from tradeability policy block.
```

Verification:
For the EC2 fixture, `repair-plan` should produce:

```text
primary_incident_class=TRADEABILITY_POLICY
recommended_mode=residual_policy_review
recommended_command=residual-closeout-plan or recovery-report
candidate_repair.rebuild-position-authority.needed=false
flatten_primary_recommendation=false
```

It must not recommend:

```text
rebuild-position-authority --apply --yes
rebuild-position-authority --full-projection-rebuild --apply --yes
flatten-position
resume
run
```

Falsification:
If actual projection divergence exists in the fixture after correct setup, repair recommendation may remain valid, but then the report must explain exactly which invariant fails.

4. Fill-trade linkage hypothesis

```text
fills.trade_id null rows are not the direct cause of this incident, but they weaken deterministic replay and forensic quality.
```

Verification:
Implement or improve a read-only diagnostic:

```bash
MODE=live uv run bithumb-bot diagnose-fill-trade-linkage
```

It should count:

```text
fills_total
fills_missing_trade_id
missing_but_safely_matchable
ambiguous
unmatchable
```

Optional repair command only if it is safe and idempotent:

```bash
MODE=live uv run bithumb-bot repair-fill-trade-linkage --dry-run
MODE=live uv run bithumb-bot repair-fill-trade-linkage --apply --yes
```

Auto-apply only when:

```text
trade_id is null
matched_trade_count == 1
client_order_id matches
qty matches
price matches within existing tolerance
fee matches within existing tolerance
timestamp is equal or very close
```

The unmatched row must remain operator-review and must not be guessed.

5. Higher-level authority hierarchy hypothesis

```text
The repo has pieces of the right model, but the operator decision surface does not cleanly separate:
  ledger truth
  portfolio/accounting projection
  materialized lot projection
  executable authority
  residual tradeability policy
  repair recommendations
```

Verification:
Inspect existing abstractions before adding new ones. Prefer evolving existing structures in:

```text
src/bithumb_bot/dust.py
src/bithumb_bot/lifecycle.py
src/bithumb_bot/position_authority_state.py
src/bithumb_bot/runtime_readiness.py
src/bithumb_bot/repair_plan.py
src/bithumb_bot/app.py
tests/test_dust.py
tests/test_position_authority_recovery_state.py
tests/test_operator_commands.py
```

Implement a clearer separation if needed:

```text
PositionTruth:
  broker_qty
  portfolio_qty
  accounting_replay_qty
  open_position_lots_total_qty
  projection_converged

ExecutableAuthority:
  open_lot_count
  executable_qty
  sellable_executable_qty
  authority_gap_reason

ResidualInventory:
  residual_qty
  residual_notional
  residual_classes[]
  exchange_sellable
  strategy_sellable

TradeabilityState:
  run_loop_allowed
  new_entry_allowed
  closeout_allowed
  reason_code
  operator_next_action
```

Do not overbuild. Add only the parts needed to make this incident class explicit, testable, and visible.

Implementation priorities:

P0 — classification and gating

Add a first-class reason code, preferably:

```text
NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

Use it when:

```text
broker/local/accounting/lot quantities match or are explicitly converged
projection_converged == true
executable_qty == 0
tracked residual qty > 0
no open/unresolved/recovery-required orders
no fee/accounting blocker that should take precedence
```

Expected behavior:

```text
resume_ready=false
run_loop_allowed=false unless policy explicitly allows residual-only resume
new_entry_allowed=false by default for material residuals
closeout_allowed=false for strategy closeout if no executable lot
operator_action_required=true
operator_next_action=residual_policy_review or residual-closeout-plan
recommended_command must not be rebuild-position-authority or flatten-position
```

P0 — repair-plan safety

Update `repair_plan.py` so broker-matched residual-only holdings are treated as a tradeability policy state, not an accounting root cause or position-authority rebuild need.

For this state:

```text
primary_incident_class=TRADEABILITY_POLICY
recommended_mode=residual_policy_review
accounting_root_cause_unresolved=false
flatten_primary_recommendation=false
candidate_repair.rebuild-position-authority.needed=false
recommended_command=uv run python bot.py residual-closeout-plan
```

If `residual-closeout-plan` is not implemented in the same patch, use `recovery-report` as the command but include a TODO/test for the future command. Prefer implementing the read-only plan now.

P0 — residual inventory / residual classification

Expose residual details in runtime readiness and reports:

```text
residual_qty
residual_notional_krw
residual_lot_count
residual_classes
exchange_sellable
strategy_sellable
residual_rows[]
```

Each row should include:

```text
lot_id
entry_trade_id
qty_open
position_state
source_mode
entry_decision_linkage
internal_lot_size
near_lot_delta
below_exchange_min_qty
below_exchange_min_notional
classes[]
```

Do not make dust-tracking sellable for strategy submission. Preserve the lot-native contract that SELL authority comes from executable lot counts, not aggregate quantity.

P0 — tests

Add regression tests that reproduce the EC2 incident without requiring live broker calls.

At minimum add tests equivalent to:

```text
test_broker_matched_residual_only_holdings_block_resume_without_rebuild_or_flatten
```

Assert:

```text
broker_qty == portfolio_qty == sum(open_position_lots.qty_open)
projection_converged == true
executable_qty == 0
tracked_residual_qty == 0.00049980
resume_ready == false
reason/blocker == NON_EXECUTABLE_RESIDUAL_HOLDINGS
recommended_command does not contain rebuild-position-authority
recommended_command does not contain flatten-position
primary_incident_class == TRADEABILITY_POLICY
```

Also test row classification:

```text
0.00039982 full_projection_rebuild_portfolio_anchor degraded_recovery_unattributed -> NEAR_LOT_RESIDUAL + PORTFOLIO_ANCHOR_RESIDUAL + DEGRADED_RECOVERY_RESIDUAL
0.00009998 ledger direct -> LEDGER_SPLIT_RESIDUAL and/or TRUE_DUST depending on thresholds
```

Add tests ensuring this state is not confused with:

```text
actual broker/portfolio mismatch
actual lot projection divergence
actual executable exposure
legacy qty-only authority missing
unresolved order recovery
fee accounting blocker
```

P1 — read-only residual closeout plan

Implement:

```bash
MODE=live uv run bithumb-bot residual-closeout-plan
```

This command must not submit orders and must not mutate the DB.

It should output:

```text
broker_qty
portfolio_qty
local_residual_qty
residual_classes
exchange_min_qty
exchange_min_notional_krw
estimated_notional_krw
strategy_closeout_allowed
operator_closeout_possible
risk_of_holding_residual
risk_of_ignoring_residual
recommended_policy_options
```

Expected distinction:

```text
strategy_closeout_allowed=false when executable_lot_count == 0
operator_closeout_possible=true if broker residual satisfies exchange min qty/notional
```

This is not flatten. It is a planning/diagnostic command.

P1 — fill-trade linkage diagnostic

Implement or improve:

```bash
MODE=live uv run bithumb-bot diagnose-fill-trade-linkage
```

Prefer JSON support:

```bash
MODE=live uv run bithumb-bot diagnose-fill-trade-linkage --json
```

Optional repair:

```bash
MODE=live uv run bithumb-bot repair-fill-trade-linkage --dry-run
MODE=live uv run bithumb-bot repair-fill-trade-linkage --apply --yes
```

Do not auto-link ambiguous or unmatched rows.

P1 — observability and JSON consistency

Where feasible, add `--json` support to operator-facing commands involved in this incident class:

```text
health --json
ops-report --json
restart-checklist --json
broker-diagnose --json
residual-closeout-plan --json
diagnose-fill-trade-linkage --json
```

If implementing all is too large, document what was added and what remains.

Reports should separate:

```text
Ledger quality:
  fills linked/missing/matchable/unmatched
  accounting replay status
  broker snapshot status

Position truth:
  broker qty
  portfolio qty
  accounting replay qty
  open lots total qty
  projection convergence

Tradeability:
  executable qty
  residual qty
  new_entry_allowed
  closeout_allowed
  reason code

Repair:
  repair recommended or not
  rebuild-position-authority recommended or explicitly not recommended
```

Design constraints:

1. Do not weaken safety gates.
2. Do not make `dust_tracking` sellable by strategy order submission.
3. Do not infer executable authority from aggregate quantity.
4. Do not recommend flatten as a primary path for this incident class.
5. Do not mutate live DB state in diagnostic commands.
6. Do not treat historical repair events as current-state proof unless there is an explicit current-state attestation and convergence proof.
7. Preserve fail-closed behavior when evidence is missing, stale, ambiguous, or contradictory.
8. Prefer append-only repair events over direct row mutation if any repair is added.
9. All repair commands must have dry-run/preview and idempotent apply semantics.
10. Keep the patch focused: classify and expose the state first, repair tooling second.

Suggested files to inspect and likely modify:

```text
src/bithumb_bot/dust.py
src/bithumb_bot/lifecycle.py
src/bithumb_bot/position_authority_state.py
src/bithumb_bot/runtime_readiness.py
src/bithumb_bot/repair_plan.py
src/bithumb_bot/app.py
src/bithumb_bot/reason_codes.py
tests/test_dust.py
tests/test_position_authority_recovery_state.py
tests/test_operator_commands.py
tests/test_accounting_projection.py
```

Before editing, inspect the current code paths and existing tests. Reuse existing abstractions where possible. Do not duplicate state models if the repo already has a suitable one.

Acceptance criteria:

1. A reproducible test fixture for broker-matched residual-only holdings exists.
2. The EC2-like fixture is classified as `NON_EXECUTABLE_RESIDUAL_HOLDINGS` or an equivalent explicit first-class reason.
3. The fixture does not recommend `rebuild-position-authority`, `flatten-position`, `resume`, or `run`.
4. The fixture keeps startup/live readiness fail-closed.
5. The fixture distinguishes:

   * position truth is converged
   * executable authority is absent
   * residual inventory exists
   * repair is not the primary action
6. Residual rows expose enough detail to tell `near_lot_residual`, `ledger_split_residual`, `portfolio_anchor_residual`, and degraded recovery residual apart.
7. Existing dust and lot-native tests still pass or are updated to preserve their safety intent.
8. New tests cover negative cases where actual projection divergence or broker mismatch still recommends review/repair rather than residual policy.
9. If fill-trade linkage tooling is implemented, it must be dry-run first and must not guess unmatched rows.
10. Final CLI/report output is operator-useful and does not imply “just rebuild or flatten” for this class.

Run tests:

```bash
uv run pytest
```

If the full suite is too slow, at least run the targeted tests you modified or added, then document exactly what was run.

Final report format:

```text
Summary
- What changed and why.

Hypotheses
- H1: verified/falsified/inconclusive, with evidence.
- H2: verified/falsified/inconclusive, with evidence.
- H3: verified/falsified/inconclusive, with evidence.
- H4: verified/falsified/inconclusive, with evidence.
- H5: verified/falsified/inconclusive, with evidence.

Design changes
- New or changed state model fields.
- New reason codes.
- New CLI/report behavior.
- Why this is not flatten/manual repair/symptom suppression.

Safety
- How fail-closed behavior is preserved.
- Why strategy SELL authority is still lot-count based.
- Why repair recommendations are constrained.

Tests
- List new/changed tests.
- Commands run.
- Results.

Operational impact
- What an operator will now see for the EC2-like incident.
- What commands are safe to run.
- What commands are intentionally not recommended.

Follow-ups
- Any intentionally deferred JSON support, forensic export, linkage repair, or broader architecture cleanup.
```

The central goal is:

```text
Classify broker-matched residual-only holdings as a first-class tradeability state, without forcing flatten or position-authority rebuild.
```

