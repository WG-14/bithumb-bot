You are working in the GitHub repository `WG-14/bithumb-bot`.

The latest patch appears to have closed most of the previous structural holes: approved profile hashing is now explicit, evidence artifacts are content-hashed, source promotion paths are resolved and checked, env selector mismatch is detected, legacy candidate profile usage is marked, and profile writes are centralized.

Do **not** assume the system is complete. Your task is to directly inspect the repo, verify the hypotheses below, and apply only the additional patches needed to fully close the approved-profile runtime contract.

The core goal remains:

> “Profile verified” must mean the runtime env selects this exact approved profile, the profile’s parent/source/evidence chain is intact, live mode ambiguity is reason-coded, successful and failed runtime decisions expose the active artifact chain, and live execution fails closed on dangerous drift.

Do not weaken the verified manual approval flow. Do not introduce automatic research-to-live propagation.

## Core safety model to preserve

```text
research evidence
  ↓
promotion artifact
  ↓ operator review
approved paper profile
  ↓ paper validation evidence
approved live-dry-run profile
  ↓ live readiness evidence
approved small-live profile
```

Do **not**:

* connect the research registry directly to live execution
* auto-copy research parameters into env files
* auto-arm live trading
* weaken live arming checks
* weaken DB/storage-root isolation
* weaken lot-native SELL authority
* weaken ledger/reconciliation protections
* require exchange credentials for profile CLI/tests
* make deterministic hashes depend on timestamps

## Hypotheses to verify or falsify

Treat these as hypotheses, not facts. Inspect the repo before modifying code.

### Hypothesis A — live arming ambiguity reason codes may be produced but lost before CLI/runtime output

`expected_profile_modes_for_runtime(runtime)` may return both expected modes and a reason, for example:

```python
(set(), "live_mode_arming_flags_ambiguous")
(set(), "live_mode_not_dry_run_or_armed")
```

But `profile-verify` or runtime verification may pass only the first value into `verify_profile_against_runtime`, causing the precise reason code to be lost and replaced with a generic mode mismatch.

Verify whether this happens in:

* `profile_cli.py`
* runtime/preflight verification paths
* tests

Required patch if verified:

* Preserve the explicit reason from `expected_profile_modes_for_runtime`.
* Pass it into verification as an explicit argument, or return a small dataclass instead of a tuple.
* Ensure CLI JSON output and runtime/preflight audit output contain the precise reason.

Suggested shape:

```python
expected_modes, mode_reason = expected_profile_modes_for_runtime(runtime)

result = verify_profile_against_runtime(
    profile_path=profile_path,
    runtime=runtime,
    require_profile=True,
    expected_profile_modes=expected_modes,
    expected_profile_mode_reason=mode_reason,
    verify_source_promotion=True,
)
```

Use repo style and naming.

Required tests:

* `MODE=live`, `LIVE_DRY_RUN=true`, `LIVE_REAL_ORDER_ARMED=true` must fail with exact reason `live_mode_arming_flags_ambiguous`.
* `MODE=live`, `LIVE_DRY_RUN=false`, `LIVE_REAL_ORDER_ARMED=false` must fail with exact reason `live_mode_not_dry_run_or_armed`.
* These reasons must be visible in `profile-verify` JSON output.
* If runtime/preflight has separate output, assert the same reason is preserved there.

Priority: **high**.

---

### Hypothesis B — profile promotion may not re-check parent source/evidence drift before creating the child profile

`verify_profile_source_promotion(profile)` may correctly verify:

* source promotion artifact path policy
* source promotion content hash
* candidate/profile metadata drift
* evidence artifact path/hash drift

But `cmd_profile_promote()` may only do:

```python
parent = load_approved_profile(profile_path)
child = promote_profile_mode(...)
```

If `promote_profile_mode()` itself does not call `verify_profile_source_promotion(parent_profile)` before constructing the child profile, a paper profile whose source promotion or evidence artifact drifted could still be promoted.

Verify the full implementation of `promote_profile_mode()`.

Required patch if verified:

* At the start of `promote_profile_mode()`, call `verify_profile_source_promotion(parent_profile)`.
* Ensure this also checks parent evidence drift.
* If child generation re-loads source promotion, keep that too; do not remove useful checks.
* Make the failure reason specific, e.g. `source_promotion_content_hash_mismatch`, `paper_validation_evidence_content_hash_mismatch`, etc.

Required tests:

* Generate a paper profile.
* Mutate the source promotion artifact.
* Attempt `profile-promote` to `live_dry_run`.
* It must fail.
* Generate a live-dry-run profile with paper validation evidence.
* Mutate the paper validation evidence.
* Attempt promotion to `small_live`.
* It must fail.
* The CLI output must expose the reason code.

Priority: **high**.

---

### Hypothesis C — runtime selector alias handling may be inconsistent between env-file verification and settings-based runtime verification

Env-file parsing may accept both:

* `APPROVED_STRATEGY_PROFILE_PATH`
* `STRATEGY_APPROVED_PROFILE_PATH`

But `runtime_contract_from_settings()` may only read `APPROVED_STRATEGY_PROFILE_PATH`.

This can cause CLI and actual runtime/preflight behavior to diverge.

Verify:

* whether `STRATEGY_APPROVED_PROFILE_PATH` is a supported alias in config/settings
* whether docs mention it
* whether env-file contract and settings contract use the same precedence
* whether tests cover both canonical and alias selectors

Required patch if verified:

Either make alias support consistent:

```python
"profile_selector": (
    str(getattr(cfg, "APPROVED_STRATEGY_PROFILE_PATH", "") or "").strip()
    or str(getattr(cfg, "STRATEGY_APPROVED_PROFILE_PATH", "") or "").strip()
)
```

or remove/deprecate the alias consistently from:

* env parsing
* config
* docs
* tests

If both are supported, precedence must be deterministic and documented.

Required tests:

* canonical selector works in env-file verification
* canonical selector works in settings/runtime verification
* alias selector works or fails according to documented policy
* if both are set, precedence is deterministic and tested

Priority: **medium-high**.

---

### Hypothesis D — successful approved profile verification audit fields may not always reach startup logs and decision telemetry

`ProfileVerificationResult.audit_fields()` may include the right fields, but successful verification may not be explicitly merged into runtime policy/context. Failure paths may expose audit fields, while success paths may only expose a subset through `load_profile_or_promotion_regime_policy()`.

Verify actual propagation into:

* startup/preflight output
* strategy config
* decision context
* BUY block telemetry
* normal BUY decision telemetry
* paper/replay decision output if applicable

Required patch if verified:

* On successful verification, explicitly merge `result.audit_fields()` into the policy/context that reaches runtime decisions.
* Startup/preflight success should log or emit:

  * `approved_profile_verification_ok=true`
  * `approved_profile_path`
  * `approved_profile_mode`
  * `approved_profile_hash`
  * `promotion_content_hash`
  * `candidate_profile_hash`
  * `manifest_hash`
  * `dataset_content_hash`
  * evidence hash fields when present
* On failure, emit reason-coded fields, including:

  * `approved_profile_runtime_mismatch`
  * `profile_mode_mismatch`
  * `source_promotion_content_hash_mismatch`
  * `approved_profile_selector_mismatch`
  * `live_mode_arming_flags_ambiguous`
* Ensure no secrets are logged.

Required tests:

* Normal successful approved profile path includes profile/source/promotion/hash chain in startup/preflight audit.
* Normal BUY decision context contains the approved profile hash chain.
* Blocked BUY decision caused by profile mismatch contains a reason-coded profile failure.
* SELL/exit behavior is not blocked solely by profile/regime-policy failure unless existing repo safety rules explicitly require it.

Priority: **medium-high**.

---

### Hypothesis E — `load_profile_or_promotion_regime_policy()` validates approved profile structure but may not verify source/evidence drift

This helper may call `validate_approved_profile()` when the file is an approved profile, but not `verify_profile_source_promotion()`. The primary approved selector path may be safe if verification happens earlier, but helper-level behavior can become dangerous if:

* another caller uses it directly
* legacy `STRATEGY_CANDIDATE_PROFILE_PATH` points to an approved profile file
* paper/replay uses it without source verification

Verify all callers.

Required patch if verified:

* Add an option such as `verify_source: bool = False` or stricter default behavior for approved profiles.
* In `strategy_config.py`, choose source verification policy explicitly for:

  * approved profile selector path
  * legacy candidate profile path
  * paper/replay compatibility path
* If a legacy path points to an approved profile, either verify its source/evidence chain or mark it as legacy/regime-policy-only with a reason-coded audit field.
* Do not allow helper behavior to silently imply full approved-profile verification when only regime policy was loaded.

Required tests:

* Approved profile loaded through this helper with source drift fails when `verify_source=True`.
* Legacy path to approved profile either verifies source/evidence drift or emits explicit legacy-scope audit fields.
* Live mode with only legacy path still fails approved profile requirement.
* Paper/replay legacy path behavior is documented and tested.

Priority: **medium**.

---

### Hypothesis F — source/evidence path policy may only require “outside repo,” not “inside data root / reports custody”

Current path policy may block repo-local artifacts but allow arbitrary external absolute paths such as `/tmp/random.json`.

This may be acceptable for compatibility, but it weakens artifact custody if undocumented.

Verify:

* current path policy for source promotion artifacts
* current path policy for paper validation evidence
* current path policy for live readiness evidence
* current path policy for approved profile output
* docs/runbooks

Required patch if verified and repo policy should be stricter:

* For live profile promotion, require source/evidence/profile artifacts to be under `PathManager.data_dir()` or a reports/profile root.
* Optionally allow looser external paths in paper mode, but make the mode-specific policy explicit.
* Add reason codes such as:

  * `source_promotion_artifact_path_outside_data_root`
  * `paper_validation_evidence_path_outside_data_root`
  * `live_readiness_evidence_path_outside_data_root`

If strict data-root custody is intentionally not implemented now, update docs clearly:

* external absolute artifact paths are allowed
* repo-local paths are rejected
* all external artifacts are content-hashed
* operators are responsible for custody of external evidence files

Required tests, depending on chosen policy:

* repo-local path rejected
* missing path rejected
* external absolute path accepted or rejected according to documented policy
* data-root path accepted
* live promotion path policy is stricter if implemented

Priority: **medium**.

---

### Hypothesis G — `profile-diff` may be misunderstood as full artifact-chain verification

`profile-diff` may only compare profile values against env/runtime values, while source promotion drift and evidence drift are checked by `profile-verify`.

This can be correct, but operator output and docs must not imply that `profile-diff` verifies the full artifact chain.

Required patch if verified:

Either:

* Add fields to `profile-diff` output:

```json
{
  "source_promotion_verified": false,
  "evidence_verified": false,
  "use_profile_verify_for_artifact_chain": true
}
```

or:

* Add `profile-diff --verify-artifact-chain` and wire it to source/evidence verification.

Also update docs/runbooks to distinguish:

* `profile-diff`: env/runtime value comparison
* `profile-verify`: env selector + runtime contract + source/evidence artifact chain verification

Required tests:

* `profile-diff` output includes the clarification fields, or
* `profile-diff --verify-artifact-chain` fails on source/evidence drift

Priority: **low-medium**.

---

## Required implementation priorities

Unless repo inspection reveals a more severe safety issue, implement in this order:

1. Preserve exact ambiguous live flag reason codes through `profile-verify` and runtime/preflight.
2. Ensure `profile-promote` re-verifies parent source promotion and evidence drift before child creation.
3. Ensure successful approved profile verification audit fields reach startup/preflight and decision telemetry.
4. Make selector alias behavior consistent between env-file verification and settings/runtime verification.
5. Add explicit source verification policy to `load_profile_or_promotion_regime_policy()`.
6. Decide and document artifact custody path policy: repo-outside-only vs data-root/reports-only.
7. Clarify `profile-diff` output or add artifact-chain verification option.

## Runtime safety requirements

Live mode:

* Missing approved profile must fail closed before BUY.
* Legacy candidate profile path must not satisfy approved live profile requirements.
* Profile/env mismatch must fail closed before BUY.
* Source promotion drift must fail closed before BUY.
* Evidence drift must fail closed before BUY if evidence is part of the active profile chain.
* Ambiguous live flags must fail with precise reason codes.
* Successful verification must leave audit fields sufficient to reproduce active profile chain.

Paper/replay mode:

* Prefer the same verification path where an approved profile selector is used.
* If legacy candidate profile path is still allowed, it must be visibly marked as legacy/regime-policy-only.
* Drift or missing evidence behavior must be documented and tested.

SELL/exit:

* Do not block SELL/exit solely because candidate regime policy or approved profile verification failed, unless existing repo safety rules explicitly require that behavior.
* Add regression tests if this behavior is not already pinned.

## Observability requirements

Expose these fields on successful verification and failure paths wherever runtime/preflight/decision context exists:

* `approved_profile_path`
* `approved_profile_mode`
* `approved_profile_hash`
* `approved_profile_verification_ok`
* `approved_profile_block_reason`
* `source_promotion_artifact_path`
* `promotion_content_hash`
* `candidate_profile_hash`
* `manifest_hash`
* `dataset_content_hash`
* `paper_validation_evidence_path`
* `paper_validation_evidence_content_hash`
* `live_readiness_evidence_path`
* `live_readiness_evidence_content_hash`
* `approved_profile_mismatch_count`
* `approved_profile_mismatches`
* exact reason code for mode ambiguity, selector mismatch, source drift, evidence drift, or runtime mismatch

Do not log secrets.

## Recovery / rollback requirements

Ensure or document:

* failed verification stops live BUY before order submission
* operator can roll back by changing only the approved profile selector to a previous approved profile
* profile generation and promotion writes are atomic
* CLI errors are deterministic and machine-readable where JSON output exists
* no CLI mutates live env files automatically

## Documentation updates

Update README, research validation docs, runbooks, and env examples as needed.

Docs must clearly state:

* canonical approved profile selector and any supported alias
* selector precedence if both canonical and alias are set
* `STRATEGY_CANDIDATE_PROFILE_PATH` is legacy/compatibility/regime-policy-only if still supported
* `profile-promote` verifies parent source/evidence drift before creating child profiles
* `profile-diff` is not full artifact-chain verification unless you add that option
* `profile-verify` is the full check for env selector + runtime contract + source/evidence chain
* artifact path custody policy: repo-outside-only or data-root/reports-only
* live fails closed on missing/mismatched/drifted approved profile
* SELL/exit policy remains unchanged

## Required tests

Add or update tests in the repo’s existing style.

At minimum:

### Ambiguous live flags

* `profile-verify` with both live flags true fails with `live_mode_arming_flags_ambiguous`.
* `profile-verify` with both live flags false fails with `live_mode_not_dry_run_or_armed`.
* Runtime/preflight path preserves the same reasons.

### Promotion chain drift

* Mutating source promotion artifact after parent profile creation causes `profile-promote` to fail.
* Mutating paper validation evidence after live-dry-run profile creation causes promotion to `small_live` to fail.
* Failure output includes the specific drift reason.

### Selector alias consistency

* Canonical selector works in env-file and settings/runtime verification.
* Alias selector works or fails according to documented policy.
* If both canonical and alias are set, precedence is deterministic.

### Successful audit propagation

* Successful runtime/preflight verification includes the full hash chain.
* Successful BUY decision context includes the full hash chain.
* Blocked BUY due to profile mismatch includes reason-coded failure fields.
* No secret values appear in audit output.

### Helper source verification

* `load_profile_or_promotion_regime_policy(..., verify_source=True)` fails on source/evidence drift.
* Legacy candidate path to an approved profile does not silently imply full verification.
* Live mode with only legacy candidate path still fails approved profile requirement.

### Path policy

* repo-local artifact/evidence path rejected.
* missing path rejected.
* external absolute path accepted or rejected according to documented policy.
* data-root/reports path accepted.
* live profile promotion follows the stricter policy if implemented.

### `profile-diff` scope

* `profile-diff` output states artifact-chain verification is not performed, or
* `profile-diff --verify-artifact-chain` fails on source/evidence drift.

### Existing safety regression

Run or preserve tests for:

* live preflight
* arming
* run lock
* reconciliation
* duplicate-order prevention
* ledger behavior
* storage-root isolation
* lot-native SELL authority
* existing profile generation/diff/verify/promote tests

## Final report format

When finished, produce this exact report:

```markdown
## Summary

## Hypotheses Verified / Falsified

For each hypothesis A–G:
- status: verified, partially verified, falsified, or inconclusive
- repo evidence
- implemented change or reason no change was needed

## Design Changes

## Runtime Safety Behavior

Explain behavior for:
- paper
- replay
- live dry-run
- live armed / small-live
- SELL/exit paths

## CLI Behavior

Include expected behavior for:
- profile-generate
- profile-diff
- profile-verify
- profile-promote

## Artifact Chain Verification

Explain:
- source promotion verification
- evidence verification
- parent profile verification
- path policy
- hash policy

## Observability / Audit Fields

List fields emitted on success and failure.

## Recovery / Rollback

Explain operator recovery from failed verification and rollback to a previous approved profile.

## Tests Added or Updated

List test files and what each verifies.

## Backward Compatibility

Explain behavior for:
- old promotion artifacts
- old approved profiles
- legacy `STRATEGY_CANDIDATE_PROFILE_PATH`
- selector aliases

## Validation

Include exact commands run and results.

## Remaining Risks / Follow-ups

Only list intentionally deferred work or unresolved hypotheses.
```

Success criterion:

> A human can still manually approve promotion from research to paper/live, but the repo now verifies that runtime env selection, runtime settings, parent profile, source promotion artifact, evidence artifacts, and runtime telemetry all point to the same approved artifact chain. Live execution fails closed on dangerous drift, and operators can reproduce and audit exactly what happened.

