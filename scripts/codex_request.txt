You are Codex working directly inside the GitHub repository `WG-14/bithumb-bot`.

Continue from the completed DB schema-duplication patch. Pytest reportedly passes, but do not treat the patch as complete merely because tests pass. Inspect the actual repository state and determine whether any additional patch is still needed.

The core issue remains operational DB safety for an automated trading experiment system: schema creation, migration, validation, recovery diagnostics, and operator-facing observability must make it impossible to silently operate on a stale or malformed DB. The previous high-priority problem was the old `db_schema.py` path that could create `portfolio(cash, qty)`. That appears to be fixed, but verify it directly.

Do not ask clarification questions. Inspect, patch only if needed, test, and report.

## Current context to verify

The latest repo state appears to have addressed the major DB schema duplication issue:

* `src/bithumb_bot/db_schema.py` no longer owns old DDL.
* `db_schema.ensure_schema()` appears to warn and delegate to `db_core.ensure_schema()`.
* `db_schema.init_portfolio()` appears to initialize the current `cash_krw`, `asset_qty`, available/locked portfolio shape.
* `src/bithumb_bot/db_core.py` appears to define:

  * `SchemaValidationError`
  * `OPERATIONAL_SCHEMA_VERSION`
  * `OPERATIONAL_SCHEMA_META_KEY`
  * required runtime table/column contracts
  * required trigger contracts
  * runtime schema fingerprinting
  * schema diagnostics
  * `assert_current_schema()`
  * fail-fast rejection for old `portfolio(cash, qty)`
* `tests/test_portfolio_schema.py` appears to cover:

  * empty DB bootstrap
  * current DB reopen
  * deprecated `db_schema` shim behavior
  * aggregate legacy `portfolio(cash_krw, asset_qty)` backfill
  * old `portfolio(cash, qty)` rejection
  * malformed partial portfolio rejection
  * portfolio available/locked total mismatch rejection

Treat all of this as a hypothesis until verified from the code.

## Primary task

Determine whether the DB schema duplication problem needs any additional patch.

My current hypothesis is:

> No additional large structural patch is needed for the original DB schema duplication issue. The remaining useful patch, if not already implemented, is a small observability fix in `build_runtime_schema_diagnostics()`: it currently reports `"schema_version": OPERATIONAL_SCHEMA_VERSION` and `"accounting_projection_model": ACCOUNTING_PROJECTION_MODEL`, which are expected code-side values, not necessarily DB-observed values. This can confuse operators when diagnosing malformed or legacy DBs because a failing DB may still show `"schema_version": 1`.

Keep this hypothesis as a hypothesis. Verify it directly.

## Inspect first

Inspect at least:

* `src/bithumb_bot/db_schema.py`
* `src/bithumb_bot/db_core.py`
* `tests/test_portfolio_schema.py`
* any tests touching schema diagnostics
* any CLI or reporting command that exposes `diagnose_db_path()` or `build_runtime_schema_diagnostics()`
* all usages of:

  * `build_runtime_schema_diagnostics`
  * `diagnose_db_path`
  * `schema_version`
  * `accounting_projection_model`
  * `schema_meta`
  * `ensure_schema_ready=False`
  * `db_schema`

Confirm whether the old `portfolio(cash, qty)` DDL is fully gone from production code. It may remain only as a test fixture or simulated legacy DB shape.

## Hypotheses to verify or refute

### H1: The original high-risk schema duplication path is now blocked

Verify:

* `db_schema.py` cannot create `portfolio(cash, qty)`.
* It either delegates to `db_core` or fails explicitly.
* Tests prove the deprecated module no longer creates old columns.
* Old `portfolio(cash, qty)` DBs are rejected before trading/reporting/repair runtime use.

If false, fix this first. Do not proceed to minor observability work until this is safe.

### H2: Runtime schema validation now fails fast on unsupported or malformed schemas

Verify:

* `db_core.ensure_db()` with normal defaults runs schema creation/migration and validation.
* `portfolio(cash, qty)` is rejected with `SchemaValidationError`, not a late generic SQLite failure.
* malformed portfolio schemas fail early with clear messages.
* valid current DBs still pass.
* aggregate legacy `cash_krw` / `asset_qty` DBs still upgrade correctly.

If false, patch validation and tests before any cosmetic diagnostics work.

### H3: No production path normally bypasses schema validation

Verify usage of `ensure_schema_ready=False`.

Allowed:

* tests
* explicit diagnostics
* controlled low-level internals, if justified

Not allowed:

* normal trading
* reporting
* repair
* live startup
* runtime commands that can operate on DB state

If a production path bypasses validation, either remove that bypass or add a clear test proving it is safe and cannot operate on malformed DBs.

### H4: Diagnostics currently blur expected schema version and observed DB schema version

Verify `build_runtime_schema_diagnostics()`.

If it returns:

```python
"schema_version": OPERATIONAL_SCHEMA_VERSION
"accounting_projection_model": ACCOUNTING_PROJECTION_MODEL
```

then this is expected/code-side information, not necessarily observed DB metadata.

This is not a large blocking safety bug, but it is an operator observability problem. A malformed, legacy, or metadata-mismatched DB should not appear to have schema version `1` unless that is clearly labeled as the expected version.

## Patch requirements if H4 is confirmed

Make a small, backward-compatible diagnostics patch.

Do not remove existing fields yet if downstream code or tests may rely on them.

Add explicit expected vs observed fields:

```python
"expected_schema_version": OPERATIONAL_SCHEMA_VERSION
"observed_schema_version": <schema_meta.schema_version if present else None>
"expected_accounting_projection_model": ACCOUNTING_PROJECTION_MODEL
"observed_accounting_projection_model": <schema_meta.accounting_projection_model if present else None>
```

Recommended compatibility policy:

* Keep `"schema_version"` for now as a compatibility alias for `expected_schema_version`.
* Keep `"accounting_projection_model"` for now as a compatibility alias for `expected_accounting_projection_model`.
* Do not let those legacy names be the only version/model signals.
* If the DB has no `schema_meta` row, observed values must be `None`.
* If the DB has a mismatched `schema_meta` row, diagnostics must show the observed mismatched value and include a validation error.
* If the DB is current, expected and observed values should match.

Make the diagnostic output operator-safe:

* legacy `portfolio(cash, qty)` DB:

  * `status == "FAIL"`
  * `legacy_schema_detected == True`
  * `expected_schema_version == OPERATIONAL_SCHEMA_VERSION`
  * `observed_schema_version is None` unless schema_meta exists
  * `recommended_action` should remain recovery-oriented
* malformed portfolio DB:

  * `status == "FAIL"`
  * `malformed_portfolio_detected == True`
  * observed version should not pretend the DB is current
* valid current DB:

  * `status == "PASS"`
  * expected and observed schema version match
  * expected and observed accounting projection model match

## Optional CLI exposure

Inspect whether there is an existing DB doctor / validate-db / diagnostics CLI command.

If a command already exists, update its output/tests to include expected vs observed fields.

If no command exists, do not add a large new CLI framework for this task. It is acceptable to keep the patch limited to the reusable diagnostics function, unless adding a small command is trivial and consistent with existing CLI structure.

## Scope boundaries

Do not rewrite the DB layer.

Do not introduce a full migration framework in this patch.

Do not change trading logic, broker behavior, live config, `.env`, secrets, or external artifacts.

Do not silently migrate `portfolio(cash, qty)` unless the repo already explicitly chose that policy. The current preferred policy appears to be explicit rejection with recovery guidance.

Do not remove compatibility fields from diagnostics unless all callers/tests prove it is safe.

Do not weaken existing schema validation to make diagnostics easier.

Do not make destructive DB changes.

## Tests to add or update

Add targeted tests if missing.

At minimum, ensure tests prove:

1. Current DB diagnostics:

   * `status == "PASS"`
   * `expected_schema_version == OPERATIONAL_SCHEMA_VERSION`
   * `observed_schema_version == OPERATIONAL_SCHEMA_VERSION`
   * `expected_accounting_projection_model == ACCOUNTING_PROJECTION_MODEL`
   * `observed_accounting_projection_model == ACCOUNTING_PROJECTION_MODEL`
   * legacy compatibility fields still exist if preserved

2. Old `portfolio(cash, qty)` diagnostics:

   * `status == "FAIL"`
   * `legacy_schema_detected is True`
   * `expected_schema_version == OPERATIONAL_SCHEMA_VERSION`
   * `observed_schema_version is None` unless test explicitly creates schema_meta
   * recommended action points to restore backup or reviewed migration/repair before operating

3. Malformed portfolio diagnostics:

   * `status == "FAIL"`
   * `malformed_portfolio_detected is True`
   * missing columns are reported
   * observed schema version is not falsely presented as current unless metadata truly says so

4. Schema metadata mismatch diagnostics:

   * create a valid current DB
   * mutate `schema_meta.schema_version` or `accounting_projection_model`
   * call diagnostics without repairing metadata
   * assert:

     * `status == "FAIL"`
     * expected value and observed value differ
     * validation errors mention metadata mismatch

5. Existing schema tests still pass:

   * old `cash/qty` rejection
   * aggregate legacy backfill
   * current DB reopen
   * deprecated `db_schema` shim

Run relevant tests:

```bash
pytest tests/test_portfolio_schema.py
```

Also run any diagnostics/CLI tests if present.

Run full pytest if practical:

```bash
pytest
```

## Expected outcome

If the inspection confirms the original issue is already fixed and only the diagnostics ambiguity remains, implement only the small observability patch.

If inspection finds a deeper remaining issue, prioritize safety in this order:

1. prevent old schema creation
2. fail fast on unsupported/malformed DBs
3. prevent runtime validation bypass
4. clarify diagnostics expected vs observed fields
5. update tests and docs only as needed

## Final report format

Report in this exact structure:

1. Summary

   * Whether additional large DB schema patch was needed
   * What was changed
   * Whether the original risk remains blocked

2. Hypothesis results

   * H1 result: confirmed/refined/falsified
   * Evidence
   * Patch or no patch
   * Remaining risk

   Repeat for H2, H3, and H4.

3. Design/observability changes

   * Expected vs observed schema version behavior
   * Expected vs observed accounting projection model behavior
   * Backward compatibility fields retained or removed
   * Recovery/diagnostics behavior for legacy and malformed DBs

4. Tests added or changed

   * List each test
   * State what failure mode it prevents

5. Commands run

   * Exact commands
   * Pass/fail result

6. Operational impact

   * What an operator now sees for a valid DB
   * What an operator now sees for old `portfolio(cash, qty)`
   * What an operator now sees for malformed or metadata-mismatched DBs
   * How this improves recovery and prevents false confidence

7. Deferred work

   * Include only real deferred work
   * Do not hide required safety fixes here

The patch is successful only if it preserves the previous schema-safety guarantees and makes diagnostics unambiguous about the difference between code-expected schema metadata and DB-observed schema metadata.
