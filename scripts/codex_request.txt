You are working in the GitHub repository `WG-14/bithumb-bot`.

Your task is to continue from the latest residual-only holdings patch and finish the remaining structural cleanup. Do not treat the current diagnosis as unquestionable fact; verify each hypothesis against the current repository and tests. The goal is not to weaken safety gates, not to force live resume, not to flatten, not to manually patch the DB, and not to suppress warnings. The goal is to make the operator-facing model unambiguous and to prevent this entire class of incidents from being misread or mishandled.

The central issue is now narrower than the original incident:

```text
The latest patch correctly classifies broker-matched residual-only holdings as TRADEABILITY_POLICY / NON_EXECUTABLE_RESIDUAL_HOLDINGS and routes operators toward residual-closeout-plan instead of rebuild-position-authority or flatten.

However, some operator-facing surfaces still mix meanings:
- startup/recovery gate vs run-loop/tradeability gate
- halt recovery resume vs run loop resume
- projection convergence reason vs tradeability block reason
- repair candidate command vs top-level recommended command
- legacy audit trade-snapshot invariants vs ledger/replay truth
```

Do not dilute this into general cleanup. This is about making the repo safer and more operable for real live automated trading experiments.

Current verified EC2 state after the latest patch:

```text
broker_qty=0.00049980
portfolio_qty=0.00049980
projected_total_qty=0.00049980
projection_converged=1
local_residual_qty=0.00049980
executable_qty=0
strategy_closeout_allowed=0
operator_closeout_possible=1
run_loop_allowed=0
new_entry_allowed=0
reason_code=NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

Current `residual-closeout-plan` behavior is good and must be preserved:

```text
preview_mode=read_only_non_mutating
broker_qty=0.000499800000
portfolio_qty=0.000499800000
projected_total_qty=0.000499800000
projection_converged=1
local_residual_qty=0.000499800000
estimated_notional_krw≈58,068.809
residual_lot_count=2
residual_classes=DEGRADED_RECOVERY_RESIDUAL|LEDGER_SPLIT_RESIDUAL|NEAR_LOT_RESIDUAL|PORTFOLIO_ANCHOR_RESIDUAL|TRUE_DUST
strategy_closeout_allowed=0
operator_closeout_possible=1
exchange_sellable=1
run_loop_allowed=0
new_entry_allowed=0
reason_code=NON_EXECUTABLE_RESIDUAL_HOLDINGS
operator_next_action=residual_policy_review
recommended_command=uv run bithumb-bot residual-closeout-plan
```

Row-level residual classification must also be preserved:

```text
lot 353:
  qty_open=0.00039982
  source_mode=full_projection_rebuild_portfolio_anchor
  entry_decision_linkage=degraded_recovery_unattributed
  classes=NEAR_LOT_RESIDUAL|PORTFOLIO_ANCHOR_RESIDUAL|DEGRADED_RECOVERY_RESIDUAL

lot 355:
  qty_open=0.00009998
  source_mode=ledger
  entry_decision_linkage=direct
  classes=LEDGER_SPLIT_RESIDUAL|TRUE_DUST
```

The latest patch already added or modified relevant code around:

```text
src/bithumb_bot/runtime_readiness.py
src/bithumb_bot/lifecycle.py
src/bithumb_bot/repair_plan.py
src/bithumb_bot/app.py
src/bithumb_bot/recovery_policy.py
src/bithumb_bot/reason_codes.py
tests/test_position_authority_recovery_state.py
```

Inspect these first. Do not assume the patch is correct; verify.

## Working hypotheses to verify or falsify

### H1 — The core residual-only classification is now correct and should be preserved

Hypothesis:

```text
For broker-matched residual-only holdings:
  broker_qty == portfolio_qty == projected_total_qty
  projection_converged == true
  executable_qty == 0
  material residual_qty > 0
  no unresolved orders
  no active fee/accounting blocker

the correct classification is:
  primary_incident_class=TRADEABILITY_POLICY
  recommended_mode=residual_policy_review
  reason_code=NON_EXECUTABLE_RESIDUAL_HOLDINGS
  run_loop_allowed=0
  new_entry_allowed=0
  closeout_allowed=0
  repair is not recommended
  flatten is not primary
```

Verification:
Run or add tests equivalent to the EC2 fixture in `tests/test_position_authority_recovery_state.py`.

Expected:

```text
repair-plan top-level recommended_command == uv run bithumb-bot residual-closeout-plan
candidate rebuild-position-authority needed == false
candidate rebuild-position-authority recommended_command is empty/null/none
flatten_primary_recommendation == false
```

Falsification:
If the current fixture actually contains a broker mismatch, projection divergence, unresolved order, active accounting blocker, or executable exposure, document it and adjust the classification. Do not silently force this state into residual policy.

### H2 — The remaining problem is operator-surface ambiguity, not the core residual inventory model

Hypothesis:

```text
residual-closeout-plan is mostly correct, but health/restart/repair surfaces still expose ambiguous fields such as:
  can_resume=true
  resume_safety=safe
  gate_blocked=0
  reason=converged
while also saying:
  run_loop_allowed=0
  new_entry_allowed=0
  closeout_allowed=0
  Do not resume the run loop
  NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

Verification:
Inspect `health`, `restart-checklist`, `repair-plan --json`, and runtime readiness outputs. Add tests asserting that residual-only holdings never produce operator-facing text that implies run-loop resume is safe.

Falsification:
If `can_resume` or `resume_safety` are already semantically scoped to a narrower halt-recovery concept, preserve them but rename or expose explicit scoped fields so operators cannot misread them.

### H3 — Startup/recovery gate and tradeability/run-loop gate need explicit separation

Hypothesis:

```text
startup gate_blocked=0 can be true while tradeability gate is blocked.
This is not wrong by itself, but it is dangerous if printed as the only gate.
```

Verification:
Find the gate evaluation/output path in `engine.py`, `runtime_readiness.py`, and `app.py`. Confirm whether `gate_blocked=0` refers only to startup recovery blockers and not strategy tradeability.

Expected new output fields:

```text
startup_recovery_gate_blocked=0
tradeability_gate_blocked=1
tradeability_reason=NON_EXECUTABLE_RESIDUAL_HOLDINGS
run_loop_allowed=0
```

Falsification:
If startup gate is intended to cover tradeability too, then residual-only holdings must set `gate_blocked=1`. Do not allow a single unqualified `gate_blocked=0` to coexist with `run_loop_allowed=0` without explanation.

### H4 — `can_resume` and `resume_safety` are overloaded and need scope clarification

Hypothesis:

```text
can_resume=true / resume_safety=safe currently risks being read as "it is safe to start the live run loop",
even when the actual tradeability state says run_loop_allowed=0.
```

Verification:
Search for `can_resume`, `resume_safety`, `evaluate_resume_eligibility`, `evaluate_restart_readiness`, and their display surfaces.

Expected behavior for `NON_EXECUTABLE_RESIDUAL_HOLDINGS`:
Either:

```text
can_resume=false
resume_safety=policy_blocked
blockers=NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

or split into scoped fields:

```text
halt_recovery_can_resume=true
run_loop_can_resume=false
tradeability_resume_safety=policy_blocked
tradeability_block_reason=NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

Prefer the split if existing semantics require preserving halt recovery behavior.

Falsification:
If there is no operator-facing ambiguity after current code inspection and tests, document why and add a regression test to lock it down.

### H5 — repair-plan candidate commands remain too loose

Hypothesis:

```text
Top-level repair-plan output is correct, but individual candidate_repairs may still contain recommended_command values even when needed=false and safe_to_apply=false.
Example from EC2:
  fee-gap-accounting-repair:
    needed=false
    safe_to_apply=false
    recommended_command="uv run python bot.py resume"
```

This is dangerous for JSON consumers and operators.

Verification:
Inspect `src/bithumb_bot/repair_plan.py`.

Expected rule:

```text
if candidate.needed == false:
  candidate.recommended_command = ""
  candidate.command_applicable = false
  candidate.not_recommended_reason = <clear reason>
```

For residual-only holdings, repair candidates should explain:

```text
not_recommended_reason=current broker/portfolio/projection are converged; residual-only tradeability policy applies
```

Falsification:
If candidates are explicitly documented as static command templates rather than recommendations, rename the field to `command_template` and add a separate `recommended_command` that is empty when not applicable. Do not leave ambiguous command fields.

### H6 — `reason=converged` is too weak for top-level repair-plan

Hypothesis:

```text
repair-plan top-level reason currently shows projection reason only, e.g. reason=converged,
while the actual operator-relevant reason is NON_EXECUTABLE_RESIDUAL_HOLDINGS.
```

Verification:
Inspect repair-plan payload construction.

Expected:

```text
projection_reason=converged
tradeability_reason=NON_EXECUTABLE_RESIDUAL_HOLDINGS
reason=NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

or equivalent unambiguous fields.

Falsification:
If `reason` is intentionally projection-specific, rename it to `projection_reason` and add `primary_reason` / `tradeability_reason`.

### H7 — legacy `audit` SELL snapshot invariant conflicts with the newer residual/ledger model

Hypothesis:

```text
audit still fails on:
  trade id=35 SELL snapshot impossible: asset_after=0.0004998, qty=0.0004

But this is not impossible if the account had other residual holdings after the SELL.
audit-ledger passes, and broker/portfolio/projection are converged.
```

Verification:
Inspect `cmd_audit()` or the audit code path in `app.py` and any audit tests.

Expected correction:
Do not flag `SELL asset_after > sell qty` as impossible by itself.

Replace or narrow the invariant to:

```text
asset_after >= 0
cash_after >= 0
accounting replay == portfolio
broker == portfolio when broker evidence is available
lifecycle consumed qty is explainable
```

Clarify roles:

```text
audit = schema/order/fill/lifecycle consistency
audit-ledger = authoritative accounting replay consistency
```

Falsification:
If `asset_after > qty` really means something else in this schema, document the intended invariant and adjust the EC2 evidence interpretation. Do not leave `audit` failing while `audit-ledger` says OK without a clear operator distinction.

## Required design changes

### P0.1 Make gate surfaces scoped and non-contradictory

Implement explicit gate scopes in health/restart/startup outputs.

Expected operator-facing shape for residual-only holdings:

```text
startup_recovery_gate_blocked=0
tradeability_gate_blocked=1
tradeability_reason=NON_EXECUTABLE_RESIDUAL_HOLDINGS
run_loop_allowed=0
new_entry_allowed=0
closeout_allowed=0
operator_next_action=residual_policy_review
recommended_command=uv run bithumb-bot residual-closeout-plan
```

If `gate_blocked` remains, qualify it:

```text
startup_gate_blocked=0
tradeability_gate_blocked=1
```

Do not leave a bare `gate_blocked=0` as the only visible gate in a state where `run_loop_allowed=0`.

### P0.2 Clarify resume semantics

Either make residual-only holdings return:

```text
can_resume=false
resume_safety=policy_blocked
```

or split:

```text
halt_recovery_can_resume=true
run_loop_can_resume=false
tradeability_resume_safety=policy_blocked
```

Any output that says `can_resume=true` must make clear it is not permission to start the strategy run loop.

Add tests that fail if residual-only holdings produce:

```text
can_resume=true
resume_safety=safe
```

without a clearly scoped `run_loop_can_resume=false`.

### P0.3 Make repair-plan candidate recommendations safe for humans and JSON consumers

In `repair_plan.py`, ensure:

```text
candidate.needed=false => candidate.recommended_command is empty/null/none
candidate.command_applicable=false
candidate.not_recommended_reason present
```

For residual-only holdings, all repair candidates that are not needed must include context that the primary issue is tradeability policy, not repair.

Do not allow inactive candidates to carry `resume`, `run`, `flatten`, or `rebuild-position-authority --apply` as `recommended_command`.

### P0.4 Split projection reason from tradeability reason

Top-level repair/recovery payloads should distinguish:

```text
projection_reason=converged
tradeability_reason=NON_EXECUTABLE_RESIDUAL_HOLDINGS
primary_reason=NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

If existing API compatibility requires keeping `reason`, set it to the primary operator-relevant reason and add `projection_reason` separately.

### P0.5 Fix or re-scope legacy audit invariants

Find the `SELL snapshot impossible` logic and update it so that a SELL trade is not invalid merely because `asset_after > sell_qty`.

Add tests covering:

```text
pre-existing residual holdings remain after SELL
asset_after > sell_qty
audit should not fail solely for that
audit-ledger remains authoritative for replay/portfolio convergence
```

If full audit refactor is too large, at least:

* downgrade this specific condition to a warning when audit-ledger/accounting projection is OK, or
* replace it with a more correct lifecycle/replay check.

Do not hide real negative-balance or impossible principal accounting errors.

## Required tests

Add or update tests in the relevant files, likely:

```text
tests/test_position_authority_recovery_state.py
tests/test_operator_commands.py
tests/test_accounting_projection.py
tests/test_dust.py
```

Minimum test cases:

1. Residual-only fixture keeps core classification

```text
broker_qty == portfolio_qty == projected_total_qty
projection_converged == true
executable_qty == 0
residual_qty > 0
primary_incident_class == TRADEABILITY_POLICY
reason_code == NON_EXECUTABLE_RESIDUAL_HOLDINGS
recommended_command == residual-closeout-plan
run_loop_allowed == false
new_entry_allowed == false
closeout_allowed == false
```

2. Residual-only fixture does not recommend repair or flatten

```text
candidate rebuild-position-authority needed == false
candidate rebuild-position-authority recommended_command empty/null/none
flatten_primary_recommendation == false
no inactive candidate has recommended_command=resume/run/flatten/rebuild --apply
```

3. Gate output is scoped

```text
startup_recovery_gate_blocked == false
tradeability_gate_blocked == true
tradeability_reason == NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

4. Resume output is scoped

```text
run_loop_can_resume == false
tradeability_resume_safety == policy_blocked
```

If legacy `can_resume=true` remains, test must assert it is explicitly scoped and cannot be mistaken for run-loop permission.

5. Projection reason and tradeability reason are separate

```text
projection_reason == converged
primary_reason == NON_EXECUTABLE_RESIDUAL_HOLDINGS
tradeability_reason == NON_EXECUTABLE_RESIDUAL_HOLDINGS
```

6. Audit no longer rejects valid residual-after-SELL state

Create or reuse a fixture where:

```text
SELL qty=0.0004
asset_after=0.0004998
residual holdings explain remaining asset
cash_after >= 0
asset_after >= 0
accounting replay converges
```

Expected:

```text
audit does not fail solely because asset_after > sell_qty
audit-ledger remains OK
```

7. Negative controls

Ensure the new relaxed audit rule still catches:

```text
negative asset_after
negative cash_after
portfolio/replay mismatch
unexplainable lifecycle consumption
real broker/portfolio mismatch
actual lot projection divergence
actual unresolved order recovery
```

## Observability requirements

Operator outputs should make the final state unmistakable:

```text
PositionTruth:
  broker=0.00049980
  portfolio=0.00049980
  projected_total=0.00049980
  projection_converged=1

ExecutableAuthority:
  executable_qty=0
  sellable_executable_qty=0
  open_lot_count=0

ResidualInventory:
  residual_qty=0.00049980
  residual_classes=...
  operator_closeout_possible=1
  strategy_closeout_allowed=0

Tradeability:
  run_loop_allowed=0
  new_entry_allowed=0
  closeout_allowed=0
  reason=NON_EXECUTABLE_RESIDUAL_HOLDINGS

RepairRecommendation:
  repair_not_recommended
  residual-closeout-plan recommended
  flatten_not_primary
```

Ensure `residual-closeout-plan` remains read-only and non-mutating.

If feasible, add `--json` support or verify existing `--json` support for:

```text
residual-closeout-plan --json
restart-checklist --json
health --json
```

If not feasible in this patch, document it as follow-up.

## Safety constraints

Do not:

```text
- allow run loop resume for residual-only holdings
- allow new entries for material residual-only holdings
- make dust_tracking sellable by strategy order submission
- infer executable authority from aggregate qty
- recommend flatten as the primary action
- recommend rebuild-position-authority for broker/portfolio/projection-converged residual-only holdings
- mutate DB in diagnostic commands
- silently ignore audit failures without replacing them with correct invariants
```

Preserve:

```text
- fail-closed behavior
- lot-count based SELL authority
- residual-closeout-plan as read-only
- broker/portfolio/projection convergence checks
- historical repair events not being treated as current-state proof unless attested
```

## Suggested implementation order

1. Inspect current `runtime_readiness.py`, `repair_plan.py`, `app.py`, `engine.py`, and audit logic.
2. Add failing tests for the remaining ambiguities.
3. Fix repair-plan candidate command applicability.
4. Split projection/tradeability reason fields.
5. Scope startup/recovery gate vs tradeability gate output.
6. Scope resume semantics.
7. Fix or re-scope audit SELL snapshot invariant.
8. Run targeted tests.
9. Run full test suite if practical.

## Commands to run

At minimum:

```bash
uv run pytest tests/test_position_authority_recovery_state.py
uv run pytest tests/test_operator_commands.py
uv run pytest tests/test_accounting_projection.py
```

If practical:

```bash
uv run pytest
```

Do not claim full validation unless the full suite was actually run.

## Final report format

Return a final report with exactly these sections:

```text
Summary
- What changed.

Hypotheses
- H1: verified/falsified/inconclusive, with evidence.
- H2: verified/falsified/inconclusive, with evidence.
- H3: verified/falsified/inconclusive, with evidence.
- H4: verified/falsified/inconclusive, with evidence.
- H5: verified/falsified/inconclusive, with evidence.
- H6: verified/falsified/inconclusive, with evidence.
- H7: verified/falsified/inconclusive, with evidence.

Design changes
- Gate scoping.
- Resume scoping.
- Repair-plan candidate command safety.
- Reason field separation.
- Audit invariant update.

Safety
- How fail-closed behavior is preserved.
- Why strategy SELL authority is still lot-count based.
- Why residual-closeout-plan remains read-only.
- Why flatten/rebuild/resume are not recommended.

Tests
- New/changed tests.
- Commands run.
- Results.

Operational impact
- What EC2-like residual-only holdings now show.
- Which command is recommended.
- Which commands are intentionally not recommended.

Follow-ups
- Any deferred JSON support.
- Any deferred fill-trade linkage diagnostic/repair.
- Any deferred forensic bundle or broader architecture cleanup.
```

The intended final state is:

```text
PositionTruth: OK
Accounting: OK
LotProjection: OK
ExecutableAuthority: none
ResidualInventory: material residual exists
Tradeability: blocked
Repair: not recommended
Operator command: residual-closeout-plan
Run/resume: not recommended
Flatten: not primary
```

This patch should finish the operator-surface correctness work started by the residual-only holdings patch.

