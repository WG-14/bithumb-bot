You are Codex working directly on the GitHub repository `WG-14/bithumb-bot`.

Your task is not to apply a narrow symptom patch. Treat the latest EC2 incident as a concrete production failure signal and use it to improve the repository into a more robust, reproducible, and operable automated trading experiment system.

Work from the hypotheses below, but do not assume they are facts. Verify or falsify them against the repository code, tests, and executable behavior.

Primary incident context:

A live EC2 run reached a state where:

```text
portfolio / broker / replay qty converged to 0.00019986 BTC
open_position_lots contained one dust_tracking row:
  qty_open=0.00019986
  position_state=dust_tracking
  executable_lot_count=0
  dust_tracking_lot_count=1

trade_lifecycles showed the latest SELL 0.00080000 BTC closed two entry lots:
  entry_trade_id=24 -> exit_trade_id=26 matched_qty=0.0004
  entry_trade_id=25 -> exit_trade_id=26 matched_qty=0.0004

But rebuild-position-authority preview remained blocked:
  safe_to_apply=0
  stage=AUTHORITY_CORRECTION_PENDING
  repair_mode=correction
  eligibility_reason=sell_after_target_buy=1, target_lot_qty_fill_mismatch=target_qty=0.000199860000,fill_qty=0.000599980000

Runtime gate remained blocked:
  POSITION_AUTHORITY_CORRECTION_REQUIRED
  can_resume=false
  run_loop_allowed=0
  new_entry_allowed=0
```

The strongest current hypothesis is:

> A single SELL can close multiple BUY lots, but `position_authority_state.py` appears to calculate partial-close residual eligibility using total SELL quantity after the target BUY (`sell_after_qty`) rather than the quantity actually matched to the target entry trade via `trade_lifecycles.matched_qty`. In the incident, total SELL after the target BUY was `0.0008`, while the target BUY’s matched closed quantity was only `0.0004`. This caused `expected_residual_qty` to collapse to zero and prevented classification as a valid partial-close dust residual.

Do not treat that as proven. Verify it.

Important repository areas to inspect:

```text
src/bithumb_bot/position_authority_state.py
src/bithumb_bot/position_authority_repair.py
src/bithumb_bot/runtime_readiness.py
src/bithumb_bot/lifecycle.py
src/bithumb_bot/db_core.py
tests/test_position_authority_recovery_state.py
```

Specific code paths to inspect:

1. `build_position_authority_assessment`
2. `_partial_close_residual_state_converged`
3. `build_position_authority_rebuild_preview`
4. `apply_position_authority_rebuild`
5. lifecycle generation and SELL matching in `lifecycle.py`
6. runtime resume gate classification in `runtime_readiness.py`

The current suspected problematic pattern is conceptually:

```python
sell_after_qty = normalize_asset_qty(_row_float(sell_after_row, "qty"))
expected_residual_qty = normalize_asset_qty(max(0.0, fill_qty - sell_after_qty))

partial_close_residual_candidate = bool(
    ...
    and abs(sell_after_qty - canonical_executable_qty) <= _EPS
    ...
)

residual_state_converged = _partial_close_residual_state_converged(
    ...
    expected_closed_qty=sell_after_qty,
    ...
)
```

This may be wrong when one SELL closes multiple entry lots. For partial-close residual reasoning, the closed quantity should likely be the target entry’s lifecycle-matched quantity, not the total later SELL quantity.

Expected investigation output before coding:

1. Confirm whether the repository already has helper logic to aggregate `trade_lifecycles.matched_qty` by `entry_trade_id` and `exit_trade_id`.
2. Confirm whether that helper is currently used only in convergence validation, not in `expected_residual_qty` and candidate classification.
3. Confirm whether existing tests cover only the case where one SELL closes one target BUY lot, and do not cover the case where one SELL closes multiple entry lots.
4. Identify whether the production incident should be classified as:

   * already converged partial-close residual state,
   * residual normalization needed,
   * authority correction needed,
   * or a broader projection/authority conflict.
5. Explain any finding that contradicts the suspected cause.

Implementation priority:

Design-level correctness comes first. Do not just suppress `POSITION_AUTHORITY_CORRECTION_REQUIRED`. Do not weaken startup safety gates globally. Do not make “dust-only” automatically resume if authority evidence is inconsistent.

Preferred fix direction:

Promote target-entry lifecycle matched quantity to first-class evidence in position authority assessment.

Implement something equivalent to:

```python
target_lifecycle_matched_qty = ... # SUM(trade_lifecycles.matched_qty)
                                  # WHERE pair=? AND entry_trade_id=?
                                  # AND exit_trade_id IN sell_trade_ids

effective_closed_qty = (
    target_lifecycle_matched_qty
    if target_lifecycle_matched_qty > _EPS
    else sell_after_qty
)

expected_residual_qty = normalize_asset_qty(max(0.0, fill_qty - effective_closed_qty))
```

Then use `effective_closed_qty`, not total `sell_after_qty`, for:

1. partial-close residual candidate classification,
2. `_partial_close_residual_state_converged(... expected_closed_qty=...)`,
3. diagnostic fields,
4. repair preview evidence.

Keep `sell_after_qty` visible as a diagnostic field, but do not use it as target-lot closed quantity when lifecycle matched evidence exists.

Add explicit diagnostics to the returned assessment, such as:

```text
sell_after_target_buy_qty
target_lifecycle_matched_qty
effective_closed_qty
expected_residual_qty
partial_close_residual_candidate
residual_state_converged
```

Testing requirements:

Add regression tests that reproduce the incident class.

At minimum, add a test similar to:

```python
def test_partial_close_residual_uses_target_lifecycle_matched_qty_when_sell_closes_multiple_entries(...):
    ...
```

The test should model:

```text
BUY A: executable lot 0.0004 plus possible residual
BUY B: qty around 0.00059998, executable lot 0.0004 plus residual
SELL: 0.00080000
trade_lifecycles:
  A -> SELL matched_qty=0.0004
  B -> SELL matched_qty=0.0004
open_position_lots:
  B dust_tracking qty approximately 0.00019986 / 0.00019998
  executable_lot_count=0
  dust_tracking_lot_count=1
```

Expected behavior should be one of these, depending on the exact fixture state:

Case A, state already converged:

```text
partial_close_residual_candidate=True
residual_state_converged=True
needs_residual_normalization=False
recovery_stage=RESUME_READY
startup_reason=None
resume_allowed=True
resume_blockers=[]
run_loop_allowed=True
new_entry_allowed=True
```

Case B, state not yet converged but safely repairable:

```text
needs_residual_normalization=True
safe_to_normalize_residual=True
preview.repair_mode=residual_normalization
preview.safe_to_apply=True
preview.recommended_command includes rebuild-position-authority --apply --yes
```

Also ensure existing tests still pass, especially:

```text
test_incident_residual_is_created_at_buy_ingestion_then_left_by_sell_matching
test_partial_close_residual_normalization_replays_buy_and_sell_authority
test_authority_correction_repairs_incident_dust_row_with_historical_sell_history
tests around POSITION_AUTHORITY_CORRECTION_REQUIRED
tests around fee pending + authority repair
```

Do not remove or relax tests that ensure unsafe authority conflicts remain blocked.

Broader design review:

After fixing the immediate bug, inspect whether this incident reveals deeper design flaws. Evaluate these hypotheses explicitly:

Hypothesis 1:
`position_authority_state.py` conflates temporal SELL-after-target totals with target-entry lifecycle attribution.

Verification:
Compare all uses of `sell_after_qty`, `sell_after_count`, `sell_trade_ids`, and lifecycle matched quantities. Identify which fields are temporal diagnostics and which fields are authority evidence.

Hypothesis 2:
State authority is too distributed across `trades`, `fills`, `orders`, `open_position_lots`, `trade_lifecycles`, `portfolio`, broker snapshots, repair records, and projection publications.

Verification:
Map the truth hierarchy used by recovery and readiness. Identify where a derived projection can override or conflict with event-sourced lifecycle truth. Determine whether `open_position_lots` and `trade_lifecycles` are consistently treated as derived projections or sometimes as authoritative sources.

Hypothesis 3:
`rebuild-position-authority` has grown into a multi-purpose repair command rather than a clear state transition.

Verification:
List all repair modes selected by `build_position_authority_rebuild_preview`, including `correction`, `residual_normalization`, `portfolio_projection_repair`, `full_projection_rebuild`, and `rebuild`. Evaluate whether preview output gives a deterministic, machine-readable next action with enough evidence and postconditions.

Hypothesis 4:
Bithumb fee/fill detail incompleteness is forcing normal live operation through recovery paths too often.

Verification:
Inspect how fee pending observations and repairs materialize trades, fills, lots, and lifecycle projections. Identify whether provisional vs authoritative fills are cleanly separated. Do not attempt to solve all fee design issues in this PR unless needed for the incident fix, but document any structural risk.

Scope control:

This task must include the immediate correctness fix and regression tests.

This task should include small observability improvements if they are low-risk:

* expose `target_lifecycle_matched_qty`
* expose `effective_closed_qty`
* expose why lifecycle evidence was or was not accepted
* make preview/recovery output more actionable for this incident class

This task should not perform a broad CLI redesign unless it is small and necessary. Instead, document longer-term design changes in the final report.

Do not:

* bypass startup safety gates,
* classify all dust as harmless,
* make broker/local quantity convergence alone sufficient for resume,
* delete repair evidence,
* mutate live DB assumptions,
* add fragile tests that only match exact production row IDs,
* hide the distinction between total later SELL qty and target matched SELL qty.

Recommended implementation sequence:

1. Run the relevant tests before changes if feasible:

   ```bash
   uv run pytest tests/test_position_authority_recovery_state.py -q
   ```

2. Add the multi-entry single-SELL regression test first and confirm it fails for the expected reason.

3. Patch `position_authority_state.py` so target lifecycle matched quantity is used for partial-close residual authority.

4. Add or update diagnostic fields in the assessment and preview output.

5. Run targeted tests:

   ```bash
   uv run pytest tests/test_position_authority_recovery_state.py -q
   ```

6. If available, run broader tests:

   ```bash
   uv run pytest -q
   ```

7. Review whether the fix changes behavior of true unsafe authority conflicts. If any existing safety test starts passing incorrectly, stop and refine.

Final report format:

Return a concise but complete report with these sections:

1. Summary

   * What was changed.
   * What was intentionally not changed.

2. Verified Root Cause

   * State which hypotheses were confirmed, weakened, or rejected.
   * Include the specific code path that caused the incident.

3. Design Rationale

   * Explain why target lifecycle matched quantity is the correct authority for partial-close residuals.
   * Explain why total SELL-after-target quantity remains diagnostic only.

4. Tests

   * List new tests.
   * List existing tests that were relevant.
   * Include command results.

5. Operational Impact

   * Expected behavior for the EC2 incident after patch:

     ```text
     rebuild-position-authority preview should either report residual_normalization safe_to_apply=1
     or classify current state as RESUME_READY if lifecycle and dust projection are already converged.
     ```
   * Expected post-repair/resume signals:

     ```text
     POSITION_AUTHORITY_CORRECTION_REQUIRED absent
     can_resume=true
     run_loop_allowed=1
     new_entry_allowed=1
     open_exposure_qty=0
     dust_tracking_qty preserved
     ```

6. Remaining Structural Risks

   * State authority hierarchy concerns.
   * Repair command overloading.
   * Fee pending and provisional fill risk.
   * Projection lineage / generation ID gap.

7. Follow-up Recommendations

   * Separate diagnostic vs repair commands.
   * Make repair previews machine-readable.
   * Treat `open_position_lots` and `trade_lifecycles` explicitly as derived projections from immutable events.
   * Add scenario fixtures for real recovery incidents:

     * BUY fee missing -> repair -> open exposure
     * SELL fee missing -> repair -> dust-only resume
     * one SELL closes one BUY
     * one SELL closes multiple BUYs
     * stale dust projection
     * broker/local converged but lifecycle missing
     * lifecycle converged but publication missing
     * fee pending + authority correction simultaneous
     * dust above exchange minimum but below internal lot boundary

Make the final code change minimal but principled: fix the authority evidence model for this incident class, preserve safety, and leave the repository more testable and operable than before.

