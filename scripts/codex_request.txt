You are working in the `WG-14/bithumb-bot` repository.

Your task is to finish the target-delta execution refactor. Do not add another residual/dust workaround. The previous patch appears to have moved `target_delta` through decision/summary generation and service forwarding, but the remaining critical gap is likely the actual live submit authority in `broker/live.py`.

Context: the core operator goal remains unchanged: leftover BTC after a BUY/SELL cycle must not corrupt the bot state machine. After restart, the bot should compare broker-verified current position against persisted target position and converge by delta. Executable leftover BTC is current position. Only below-exchange-minimum delta is true dust. 

## Core hypothesis to verify

Treat this as a hypothesis. Verify it directly in code before patching:

```text
target_position.py:
  likely has the correct target_delta model.

execution_service.py:
  likely creates target_submit_plan with:
    source=target_delta
    authority=target_position_delta
    intent_type=target_delta_rebalance

LiveSignalExecutionService:
  likely forwards target_submit_plan to live_execute_signal.

broker/live.py:
  may still not have a first-class target_delta execution_submit_plan path.
  may still derive SELL quantity from lot-native authority.
  may still allow harmless dust / no_executable_exit_lot suppression to intercept executable target_delta SELL.

engine.py:
  may import target state helpers, but run_loop integration may still be insufficiently proven.
```

If any part of this hypothesis is wrong, report the exact finding and adjust the patch accordingly. Do not assume.

## Non-negotiable priority

The next patch must close the actual live execution path.

This must become true:

```text
EXECUTION_ENGINE=target_delta

target_submit_plan is not just logged.
target_submit_plan is not just forwarded.
target_submit_plan is the live submit authority.

For target_delta SELL:
  submit qty comes from execution_submit_plan["qty"].
  submit source is target_delta.
  submit authority is target_position_delta.
  submit intent_type is target_delta_rebalance.
  sellable_executable_lot_count is not required.
  RESIDUAL_LIVE_SELL_MODE is irrelevant.
  harmless dust / dust_only_remainder / no_executable_exit_lot must not suppress executable_delta.
```

## Scope

Inspect and patch only what is needed to close target_delta execution.

Start with:

```text
src/bithumb_bot/broker/live.py
src/bithumb_bot/execution_service.py
src/bithumb_bot/engine.py
src/bithumb_bot/target_position.py
src/bithumb_bot/db_core.py
src/bithumb_bot/dust.py
src/bithumb_bot/decision_context.py
src/bithumb_bot/runtime_readiness.py
tests/test_target_position_execution.py
tests/test_failsafe.py
```

Search for:

```text
target_delta
target_position_delta
target_delta_rebalance
target_submit_plan
execution_submit_plan
sellable_executable_lot_count
sellable_executable_qty
no_executable_exit_lot
dust_only_remainder
harmless_dust
RESIDUAL_LIVE_SELL_MODE
load_target_position_state
upsert_target_position_state
previous_target_exposure_krw
```

## Required verification before patching

### 1. Verify execution_service.py

Confirm whether `build_execution_decision_summary()` creates a target plan like:

```text
target_submit_plan.source == "target_delta"
target_submit_plan.authority == "target_position_delta"
target_submit_plan.intent_type == "target_delta_rebalance"
target_submit_plan.strategy_context == "target_delta"
target_submit_plan.side in {"BUY", "SELL", "NONE"}
target_submit_plan.qty == target_decision.submit_qty
target_submit_plan.submit_expected == target_decision.would_submit
target_submit_plan.block_reason == target_decision.block_reason
```

Confirm whether `EXECUTION_ENGINE=target_delta` returns an execution decision with:

```text
residual_submit_plan = None
buy_submit_plan = None
target_submit_plan = ...
```

Do not rewrite this if already correct.

### 2. Verify LiveSignalExecutionService

Confirm whether `LiveSignalExecutionService.execute()` already:

```text
reads decision_context["execution_decision"]["target_submit_plan"]
requires source == target_delta
requires authority == target_position_delta
requires submit_expected == true
requires block_reason == none
passes execution_submit_plan=target_submit_plan to live_execute_signal(...)
does not fall back to residual path for target_delta
```

If already correct, preserve it.

### 3. Verify broker/live.py

This is the most important part.

Confirm whether `live_execute_signal()` or its helpers have a first-class branch for:

```text
execution_submit_plan.source == "target_delta"
execution_submit_plan.authority == "target_position_delta"
```

If not, implement it.

The target_delta live path must:

```text
validate execution_submit_plan is a dict
validate source == target_delta
validate authority == target_position_delta
validate side in BUY/SELL
validate qty > 0
validate block_reason == none
validate submit_expected == true
use plan.qty as order_qty
use plan.side as side
use plan.idempotency_key if present
set intent_type=target_delta_rebalance
set strategy_context=target_delta
set submit_qty_source=target_position_delta
set source=target_delta
set authority=target_position_delta
```

It must still run generic live safety gates:

```text
kill switch / order submission halt
LIVE_DRY_RUN / LIVE_REAL_ORDER_ARMED policy
unresolved order gate
open order / duplicate intent protections
min_qty / min_notional / qty_step / max decimals
standard submit pipeline
fill application / accounting
idempotency / dedup
```

But it must not use these as the primary quantity authority in target_delta:

```text
sellable_executable_lot_count
sellable_executable_qty
position_state.normalized_exposure.sellable_executable_lot_count
position_state.normalized_exposure.sellable_executable_qty
residual_sell_candidate
RESIDUAL_LIVE_SELL_MODE
```

### 4. Verify engine.py run_loop target state

Confirm whether the live run loop actually does this:

```text
load target_position_state for settings.PAIR
pass previous_target_exposure_krw to build_execution_decision_summary
include execution_decision.target_submit_plan in decision_context
pass that decision_context to LiveSignalExecutionService
upsert target_position_state on BUY and SELL using target_new_exposure_krw
preserve previous target on HOLD
fail closed on HOLD without target state
```

If this is missing or only partially present, patch it.

Do not silently initialize HOLD to flat. In target_delta mode, HOLD without persisted target must remain:

```text
missing_persistent_target_state
submit_expected=false
```

### 5. Verify dust suppression interaction

In `EXECUTION_ENGINE=target_delta`, old lot-native dust suppression must not intercept executable target delta.

This must be true:

```text
target_dust_classification == executable_delta:
  no harmless dust suppression
  no no_executable_exit_lot suppression
  no dust_only_remainder suppression
  proceed to target_delta live submit if generic safety gates pass

target_dust_classification == true_dust:
  no live submit
  safe no-op
```

Preserve lot_native behavior. The old suppression path can remain valid only when `EXECUTION_ENGINE=lot_native`.

## Required implementation

### A. Add broker/live.py target_delta submit path

Implement a clear, auditable path. Prefer a helper if the file is large.

Suggested shape:

```python
def _target_delta_submit_plan(execution_submit_plan: dict[str, object] | None) -> dict[str, object] | None:
    ...
```

or an equivalent local resolver.

The target_delta path should produce a live execution intent equivalent to:

```text
side = plan["side"]
order_qty = plan["qty"]
submit_qty_source = "target_position_delta"
intent_type = "target_delta_rebalance"
strategy_context = "target_delta"
use_qty_intent_key = true
idempotency_key = plan["idempotency_key"]
entry_sizing = None unless existing BUY plumbing requires a harmless placeholder
exit_sizing = None unless existing SELL plumbing requires a harmless placeholder
canonical_sell = None
diagnostic_sell_qty = None
```

If existing code requires sizing/feasibility objects, create target_delta-specific objects or minimal adapters, but do not reintroduce lot-native SELL authority.

Add explicit log fields:

```text
execution_engine=target_delta
execution_submit_plan_source=target_delta
execution_submit_plan_authority=target_position_delta
target_delta_side
target_delta_qty
target_delta_notional_krw
target_delta_intent_type
target_delta_strategy_context
target_delta_idempotency_key
submit_qty_source=target_position_delta
```

### B. Prevent target_delta from falling into lot-native SELL suppression

Where the live code currently checks things like:

```text
canonical_sell is None
sellable_executable_lot_count <= 0
exit_allowed false
dust_only_remainder
harmless_dust
no_executable_exit_lot
```

ensure those checks do not block a validated target_delta executable plan.

Do not remove these checks for lot_native.

### C. Confirm run_loop target state integration

If missing, wire this flow in `engine.py`:

```text
target_state = load_target_position_state(conn, pair=settings.PAIR)
previous_target_exposure_krw = target_state.target_exposure_krw if target_state else None

execution_summary = build_execution_decision_summary(
    ...,
    previous_target_exposure_krw=previous_target_exposure_krw,
)

decision_context["execution_decision"] = execution_summary.as_dict()

if EXECUTION_ENGINE == "target_delta":
    target_decision = execution_summary.as_dict()["target_shadow_decision"] or equivalent
    if raw_signal in BUY/SELL and target_new_exposure_krw is not None:
        upsert_target_position_state(...)
    if raw_signal == HOLD:
        preserve previous target state
```

Use existing repo style and function signatures. If this is already implemented, do not duplicate it; add tests proving it.

## Required tests

Add or update tests that directly prove the remaining execution path.

### 1. broker/live.py uses target_delta plan qty

Test:

```text
EXECUTION_ENGINE=target_delta
execution_submit_plan = {
  source: "target_delta",
  authority: "target_position_delta",
  intent_type: "target_delta_rebalance",
  strategy_context: "target_delta",
  side: "SELL",
  qty: 0.0004998,
  notional_krw: valid,
  submit_expected: true,
  block_reason: "none",
  idempotency_key: "..."
}
sellable_executable_lot_count=0
RESIDUAL_LIVE_SELL_MODE=telemetry
```

Expected:

```text
live path uses plan.qty
does not require sellable_executable_lot_count
does not require residual sell policy
does not block with residual_live_sell_mode_telemetry
reaches standard submit pipeline or mocked broker submit with SELL qty=0.0004998
```

### 2. target_delta SELL does not call lot-native sell sizing as quantity authority

Mock or instrument as appropriate.

Expected:

```text
target_delta SELL does not derive order_qty from build_sell_execution_sizing
target_delta SELL does not use position_state.normalized_exposure.sellable_executable_qty
target_delta SELL uses execution_submit_plan["qty"]
```

If the existing pipeline still calls generic validation helpers, that is acceptable only if they do not replace the target_delta qty.

### 3. LiveSignalExecutionService rejects invalid target plans

Test cases:

```text
missing target_submit_plan
source != target_delta
authority != target_position_delta
submit_expected=false
block_reason != none
side=NONE
qty <= 0
```

Expected:

```text
executor not called
no residual fallback
```

### 4. target_delta executable_delta bypasses harmless dust suppression

Test:

```text
EXECUTION_ENGINE=target_delta
target_submit_plan.target_dust_classification=executable_delta
old decision context indicates:
  dust_only_remainder
  harmless_dust
  sellable_executable_lot_count=0
```

Expected:

```text
target_delta submit proceeds
harmless dust suppression is not recorded
no no_executable_exit_lot suppression
```

### 5. true_dust no-op remains safe

Test:

```text
EXECUTION_ENGINE=target_delta
target_submit_plan.submit_expected=false
target_submit_plan.block_reason=delta_below_exchange_min
target_dust_classification=true_dust
```

Expected:

```text
no live submit
no error
clear true_dust observability
```

### 6. engine.py run_loop integration

Add integration-style tests, not just pure DB function tests.

Cases:

```text
A. persisted target=0, broker_qty=0.0004998, signal=SELL or HOLD:
   expected target_delta SELL plan reaches LiveSignalExecutionService/live executor.

B. persisted target=0, broker_qty below exchange min, signal=HOLD or SELL:
   expected true_dust no-op, no state-machine corruption.

C. persisted target=100000, broker has partial BTC exposure, signal=HOLD:
   expected BUY only missing delta if executable.

D. no persisted target, signal=HOLD:
   expected missing_persistent_target_state and no submit.

E. unsafe readiness, open order or broker/local mismatch:
   expected no submit, explicit target block reason.
```

### 7. lot_native regression

Prove existing behavior remains intact when:

```text
EXECUTION_ENGINE=lot_native
```

Expected:

```text
lot-native SELL still uses sellable_executable_lot_count
residual policy still behaves as before
harmless dust suppression still applies in lot_native
```

## Acceptance criteria

This patch is complete only if all are true:

```text
1. target_submit_plan is consumed by LiveSignalExecutionService.
2. broker/live.py has an explicit target_delta execution_submit_plan path.
3. target_delta live SELL uses execution_submit_plan["qty"].
4. target_delta live SELL does not require sellable_executable_lot_count.
5. target_delta live SELL does not depend on RESIDUAL_LIVE_SELL_MODE.
6. target_delta executable_delta is not blocked by harmless_dust / dust_only_remainder / no_executable_exit_lot.
7. target_delta true_dust no-ops safely.
8. run_loop reads persisted target state and passes previous_target_exposure_krw into execution summary.
9. run_loop upserts target state on BUY/SELL.
10. HOLD preserves persisted target.
11. HOLD without persisted target fails closed.
12. unsafe readiness blocks target_delta submit with explicit reason.
13. lot_native behavior remains backward compatible.
14. tests prove actual live path behavior, not just summary creation.
```

## Non-goals

Do not rewrite strategy logic.

Do not delete lot_native mode.

Do not delete recovery gates.

Do not trust broker balance blindly.

Do not make live order submission less safe.

Do not add new residual/dust policy switches.

Do not treat executable leftover BTC as dust in target_delta.

Do not require perfect zero BTC after SELL. The model is target convergence with true dust tolerated.

## Suggested test commands

Run focused tests first:

```bash
pytest tests/test_target_position_execution.py -q
pytest tests/test_failsafe.py -q
```

Run any new broker/live or engine integration tests you add.

Then, if feasible:

```bash
pytest -q
```

If the full suite cannot run due to environment constraints, report the exact command and failure.

## Final report format

Report exactly in this format:

```markdown
# Target-Delta Live Authority Closure Report

## Summary
- What was already correct:
- What was still incomplete:
- What changed in this patch:

## Verification
### target_position.py
- Findings:

### execution_service.py
- Findings:

### LiveSignalExecutionService
- Findings:

### broker/live.py
- Findings:
- New target_delta path:

### engine.py run_loop
- Findings:
- Target state load/upsert behavior:

## Key Behavior Proven
### Executable leftover BTC with target=0
- Input:
- Expected:
- Actual:

### True dust leftover with target=0
- Input:
- Expected:
- Actual:

### HOLD with persisted target after restart
- Input:
- Expected:
- Actual:

### HOLD without persisted target
- Input:
- Expected:
- Actual:

### Unsafe readiness
- Input:
- Expected:
- Actual:

## Safety and Compatibility
- Safety gates preserved:
- lot_native compatibility:
- residual policy independence in target_delta:
- dust suppression behavior by engine mode:

## Tests
- New/updated tests:
- Commands run:
- Results:

## Observability
- New/verified fields:
- Example executable_delta log:
- Example true_dust log:
- Example unsafe_state log:

## Remaining Risks / Follow-ups
- Known limitations:
- Recommended next cleanup:
```

Remember: the previous patches likely built the target_delta brain and service forwarding. This task is to make `broker/live.py` use that plan as the actual live order authority. The patch is not complete until target_delta SELL can submit executable leftover BTC without strategy lot authority, residual policy, or harmless dust suppression intercepting it.

