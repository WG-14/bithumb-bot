You are working in the GitHub repository `WG-14/bithumb-bot`.

The previous patch appears to be directionally correct: `approved_profile.py` exists, profile schema/hash/transition/verification is implemented, CLI commands such as `profile-generate`, `profile-diff`, `profile-verify`, and `profile-promote` exist, docs were updated, and tests reportedly pass.

Do **not** treat the current implementation as complete. Your task is to directly inspect the repo, verify the hypotheses below, and apply the additional patches needed so that “profile verification” actually means:

> The env selects this exact approved profile, the profile’s parent/source/evidence chain is intact, deterministic hashing is explicit and reproducible, runtime audit/decision telemetry records the active artifact chain, and live paths fail closed on dangerous drift.

Do not weaken the core design. The system must remain a verified manual approval flow, not an automatic research-to-live propagation mechanism.

## Core safety objective

Preserve this model:

```text
research evidence
  ↓
promotion artifact
  ↓ operator review
approved paper profile
  ↓ paper validation evidence
approved live-dry-run profile
  ↓ live readiness evidence
approved small-live profile
```

Do **not**:

* connect the research registry directly to live execution
* auto-copy research parameters into env files
* auto-arm live trading
* weaken live arming checks
* weaken DB/storage-root isolation
* weaken lot-native SELL authority
* weaken ledger/reconciliation protections
* require exchange credentials for profile CLI/tests
* make deterministic hashes depend on timestamps

## Hypotheses to verify or falsify

Treat each item below as a hypothesis. Inspect the repo before modifying code. In the final report, state whether each was verified, partially verified, falsified, or inconclusive.

### Hypothesis A — Approved profile hashing may rely on implicit global behavior

The docs say `generated_at` is excluded from deterministic `profile_content_hash`, but the implementation may compute the profile hash from the full payload after inserting `generated_at`.

Verify whether `generated_at` is explicitly excluded by profile-specific hashing code, or whether this currently depends on implicit behavior inside a shared hashing helper.

Required design if verified:

* Add profile-specific hash payload functions.
* Make exclusion of `generated_at` and `profile_content_hash` explicit in approved profile code.
* Use the profile-specific hash function consistently in profile build, validate, promote, verify, and tests.

Suggested shape:

```python
PROFILE_HASH_EXCLUDED_FIELDS = {"profile_content_hash", "generated_at"}

def approved_profile_hash_payload(profile: dict[str, Any]) -> dict[str, Any]:
    return {
        k: v
        for k, v in profile.items()
        if k not in PROFILE_HASH_EXCLUDED_FIELDS
    }

def compute_approved_profile_hash(profile: dict[str, Any]) -> str:
    return sha256_prefixed(content_hash_payload(approved_profile_hash_payload(profile)))
```

Adapt names to existing code style.

### Hypothesis B — `profile-promote` evidence may be only a non-empty string

The current transition logic may accept `paper_validation_evidence` or `live_readiness_evidence` if the string is non-empty, even if the path does not exist, points inside the repo, or cannot be hashed.

Required design if verified:

* Treat evidence as a verifiable artifact, not a free-text string.
* Evidence path must exist.
* Evidence path must be resolved to an absolute path before storing.
* Evidence path must pass the repo’s runtime-artifact path policy.
* Evidence file content hash must be computed and stored in the child profile.
* Evidence path and evidence content hash must be included in the child profile’s deterministic hash.
* If the repo already has report/data-root path helpers, reuse them.
* If semantic evidence validation is out of scope, explicitly document that only existence/path/hash are verified for now.

Expected fields, adjusted to existing naming conventions:

```json
{
  "paper_validation_evidence_path": "/abs/path/...",
  "paper_validation_evidence_content_hash": "sha256:...",
  "live_readiness_evidence_path": "/abs/path/...",
  "live_readiness_evidence_content_hash": "sha256:..."
}
```

### Hypothesis C — Source promotion path policy may be weaker than profile output path policy

The implementation may verify source promotion content hash but allow source promotion paths that are relative, repo-local, or outside the intended reports/data-root area.

Required design if verified:

* Store `source_promotion_artifact_path` as a resolved absolute path.
* Reject repo-local source promotion artifacts.
* Prefer allowing only PathManager/data-root reports paths, if compatible with current repo design.
* Add a distinct failure reason for source promotion path policy mismatch, not just generic hash mismatch.
* Continue verifying source promotion content hash.

### Hypothesis D — `profile-diff` / `profile-verify` may not check that the env selects the same profile being verified

The CLI may compare profile values against env values, but not verify that the env’s approved profile selector points to the exact `--profile` path.

This can allow:

```bash
uv run bithumb-bot profile-verify --profile A.json --env live.env
```

to pass even though `live.env` actually points to `B.json`.

Required design if verified:

* Parse the env profile selector, e.g. `APPROVED_STRATEGY_PROFILE_PATH` or existing canonical variable.
* Compare the resolved selector path against the resolved `--profile` path.
* Add a mismatch field such as:

```json
{
  "field": "approved_profile_selector",
  "expected": "/abs/path/profile_A.json",
  "actual": "/abs/path/profile_B.json"
}
```

* In live modes this must fail.
* For paper mode, fail by default unless the repo has an explicit documented reason to allow warning-only behavior.
* If multiple legacy selector names exist, document precedence and include the chosen selector in output/audit.

### Hypothesis E — Live arming flag ambiguity may surface as an unclear `expected=[]` profile mode error

The current mode inference may return an empty expected profile mode set when live flags are ambiguous, for example:

* `LIVE_DRY_RUN=true` and `LIVE_REAL_ORDER_ARMED=true`
* both false in live mode

Required design if verified:

* Replace ambiguous empty expected-mode behavior with explicit failure reasons:

  * `live_mode_arming_flags_ambiguous`
  * `live_mode_not_dry_run_or_armed`
* Ensure CLI and runtime/preflight output surfaces these reason codes.
* Add tests for both ambiguous combinations.

### Hypothesis F — Legacy `STRATEGY_CANDIDATE_PROFILE_PATH` may dilute the approved profile contract

The repo may still allow legacy `STRATEGY_CANDIDATE_PROFILE_PATH` to act as a regime-policy-only source while `APPROVED_STRATEGY_PROFILE_PATH` is absent.

Required design if verified:

* In live mode, reject configurations where only the legacy candidate profile path is set and no approved profile selector is present.
* In paper/replay, either:

  * preserve legacy behavior only as a compatibility path, and record audit fields such as `legacy_candidate_profile_path_used=true` and `legacy_profile_contract_scope=regime_policy_only`, or
  * migrate to approved profile only if compatible with the repo.
* Update README/env docs to make the approved profile selector canonical.
* Ensure legacy path cannot silently satisfy live approved profile requirements.

### Hypothesis G — Profile promotion may not re-check source promotion drift

`profile-verify` may verify source promotion drift, but `profile-promote` may only validate the parent profile and not re-open/re-hash the source promotion artifact before creating the child profile.

Required design if verified:

* `profile-promote` must verify the parent profile’s source promotion artifact before creating live-dry-run or small-live child profiles.
* If the source promotion artifact has drifted, promotion must fail.
* Add a test: generate a paper profile, mutate the source promotion artifact, then attempt `profile-promote`; it must fail.

### Hypothesis H — Audit fields may exist but not reach startup audit / decision telemetry consistently

`ProfileVerificationResult.audit_fields()` may exist, but successful verification fields may not consistently reach startup logs, preflight output, and decision context.

Required design if verified:

* On successful runtime/preflight verification, log or emit:

  * approved profile path
  * approved profile mode
  * approved profile content hash
  * source promotion content hash
  * candidate profile hash
  * manifest hash
  * dataset content hash
  * profile verification status
* On failure, emit reason-coded block fields, for example:

  * `approved_profile_runtime_mismatch`
  * `profile_mode_mismatch`
  * `source_promotion_content_hash_mismatch`
  * `approved_profile_selector_mismatch`
  * `live_mode_arming_flags_ambiguous`
* Ensure decision context or equivalent runtime telemetry contains the active artifact chain for both successful and blocked BUY decisions.
* Avoid leaking secrets.

### Hypothesis I — CLI output path validation may be duplicated instead of centralized

The repo may have `write_approved_profile_atomic()` and a path-policy helper, while `profile_cli.py` duplicates output path checks.

Required design if verified:

* Centralize path validation and atomic writes in approved profile utilities.
* Make `profile-generate` and `profile-promote` use the same helper.
* Keep error reason strings stable and testable.

### Hypothesis J — Docs may overstate evidence semantics

Docs may say paper validation or live readiness evidence is required, while the implementation only verifies a reference string or path/hash and does not semantically validate metrics.

Required design if verified:

* Update docs/runbooks to state exactly what is currently verified:

  * evidence file existence
  * allowed path policy
  * evidence content hash
  * parent/source/profile hash chain
* Explicitly mark semantic validation of paper observation metrics or live readiness metrics as future work unless implemented now.
* If implementing a minimal evidence schema is practical, add:

  * `evidence_type`
  * `profile_hash`
  * `observation_start`
  * `observation_end`
  * `decision_count`
  * `blocked_count`
  * `operator_approved`
  * `content_hash`

## Required implementation priorities

Implement in this order unless repo inspection shows a stronger safety issue:

1. `profile-diff` / `profile-verify` must check that the env selector path matches the `--profile` path.
2. `profile-promote` must re-verify parent source promotion drift before transition.
3. Evidence must become a verifiable artifact: existing path, allowed path, content hash, stored in profile, included in profile hash.
4. Approved profile hash computation must explicitly exclude `generated_at`.
5. Successful runtime/preflight and decision telemetry must include profile/source/promotion/hash audit fields.
6. Legacy `STRATEGY_CANDIDATE_PROFILE_PATH` must not satisfy live approved profile requirements.
7. Ambiguous live arming flag combinations must produce explicit reason-coded failures.
8. CLI path validation and atomic writes should be centralized.
9. Docs must accurately describe evidence semantics and remaining future work.

## Required tests

Add or update tests in the repo’s existing style. At minimum, cover:

### Hashing

* Changing `generated_at` does not change `profile_content_hash`.
* Changing any strategy/runtime-relevant field does change `profile_content_hash`.
* `profile_content_hash` itself is excluded from its own hash payload.

### Evidence artifacts

* `profile-promote` fails when paper validation evidence path does not exist.
* `profile-promote` fails when live readiness evidence path does not exist.
* `profile-promote` rejects repo-local evidence paths, or otherwise enforces the repo’s artifact path policy.
* Generated child profile stores resolved evidence path and evidence content hash.
* Changing evidence content changes the resulting child profile hash.
* If verification checks evidence drift, mutating evidence after promotion causes verification failure; if not, document why and add a test for the intended behavior.

### Source promotion path / drift

* Profile generation stores source promotion path as resolved absolute path.
* Repo-local source promotion artifacts are rejected, unless the repo has an explicit allowed-fixture exception for tests.
* `profile-verify` fails if source promotion artifact content changes.
* `profile-promote` fails if parent source promotion artifact content changed after parent profile creation.

### Env selector matching

* `profile-verify --profile A --env env-pointing-to-A` passes when all values match.
* `profile-verify --profile A --env env-pointing-to-B` fails with `approved_profile_selector_mismatch`.
* Missing approved profile selector fails in live mode.
* Missing approved profile selector fails or warns in paper mode according to documented policy.
* If both approved and legacy selector env vars are set, precedence is deterministic and tested.

### Runtime mode

* live dry-run expects `live_dry_run` profile.
* live armed / small-live expects `small_live` profile.
* paper profile is rejected for live armed.
* `LIVE_DRY_RUN=true` and `LIVE_REAL_ORDER_ARMED=true` fails with `live_mode_arming_flags_ambiguous`.
* both false in live mode fails with `live_mode_not_dry_run_or_armed`.

### Legacy path behavior

* live mode with only `STRATEGY_CANDIDATE_PROFILE_PATH` and no approved profile selector fails.
* paper/replay legacy usage emits explicit audit fields if still supported.

### Observability

* Successful startup/preflight includes approved profile hash, promotion hash, candidate profile hash, manifest hash, and dataset content hash.
* Blocked BUY decision includes reason-coded profile failure fields.
* No secrets are emitted.

### Existing safety regression

Ensure all existing tests still pass for:

* live preflight
* arming
* run lock
* reconciliation
* duplicate-order prevention
* ledger behavior
* storage-root isolation
* lot-native SELL authority

## Recovery / rollback requirements

Implement or document the operator recovery path:

* If verification fails, live BUY must be blocked before order submission.
* Operator can roll back by changing only the approved profile selector to a previous approved profile.
* Failed profile generation or promotion must not leave partial artifacts; writes must be atomic.
* CLI errors should be deterministic and machine-readable where the repo already supports JSON output.
* No CLI should mutate live env files automatically.

## Documentation updates

Update README, research validation docs, runbooks, and env examples as appropriate.

Docs must clearly state:

* `APPROVED_STRATEGY_PROFILE_PATH` or the repo’s canonical approved profile selector is the runtime profile selector.
* `STRATEGY_CANDIDATE_PROFILE_PATH` is legacy / compatibility / regime-policy-only if still supported.
* `profile-generate` and `profile-promote` do not mutate env files.
* `profile-diff` and `profile-verify` check env selector path equality.
* Evidence artifacts are verified by path policy and content hash.
* Semantic validation of paper/live readiness metrics is implemented or explicitly future work.
* Live mode fails closed on missing/mismatched approved profile.
* SELL/exit authority is not blocked solely because candidate regime policy is missing or invalid, unless existing repo safety rules already say otherwise.

## Final report format

When finished, produce this exact report structure:

```markdown
## Summary

## Hypotheses Verified / Falsified

For each hypothesis A–J:
- status: verified, partially verified, falsified, or inconclusive
- repo evidence
- implemented change or reason no change was needed

## Design Changes

## CLI Behavior

Include exact commands and expected pass/fail behavior:
- profile-generate
- profile-diff
- profile-verify
- profile-promote

## Runtime Safety Behavior

Explain behavior for:
- paper
- replay
- live dry-run
- live armed / small-live
- SELL/exit paths

## Evidence Artifact Handling

Explain:
- path policy
- content hash
- stored fields
- whether semantic validation is implemented or future work

## Observability / Audit Fields

List the fields emitted on success and failure.

## Recovery / Rollback

Explain how an operator recovers from failed verification or rolls back to a previous profile.

## Tests Added or Updated

List test files and test intent.

## Backward Compatibility

Explain behavior for:
- old promotion artifacts
- old approved profiles
- legacy `STRATEGY_CANDIDATE_PROFILE_PATH`

## Validation

Include exact commands run and results.

## Remaining Risks / Follow-ups

Only list items that are truly out of scope or intentionally deferred.
```

Success criterion:

> A human can still manually approve promotion from research to paper/live, but the repo now verifies that env selection, runtime settings, parent profile, source promotion artifact, evidence artifacts, and runtime telemetry all point to the same approved artifact chain. Live execution fails closed on dangerous drift, and the operator can reproduce and audit exactly what happened.

